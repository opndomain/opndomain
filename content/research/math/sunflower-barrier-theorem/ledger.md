# KNOWLEDGE LEDGER

## PROVEN RESULTS (use these, do not re-derive)
(none yet)

## DEAD APPROACHES (do NOT retry these)
- [D1] Upgrading the harmonic layered model to an \exp(\Theta(w\log w)) lower bound while keeping the target notion “reach \Lambda(w)-spread for one global scalar \Lambda(w)” is impossible, because the explicit regularizer achieves \Lambda-spread with only \exp(O(w\log\log w)) loss. (killed by The Formaliz — REASON:  (killed by compressed, iter 0)
- [D2] Any repaired target depending only on witness size |T| is dead, because it collapses to the scalar threshold \Lambda_b=\sup_t b_t^{-1/t} and is defeated by the canonical regularizer. (killed by The Adversary, iter 1) — REASON:  (killed by compressed, iter 0)
- [D3] Full symmetrization that hides the layers while preserving arbitrary-witness renormalization and making witness statistics depend only on |T| is dead, because multiplicative recursion destroys the desired harmonic obstruction. (killed by The Builder, iter 1) — REASON:  (killed by compressed, iter 0)
- [D4] Replacing |T| by any single weighted witness size \sum_i \omega_i |T\cap \text{layer }i| is dead, because it has a cheap linear majorant and the harmonic family regularizes with only \exp(O(w\log\log w)) loss. (killed by The Adversary, iter 1) — REASON:  (killed by compressed, iter 0)

## OPEN QUESTIONS (attack these)
- [O1] Find a minor-closed typed target on dual-rank geometry strong enough to force the desired lower bound in the representable setting, if such a target exists. — CONTEXT: The Formalizer (iter 1) — CONTEXT: compressed (iter 0)
