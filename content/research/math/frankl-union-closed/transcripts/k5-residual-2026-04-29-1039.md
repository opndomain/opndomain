# Research Workshop: Frankl's Union-Closed Sets Conjecture: Closing the B < 75/119 Residual

**Date:** 2026-04-29
**Models:** The Proof Writer (codex/gpt-5.4), The Calculator (codex/gpt-5.4), The Explorer (codex/gpt-5.4)
**Iterations:** 5, **Build passes:** 3

## Problem

Frankl's Union-Closed Sets Conjecture (1979): For any finite union-closed family F, some element belongs to at least half the sets. Target: prove max p_x >= 0.39.

RIGOROUSLY VERIFIED RESULTS (do not re-derive):

[P1] FIBER DECOMPOSITION: For minimal M in F, define fibers L_T = {S ⊆ M : T ∪ S in F} for T in projection P. Then F = ⨆_T {T ∪ S : S in L_T}, each L_T union-closed on M with M in L_T. Root fiber L_∅ = {M}.

[P2] k≤4 BOUND: Any F with minimal set size ≤ 4 has max p_x ≥ 2/5 = 0.40.

[P3] Any <0.39 counterexample has all minimal sets of size ≥ 5.

[P4] SINGLE-FIBER LP: For k=5 the sharp extremal bad fiber is L = {∅,{a},{b},{a,b},M} with avg|S| = 9/5, label e={a,b}.

[P5] CROSS-FIBER FORCING (verified): For ANY two bad fibers T,U with labels e,f, L_{T∪U} ⊇ 2^{e∪f} ∪ {M}. If |e∪f|=3: avg ≥ 17/9. If |e∪f|=4: avg ≥ 37/17.

[P6] BAD-FIBER FREQUENCY: For vertex i, p_i^bad = B/5 + (2/5)d_i where d_i = Σ_{e∋i} B_e.

[P7] r-FOLD OVERLAP DENSITIES (verified): If a fiber is A_z-good for r different z, avg|S| ≥ a_r where a_1=37/17, a_2=57/25, a_3=69/29, a_4=76/31, a_5=5/2. Adding sets cannot lower these below a_r.

[P8] GLOBAL SUM BOUND (verified): Σ_x p_x ≥ 9/5 + (7/50)C where C = Σ_z G_z (total slice-counted good mass). Combined with C ≥ (17/10)B gives Σ_x p_x ≥ 9/5 + (119/500)B.

[P9] HIGH-B THRESHOLD (verified): B ≥ 75/119 ≈ 0.630 implies max p_x ≥ 0.39.

[P10] DISJOINT PAIR THRESHOLD: If disjoint bad labels e,f have B_e + B_f ≥ 1/4, then max p_x ≥ 0.39.

[P11] STAR ESTIMATE (verified): If all bad labels contain fixed x, then p_x ≥ (3/5)B + (1/32)(1-B). At B=75/119 this gives 0.3897 — short by 0.0003.

[P12] K4-SLICE BOUND: If induced K_4 has bad mass W_A ≥ 969/1972, then max p_x ≥ 0.39.

DEAD APPROACHES (verified dead, do NOT retry):
[D1] Entropy fiber-defect — saturates at 0.382.
[D2] Variance concentration — covariances Theta(n^2).
[D3] Union bound on minimal 3-sets — vacuous at t≥6.
[D4] Unaugmented recursion F^{-x} — gives only 1/4.
[D5] Single-fiber LP for k=5 — gives only 9/25 = 0.36.
[D6] Subfamily frequency transfer to ambient family.
[D7] N^2 distinct mixed unions from N fibers — collapse to O(N).
[D8] Denominator-drop trick (computing favorable ratio on subsystem, ignoring outside mass).
[D9] Product-chain extremal [P17 was false] — collapses worse than claimed.
[D10] Overlapping-pair multiplicity bound mult_V ≤ min(t,s) — false as union-map claim.
[D11] Star + uniform 1/32 good-floor — yields only 0.3897 < 0.39 at threshold.
[D12] Treating r-fold overlap as pure loss — overlap is bonus density, not waste.

## Constraints

Close the residual regime B < 75/119 in the k=5 case. The star case is closed for B ≥ 0.612. The general case is closed for B ≥ 0.630. The remaining gap: low-B configurations where bad mass is spread thinly. Close this gap to prove max p_x ≥ 0.39 for ALL union-closed families. Tag all results as PROVEN/DEAD/OPEN.

## Creative Direction

NEW RESULTS FROM LATEST WORKSHOP (verified, do not re-derive):

[P13] A/B DECOMPOSITION: For fiber L with {x} in L, define A = {S ⊆ Y : S in L}, B = {S ⊆ Y : S∪{x} in L} where Y=M\{x}. Then A union-closed, B union-closed, A ⊆ B, p_x(L) = |B|/(|A|+|B|). If Y ∉ A then |A| ≤ 8.

[P14] FIBER VERTEX FLOOR: Any fiber on 5-set with M in L has p_x(L) ≥ 1/17, not 1/32. Sharp.

[P15] STAR-FORCED TRIPLE: If L contains 2^{x,i,j} ∪ {M}, then p_x(L) ≥ 9/17. Sharp.

[P16] TWO-CUBE SPAN: If L contains 2^{x,i,j} ∪ {M} AND 2^{x,k,l} ∪ {M} with {i,j}∪{k,l}=Y, then p_x(L) ≥ 1/2. Sharp.

[P17] IMPROVED STAR: p_x ≥ (3/5)B + (1/17)(1-B) + (8/17)b_2, where b_2 = second-largest arm mass. Star closed for B ≥ 563/920 ≈ 0.612.

[P18] STAR COROLLARY: Star closed whenever b_2 ≥ (563-920B)/750.

THE EXACT REMAINING GAP: Non-star configurations with B < 75/119 ≈ 0.630. In these configs, bad labels are NOT all through one vertex, so there exist non-intersecting bad pairs. Each non-intersecting pair forces a 4-support good fiber with p_x ≥ 9/17 for x in the support. The question is whether the forced good mass from ALL such pairs, combined with the 1/17 universal floor on remaining fibers, pushes some vertex past 0.39.

CONCRETE ATTACK: Solve the 10-variable LP on K_5 edge masses B_e. For each vertex x, p_x = B/5 + (2/5)d_x + good contributions. The good contributions come from: (a) forced fibers from non-intersecting bad pairs (p_x ≥ 9/17 if x in support, 1/17 otherwise), (b) all other good fibers (p_x ≥ 1/17). Find the adversary-optimal (B_e) that minimizes max_x p_x subject to Σ B_e = B < 75/119 and B_e + B_f < 1/4 for disjoint e,f. If the LP minimum exceeds 0.39, the proof is complete.

---

## Iteration 1

### EXPLORE

#### The Proof Writer (codex/gpt-5.4)

**TRANSLATED**

Write the residual `k=5` problem as a weighted overlap network on `K_5`.

Let the vertices be the five elements of `M`.  
Each bad label `e` is an edge of `K_5`, with weight `B_e >= 0`, and
\[
B=\sum_{e\in E(K_5)} B_e < \frac{75}{119}.
\]
For a vertex `x`, let
\[
d_x=\sum_{e\ni x} B_e.
\]
By `[P6]`, the bad-fiber contribution to the frequency of `x` is
\[
p_x^{\rm bad}=\frac B5+\frac25 d_x.
\]

For each `z in M`, write `A_z=M\setminus\{z\}`.  
A disjoint bad pair inside `A_z` forces an `A_z`-supported good fiber; by `[P15]`, every vertex in `A_z` gets frequency at least `9/17` on that fiber, while `[P14]` gives the universal floor `1/17` on any good fiber.

So the translated task is:

> Route forced good mass onto the five 4-vertex supports `A_z`, then show some vertex load
> \[
> \frac B5+\frac25 d_x + \text{(good-floor)} + \text{(support bonus)}
> \]
> exceeds `0.39`.

The obstruction in this language is precise: disjoint bad pairs create support-certificates, but the union map can collapse many certificates onto the same good fiber. So the missing ingredient is a deterministic lower bound converting edge-weight overlap into certified support mass.

---

**PROVEN Lemma 1**

Let `H_z` be any disjointly allocated mass of good fibers carrying a certified `A_z`-cube. Then for every vertex `x`,
\[
p_x\ge \frac{1-B}{17}+\frac B5+\frac25 d_x+\frac{8}{17}\sum_{z\neq x} H_z.
\]

**Proof.**

Every good fiber contributes at least `1/17` to `p_x` by `[P14]`, so the total good-mass floor is `(1-B)/17`.

Now fix `z`. On any good fiber counted in `H_z`, every vertex of `A_z` has frequency at least `9/17` by `[P15]`. Relative to the universal `1/17` floor, each such vertex gets an extra bonus of
\[
\frac{9}{17}-\frac{1}{17}=\frac{8}{17}.
\]
Thus vertex `x` receives this extra bonus from exactly those allocated supports `A_z` with `z\neq x`.

Adding the bad part `[P6]` gives
\[
p_x\ge \frac{1-B}{17}+\left(\frac B5+\frac25 d_x\right)+\frac{8}{17}\sum_{z\neq x}H_z.
\]
QED.

---

**PROVEN Lemma 2**

For each vertex `z`,
\[
W_z:=\sum_{e\subseteq A_z} B_e = B-d_z.
\]
Hence, in the residual regime, for every `z`,
\[
W_z<\frac{969}{1972}
\quad\text{and therefore}\quad
d_z> B-\frac{969}{1972}.
\]

**Proof.**

`W_z` is the bad mass on the induced `K_4` obtained by deleting `z`. Since every bad edge either lies inside `A_z` or is incident to `z`,
\[
W_z=B-d_z.
\]

If some `W_z >= 969/1972`, then `[P12]` closes the case. Therefore every residual configuration satisfies
\[
W_z<\frac{969}{1972}.
\]
Substituting `W_z=B-d_z` gives
\[
d_z>B-\frac{969}{1972}.
\]
QED.

---

**PROVEN Lemma 3**

For each `z`, let
\[
\kappa_z=\max\{B_{ab}+B_{cd},\; B_{ac}+B_{bd},\; B_{ad}+B_{bc}\},
\]
where `{a,b,c,d}=A_z`. Then
\[
\kappa_z\ge \frac{W_z}{3},
\qquad
\sum_{z} \kappa_z \ge B.
\]

**Proof.**

Inside `A_z`, the six edges split into the three perfect matchings
\[
\{ab,cd\},\quad \{ac,bd\},\quad \{ad,bc\}.
\]
Their sums add up to `W_z`, so the largest is at least `W_z/3`.

Also each edge of `K_5` avoids exactly three vertices, hence appears in exactly three of the `W_z`. Therefore
\[
\sum_z W_z = 3B.
\]
Using `\kappa_z \ge W_z/3`,
\[
\sum_z \kappa_z \ge \frac13 \sum_z W_z = B.
\]
QED.

---

**DEAD Lemma 4 (support-agnostic averaging is insufficient below `0.4887`)**

Assume one could prove the optimistic routing bound
\[
H_z\ge \kappa_z \quad\text{for all } z.
\]
Even then, a proof using only Lemma 1 and averaging over the five vertices yields at best
\[
\max_x p_x \ge \frac1{17}+\frac{864}{1275}B.
\]
So this line alone only closes the range
\[
B\ge \frac{28713}{58752}\approx 0.4887,
\]
not the full residual regime.

**Proof.**

Average the Lemma 1 bound over `x`. Since
\[
\frac15\sum_x d_x = \frac{2B}{5},
\]
and each support `A_z` contains four vertices,
\[
\frac15\sum_x \sum_{z\neq x} H_z = \frac45 \sum_z H_z.
\]
Thus some `x` satisfies
\[
p_x\ge \frac{1-B}{17}+\frac B5+\frac25\cdot \frac{2B}{5}+\frac{8}{17}\cdot \frac45 \sum_z H_z.
\]
Under `H_z\ge \kappa_z` and Lemma 3,
\[
\sum_z H_z \ge \sum_z \kappa_z \ge B.
\]
Hence
\[
p_x\ge \frac{1-B}{17}+\frac B5+\frac{4B}{25}+\frac{32B}{85}
= \frac1{17}+\frac{864}{1275}B.
\]
To force `p_x >= 39/100`, this requires
\[
\frac1{17}+\frac{864}{1275}B \ge \frac{39}{100},
\]
equivalently
\[
B\ge \frac{28713}{58752}\approx 0.4887.
\]
So any argument that only uses total support budget, without aligning support mass to vertex degree, cannot finish the whole problem. QED.

---

**OPEN**

The graph translation isolates the exact missing bridge:

> Find a deterministic support-routing inequality that correlates the certified `A_z`-mass with the slice weight `W_z=B-d_z`, not merely with the total `\sum_z W_z`.

A usable form would be something like
\[
H_z \ge \phi(W_z)
\]
with `\phi` increasing, because then Lemma 1 couples the bonus term to the same vertex parameter `d_x` that already appears in the bad term.

The resistant feature is the many-to-one collapse of pair-forcing into the same good fiber. In graph language: we can see overlap certificates on the edge side, but we do not yet have a sharp transport theorem from certificate mass to support mass. That is the structural bottleneck.

#### The Calculator (codex/gpt-5.4)

**Translation**

`TRANSLATED` Let the 10 bad-label masses be edge loads `b_{ij} >= 0` on `K_5`, with

\[
\sum_{1\le i<j\le 5} b_{ij}=B,\qquad d_x=\sum_{y\ne x} b_{xy}.
\]

The raw score floor at vertex `x` is the verified affine term

\[
p_x^{\rm raw}= \frac B5+\frac25 d_x.
\]

Now introduce 5 four-vertex chambers. Chamber `Q_v` is the `K_4` on `V\setminus\{v\}`. A forced good fiber on `Q_v` gives frequency at least `9/17` to the 4 vertices inside the chamber and at least `1/17` to the missing vertex. So if chamber `Q_v` carries certified good mass `g_v`, then every vertex gets the universal floor `(1-B)/17`, and vertex `x` gets an extra bonus `(8/17)\sum_{v\ne x} g_v`. Hence the translated score is

\[
p_x \ge \Phi_x:=\frac{1-B}{17}+\frac B5+\frac25 d_x+\frac{8}{17}\sum_{v\ne x} g_v.
\]

If one uses only the currently visible “one disjoint pair forces one chamber” mechanism, the natural chamber lower bound is

\[
g_v \ge m_v:=\max\{\min(b_{ab},b_{cd}),\min(b_{ac},b_{bd}),\min(b_{ad},b_{bc})\},
\]

where `{a,b,c,d}=V\setminus\{v\}`.

**Boundary checks at \(B=75/119\)**

`PROVEN` Old star bound:
\[
\frac35B+\frac1{32}(1-B)=\frac{53}{136}\approx 0.38970588<0.39.
\]

`PROVEN` Improved star baseline:
\[
\frac35B+\frac1{17}(1-B)=\frac{809}{2023}\approx 0.39990114>0.39.
\]

`PROVEN` Uniform edge load `b_{ij}=B/10=15/238` gives
\[
d_x=\frac{2B}{5},\qquad m_v=\frac B{10},
\]
so
\[
\Phi_x=\frac{1-B}{17}+\frac{9B}{25}+\frac{8}{17}\cdot\frac{4B}{10}
=\frac{743}{2023}\approx 0.36727632.
\]
So the coarse chamber model does not reach `0.39` even at the threshold.

**Stress test of the coarse translated model**

`PROVEN` The following edge-load vector satisfies all visible boundary constraints of the coarse model:

Edges ordered as
`(12,13,14,15,23,24,25,34,35,45)`:
\[
(0.0270674,\,0.1260955,\,0.1321868,\,0.0337409,\,0.0442797,\,0.0323189,\,0.0393750,\,0.1177854,\,0.0356744,\,0.0417281).
\]

Checks:
- Total load is `75/119`.
- Every disjoint pair sum is `< 0.17647 < 1/4`.
- Every `K_4` slice load is below `969/1972`; the five slice loads are approximately
  `0.31116, 0.48721, 0.30642, 0.30623, 0.47973`.

For this vector,
\[
d \approx (0.31909,\,0.14304,\,0.32383,\,0.32402,\,0.15052),
\]
\[
m \approx (0.04173,\,0.04173,\,0.03938,\,0.03938,\,0.04428),
\]
and therefore
\[
\max_x \Phi_x \approx 0.35605.
\]

`DEAD` The 10-variable edge-mass program is not self-closing if the only chamber bonus inserted is `g_v=m_v` plus the universal `1/17` floor. Even after imposing the disjoint-pair cap and the verified `K_4` slice cap, that translated program still allows top score well below `0.39`.

**What resists translation**

`OPEN` The missing ingredient is a stronger chamber-capacity law: edge loads alone do not tell you how much distinct good mass the disjoint-pair forcing injects into a 4-vertex chamber, because many pair interactions can collapse onto the same chamber. In optimization language, the unresolved object is a lower bound of the form

\[
g_v \ge \Gamma(\text{edge profile on }Q_v)
\]

that is strictly stronger than `m_v`, or a cross-chamber coupling inequality linking the five `g_v` simultaneously.

That is the main computational conclusion from this explore pass: the residual regime is not blocked by degree balancing; it is blocked by insufficient certified chamber mass.

#### The Explorer (codex/gpt-5.4)

**TRANSLATED**

Work on the ground set `X = {1,2,3,4,5}` as a weighted graph `H` on `X`.

- Put weight `b_e = B_e` on edge `e ⊂ X`, where `e` is a bad label.
- Total bad mass is `B = Σ_e b_e < 75/119`.
- For each vertex `x`, let `d_x = Σ_{e∋x} b_e`.
- For each missing vertex `v`, let the 4-vertex slice be `A_v = X \ {v}` and let
  `W_v = Σ_{e⊂A_v} b_e = B - d_v`.

This is now a weighted `K_5` / matching problem.

The verified frequency lower bound becomes

`p_x ≥ (1-B)/17 + B/5 + (2/5)d_x + (8/17)h_x`

where `h_x` is the total mass of good fibers supported on 4-sets containing `x`. In graph language: bad edges give the `d_x` term; each forced 4-support good fiber gives a bonus to the four vertices it supports.

The disjoint-pair condition is:

- For every matching `{e,f}` of size 2 in `K_5`, `b_e + b_f < 1/4`.

So the residual problem is:

- Non-star weighted graphs on 5 vertices
- Total edge-weight `B < 75/119`
- No 2-matching of weight `≥ 1/4`
- Show some vertex gets enough support bonus to make `p_x ≥ 0.39`.

---

**PROVEN**

1. For each slice `A_v ≅ K_4`, its 6 edges split into the 3 perfect matchings of `K_4`. Therefore some opposite pair in `A_v` has weight at least `W_v/3`.

Reason: the three perfect matchings partition `E(K_4)`.

2. Slice masses satisfy

`Σ_v W_v = 3B`.

Reason: each edge of `K_5` is contained in exactly 3 of the five 4-vertex slices.

3. For each vertex `x`,

`Σ_{v≠x} W_v = 2B + d_x`.

Reason: `W_v = B - d_v`, so
`Σ_{v≠x} W_v = 4B - Σ_{v≠x} d_v = 4B - (2B - d_x)`.

4. The “flattest” non-star support pattern is the 5-cycle. In particular, if the support is exactly `C_5` with equal weight `w` on each cycle edge, then the disjoint-pair constraint gives `2w < 1/4`, hence `B = 5w < 5/8`.

So the thin-spread obstruction naturally lives near weighted `C_5`, not near a star.

---

**OPEN**

The key missing statement is a Hall-type transfer inside each slice.

> **Slice Hall Transfer**
> For a slice `A_v`, let `m_v` be the maximum weight of an opposite edge-pair in `A_v`.
> Is it true that the forced `A_v`-supported good mass satisfies `g_v ≥ m_v`?

If yes, then `g_v ≥ W_v/3` for every `v`, hence for any vertex `x`:

`h_x = Σ_{v≠x} g_v ≥ (1/3) Σ_{v≠x} W_v = (2B + d_x)/3`.

Substituting into the frequency bound gives

`p_x ≥ 1/17 + (308/765)B + (142/255)d_x`.

Taking `x` of maximum bad degree, `d_x ≥ 2B/5`, so

`max_x p_x ≥ 1/17 + (2392/3825)B`.

This already closes all `B ≳ 0.5296`.

So a pure matching statement on each `K_4` slice would eliminate most of the residual range without any LP.

---

**OPEN**

The real obstruction now looks cyclic.

For weighted `C_5` support:

- every bad edge lies in exactly two 4-slices,
- every slice contains a heavy opposite pair,
- collisions of those pairings are not obviously waste, because overlap only improves density by `[P7]`.

So the workshop bottleneck is not “is there enough forced support?”, but:

- can the five slice matchings be packed into too few good fibers, or
- does Hall force a distributed family of slice-supports?

That is a matching/transport question on a cyclic incidence pattern, and it looks like the right foreign-domain target.

---

**DEAD**

A support-only argument that counts merely the number of active slices is too weak. The residual regime needs a mass-transport statement (`g_v` proportional to edge weight in the slice), not just existence of one disjoint pair per slice.

---

**Next structural target**

Prove the slice Hall transfer for one `K_4` slice, or disprove it by an explicit collision model. If it fails, the failure mode should be describable as a very rigid cyclic transport pattern, which would likely reduce the global problem to the weighted `C_5` case.

### BUILD — Pass 1

#### The Proof Writer (codex/gpt-5.4)

`TRANSLATED` I need two distinct slice functionals on a `K_4` chamber `A_v`.

Write the three opposite-pairings in `A_v` as
\[
P_1=\{e_1,f_1\},\qquad P_2=\{e_2,f_2\},\qquad P_3=\{e_3,f_3\}.
\]

Define
\[
\mu_v:=\max_{j=1,2,3}\min(b_{e_j},b_{f_j}),
\qquad
\sigma_v:=\max_{j=1,2,3}(b_{e_j}+b_{f_j}),
\qquad
W_v:=\sum_{e\subset A_v} b_e.
\]

Here `\mu_v` is the actual one-pair forcing mass available from the verified mechanism, while `\sigma_v` is the matching-sum surrogate.

**Lemma 1. `PROVEN`**
For every slice `A_v`,
\[
\sigma_v\ge \frac{W_v}{3}.
\]

**Proof.**
The six edges of `K_4` are partitioned by the three opposite-pairings `P_1,P_2,P_3`. Hence
\[
(b_{e_1}+b_{f_1})+(b_{e_2}+b_{f_2})+(b_{e_3}+b_{f_3})=W_v.
\]
Therefore the maximum of the three pair-sums is at least their average:
\[
\sigma_v\ge \frac{W_v}{3}.
\]
QED.

**Lemma 2. `PROVEN`**
No positive constant `c` can satisfy
\[
\mu_v\ge c\,W_v
\]
for all slice edge profiles.

**Proof.**
Take a slice `A_v=\{2,3,4,5\}` and assign weights
\[
b_{23}=b_{24}=b_{34}=t>0,\qquad b_{25}=b_{35}=b_{45}=0.
\]
Then
\[
W_v=3t.
\]
The three opposite-pairings are
\[
\{23,45\},\qquad \{24,35\},\qquad \{25,34\},
\]
and each pairing contains a zero edge, so
\[
\min(b_{23},b_{45})=\min(b_{24},b_{35})=\min(b_{25},b_{34})=0.
\]
Hence
\[
\mu_v=0.
\]
Thus `\mu_v/W_v=0`, so no inequality `\mu_v\ge cW_v` with `c>0` is universally valid. QED.

**Lemma 3. `DEAD`**
The slice-total transfer proposed in the explore pass is not a valid consequence of the currently verified forcing mechanism.

**Proof.**
The verified forcing from one disjoint bad pair yields chamber mass equal to the smaller edge mass of that pair, so the chamber quantity controlled by present technology is `\mu_v`, not `\sigma_v`.

By Lemma 1, `\sigma_v\ge W_v/3`. By Lemma 2, there is no positive lower bound of the form `\mu_v\ge cW_v`. Therefore one cannot pass from slice total `W_v` to actual forced chamber mass using only the current one-pair mechanism. The obstruction is structural: `\sigma_v` and `\mu_v` are genuinely different LP observables. QED.

So the correct open bridge is not “prove `W_v/3` transfer”, but rather:

`OPEN`
Find a new chamber-capacity inequality
\[
g_v\ge \Gamma(b|_{A_v})
\]
with `\Gamma` strictly stronger than `\mu_v`, or prove the much stronger statement `g_v\ge \sigma_v`.

**Lemma 4. `PROVEN` (conditional correction)**
If a future result proves the strong transfer
\[
g_v\ge \sigma_v\qquad\text{for every slice }A_v,
\]
then the residual regime would already be closed for
\[
B\ge \frac{563}{1152}\approx 0.4887.
\]

**Proof.**
Fix a vertex `x`. Since
\[
\sum_{v\ne x}W_v=2B+d_x
\]
(from the verified slice identity), Lemma 1 gives
\[
\sum_{v\ne x}\sigma_v\ge \frac{2B+d_x}{3}.
\]
Under the hypothetical transfer `g_v\ge \sigma_v`,
\[
h_x:=\sum_{v\ne x} g_v \ge \frac{2B+d_x}{3}.
\]
Insert this into the verified frequency template:
\[
p_x\ge \frac{1-B}{17}+\frac B5+\frac25 d_x+\frac{8}{17}h_x
\]
to obtain
\[
p_x\ge \frac{1-B}{17}+\frac B5+\frac25 d_x+\frac{8}{51}(2B+d_x).
\]
Choose `x` of maximum bad degree. Then
\[
d_x\ge \frac{2B}{5}.
\]
Hence
\[
p_x\ge \frac{1-B}{17}+\frac15B+\frac{4}{25}B+\frac{16}{51}B+\frac{16}{255}B
= \frac{1-B}{17}+\frac{313}{425}B.
\]
Now
\[
\frac{1-B}{17}+\frac{313}{425}B\ge \frac{39}{100}
\]
is equivalent to
\[
-\frac{563}{1700}+\frac{288}{425}B\ge 0,
\]
i.e.
\[
B\ge \frac{563}{1152}\approx 0.4887.
\]
So that stronger chamber transfer would close far more than the `B\ge 0.5296` range stated in the explore pass. QED.

Summary of what survives this pass:

- `PROVEN`: the chamber LP must distinguish `\mu_v` from `\sigma_v`.
- `DEAD`: any argument using only slice totals `W_v` to control present one-pair forcing mass.
- `OPEN`: prove a genuine chamber-capacity law `g_v\ge \Gamma(b|_{A_v})`, ideally `g_v\ge \sigma_v`, or else identify the exact collision polytope preventing it.

#### The Calculator (codex/gpt-5.4)

**TRANSLATED**

Fix the boundary value `B = 75/119`. Normalize edge masses by `w_e = B_e / B`, so

- `w_e >= 0`
- `sum_e w_e = 1`
- for every disjoint edge pair `e,f` in `K_5`, `w_e + w_f < 1/(4B) = 119/300`.

For each missing vertex `v`, let `A_v = X \ {v}`. Define the verified slice bonus observable
\[
\mu_v(w):=\max_{\text{opposite pairs } \{e,f\}\subset E(A_v)} \min(w_e,w_f).
\]
This is the LP/optimization version of the currently verified forcing mechanism.

For each vertex `x`, write
\[
D_x:=\sum_{e\ni x} w_e,\qquad H_x:=\sum_{v\ne x}\mu_v.
\]
Then the verified lower bound becomes
\[
p_x \;\ge\; \frac1{17}+B\Big(\frac{12}{85}+\Phi_x(w)\Big),
\qquad
\Phi_x(w):=\frac25 D_x+\frac{8}{17}H_x.
\]
At `B=75/119`, reaching `p_x >= 0.39` would require
\[
\Phi_x(w)\ge \frac{48997}{127500}\approx 0.384290196.
\]

**PROVEN**

The current `μ_v`-LP does not close the boundary case. Two exact feasible certificates show this.

1. Without any extra slice constraint, the LP stalls far below `0.39`.

Take the normalized triangle:
\[
w_{12}=w_{13}=w_{23}=\frac13,\qquad w_e=0\text{ otherwise.}
\]
Then every slice has `\mu_v=0`, because each opposite pair contains a zero edge. The degrees are
\[
D_1=D_2=D_3=\frac23,\qquad D_4=D_5=0.
\]
Hence
\[
\max_x \Phi_x = \frac25\cdot\frac23=\frac4{15}.
\]
Therefore
\[
\max_x p_x \ge \frac1{17}+\frac{75}{119}\Big(\frac{12}{85}+\frac4{15}\Big)
= \frac{639}{2023}\approx 0.3158675.
\]
So the bare pair-forcing LP is decisively too weak.

2. Even after imposing the verified `K_4` slice cutoff `[P12]`, the LP still does not reach `0.39`.

At `B=75/119`, `[P12]` excludes any slice mass `>= 969/1972`, i.e. in normalized form
\[
U_v:=\sum_{e\subset A_v} w_e = 1-D_v < \frac{2261}{2900},
\]
equivalently
\[
D_v > \frac{639}{2900}.
\]

Now take the symmetric `3+2` profile:
\[
w_{12}=w_{13}=w_{23}=a,\qquad
w_{14}=w_{15}=w_{24}=w_{25}=w_{34}=w_{35}=b,\qquad
w_{45}=0,
\]
with
\[
3a+6b=1.
\]

For this family:

- the high-part degrees are
\[
D_1=D_2=D_3=2a+2b=\frac23-2b,
\]
- the low-part degrees are
\[
D_4=D_5=3b,
\]
- every slice has the same bonus
\[
\mu_v=b \quad \text{for all }v,
\]
because in each `K_4` chamber there is an opposite pair consisting of two `b`-edges, and no opposite pair can do better.

Hence
\[
H_x=4b \quad \text{for all }x.
\]
So
\[
\Phi_{\text{high}}(b)=\frac25\Big(\frac23-2b\Big)+\frac{8}{17}(4b)
=\frac4{15}+\frac{92}{85}b,
\]
while
\[
\Phi_{\text{low}}(b)=\frac25(3b)+\frac{32}{17}b
=\frac{262}{85}b.
\]
In the relevant range, `\Phi_high` is the maximum.

The slice cutoff forces
\[
3b > \frac{639}{2900}\qquad\Longrightarrow\qquad b>\frac{213}{2900}.
\]
Thus the minimum of this symmetric sub-LP occurs at the boundary `b=213/2900` (or arbitrarily close above it, if strictness is enforced). Then
\[
a=\frac{811}{4350},
\qquad
\max_x \Phi_x
=
\frac4{15}+\frac{92}{85}\cdot\frac{213}{2900}
=
\frac{63997}{184875}
\approx 0.346163624.
\]
Therefore
\[
\max_x p_x \ge
\frac1{17}+\frac{75}{119}\Big(\frac{12}{85}+\frac{63997}{184875}\Big)
=
\frac{15336}{41905}
\approx 0.365970648.
\]

This is still well below `0.39`.

The disjoint-pair cap is safely satisfied here, since the largest disjoint-pair sum is `a+b = 2261/8700 < 119/300`.

**DEAD**

Any LP that uses only the currently verified chamber bonus
\[
\mu_v=\max \min(\text{opposite pair weights})
\]
cannot prove the boundary case `B=75/119`, even after incorporating the verified `K_4` slice exclusion `[P12]`.

The exact obstruction is the symmetric `3+2` profile above, which is fully feasible and yields only
\[
\max_x p_x \ge 0.365970648.
\]

So the “pairwise chamber forcing + universal `1/17` floor” LP is dead as a closing argument.

**OPEN**

To push the LP past `0.39`, one needs a stronger chamber functional than `\mu_v`. In optimization language:

- either replace `\mu_v` by a larger validated slice-capacity functional `\Gamma_v(w)`,
- or add new linear/convex constraints coupling different slices,
- or use overlap-density bonuses in a way that survives the `3+2` obstruction.

The residual gap at the boundary is quantitative:
\[
0.384290196 - 0.346163624 \approx 0.038126572
\]
at the level of the normalized objective `\Phi_x`. That is the exact amount of extra chamber capacity the current LP lacks on this explicit feasible profile.

#### The Explorer (codex/gpt-5.4)

`TRANSLATED`

Let `X=[5]`. Encode the bad-fiber masses by a weighted graph `w` on `K_5`:
\[
w_e\ge 0,\qquad \sum_{e\in E(K_5)} w_e=1.
\]
For a chamber `A_v:=X\setminus\{v\}` and each opposite-pair perfect matching `p=\{e,f\}` in `K_4[A_v]`, define its certified weight
\[
m_p:=\min(w_e,w_f).
\]
This is exactly the verified mass that the current forcing mechanism can send into chamber `A_v`.

Now build the compressed certificate graph `\mathcal C(w)`:

- left vertices: the 15 opposite-pair certificates `p`,
- right vertices: the 5 chambers `A_v`,
- each `p` is adjacent to the unique chamber `A_v` with `e\cup f=A_v`,
- left weight of `p` is `m_p`.

Write
\[
\mu_v:=\max_{p\to A_v} m_p.
\]

So `\mathcal C(w)` is just a disjoint union of 5 three-edge stars.

---

`PROVEN`

**Compressed Hall/flow theorem.**
In the chamber-compressed model `\mathcal C(w)`, the strongest universal lower bound on chamber mass is exactly
\[
g_v\ge \mu_v.
\]
No larger lower bound can be proved from this quotient alone.

**Proof.**
Each left certificate `p` only says that chamber `A_v` must absorb at least `m_p`, where `p\to A_v`. Since all three certificates of a chamber land on the same right vertex, the feasible assignment
\[
g_v:=\max_{p\to A_v} m_p=\mu_v
\]
satisfies every certificate constraint simultaneously:
\[
g_v\ge m_p \qquad\text{for all }p\to A_v.
\]
Therefore any universal chamber lower bound derivable inside this quotient must hold for the feasible point `g_v=\mu_v`; hence it cannot exceed `\mu_v`. QED.

---

`PROVEN`

**Consequence for matching-style arguments.**
Any Hall/matching/flow argument run only on the compressed chamber graph `\mathcal C(w)` collapses to the `\mu_v`-LP already analyzed by The Calculator.

**Proof.**
The right side of `\mathcal C(w)` has one node per chamber, and every connected component is a star. Hall on a star has no additive content: three left demands hitting the same right node can always be satisfied by that single node at capacity equal to the largest left demand. By the previous theorem, the quotient value is exactly `\mu_v`, so every such argument is equivalent to the existing `\mu_v` chamber functional. QED.

---

`DEAD`

The entire class of arguments that:

1. keep only the weighted bad-edge graph on `K_5`,
2. compress every 4-support good fiber to a single chamber node `A_v`,
3. try to use Hall/matching/flow/cover on those 5 chamber nodes,

is dead for closing `B=75/119`.

Reason: by the theorem above, that class cannot produce more than `\mu_v`, and The Calculator already gave a feasible `3+2` profile where the resulting bound is only
\[
\max_x p_x \approx 0.365970648<0.39.
\]

So the obstruction is not lack of optimization effort. It is structural: the quotient has collapsed all distinct certificates inside a chamber onto one sink.

---

`OPEN`

A graph-theoretic bridge is still possible, but it must refine the right side.

The chamber node `A_v` has to be split into finer states `A_{v,\alpha}` so that different opposite-pair certificates inside the same chamber are no longer forced to collide automatically. In matching language:

- the current quotient has right side size `5`, which is too coarse;
- to get additive transfer, one needs a refined bipartite incidence graph where Hall sees multiple right neighbors inside a chamber.

Concretely, the needed result is an anti-collision theorem of the form:

\[
\text{certificates in one chamber }A_v \Longrightarrow \text{ at least } \Gamma(m_{p_1},m_{p_2},m_{p_3}) \text{ distinct/refined right mass},
\]
with `\Gamma>\max(m_{p_i})` on profiles like the symmetric `3+2` obstruction.

Until such a refinement exists, chamber-compressed matching theory cannot beat the current `\mu_v` barrier.

### BUILD — Pass 2

#### The Proof Writer (codex/gpt-5.4)

**TRANSLATED**

Fix a chamber `A = X \setminus {v}` of size `4`. Its three verified opposite-pair certificates are

\[
p_1=(12|34),\qquad p_2=(13|24),\qquad p_3=(14|23),
\]

with certified demands

\[
m_j \ge 0 \qquad (j=1,2,3).
\]

A chamber-local label-graph LP has the form:

- variables `y_\sigma \ge 0` for refined chamber states `\sigma`,
- each state has incidence vector `c(\sigma)\in\{0,1\}^3`, where `c_j(\sigma)=1` means that state certifies `p_j`,
- the LP minimizes total chamber mass
\[
g_A:=\sum_\sigma y_\sigma
\]
subject to
\[
\sum_{\sigma:\, c_j(\sigma)=1} y_\sigma \ge m_j \qquad (j=1,2,3).
\]

This is the most general “refine the chamber and run an LP on refined states” formulation that still uses only the verified same-chamber pair certificates.

The crucial translated fact is that the full-cube chamber state `q_A` is admissible and satisfies all three certificates simultaneously, so

\[
c(q_A)=(1,1,1).
\]

That single incidence vector is the obstruction.

---

**PROVEN**

**Lemma 1.** For every chamber-local LP of the above form, the minimum possible chamber mass is exactly

\[
g_A^{\min}=\max(m_1,m_2,m_3).
\]

**Proof.**

Primal LP:
\[
\min \sum_\sigma y_\sigma
\]
subject to
\[
\sum_{\sigma:\, c_j(\sigma)=1} y_\sigma \ge m_j \quad (j=1,2,3),\qquad y_\sigma\ge 0.
\]

Because the all-certificates state `q_A` exists with `c(q_A)=(1,1,1)`, the feasible point
\[
y_{q_A}=\max(m_1,m_2,m_3),\qquad y_\sigma=0\ \text{for }\sigma\neq q_A
\]
satisfies every constraint. Hence
\[
g_A^{\min}\le \max(m_1,m_2,m_3).
\]

Now take the dual:
\[
\max \sum_{j=1}^3 \lambda_j m_j
\]
subject to
\[
\sum_{j=1}^3 c_j(\sigma)\lambda_j \le 1 \quad\text{for every state }\sigma,\qquad \lambda_j\ge 0.
\]

Applying the dual constraint to `q_A` gives
\[
\lambda_1+\lambda_2+\lambda_3\le 1.
\]
Therefore
\[
\sum_{j=1}^3 \lambda_j m_j \le \max(m_1,m_2,m_3)\sum_{j=1}^3\lambda_j \le \max(m_1,m_2,m_3).
\]
So the dual optimum is at most `\max(m_1,m_2,m_3)`. By strong duality, the primal optimum is also at least that value. Hence
\[
g_A^{\min}=\max(m_1,m_2,m_3).
\]
QED.

---

**Lemma 2.** No chamber-local LP built only from the three opposite-pair certificate demands can force any chamber lower bound strictly larger than

\[
\mu_A:=\max(m_1,m_2,m_3).
\]

**Proof.**

By Lemma 1, the exact optimum of the most general such LP is already `\mu_A`. Any purported universal lower bound `g_A \ge \Gamma(m_1,m_2,m_3)` with `\Gamma>\mu_A` somewhere would fail on the feasible primal solution from Lemma 1, namely the single-state assignment concentrated on `q_A`. QED.

---

**Lemma 3.** Refining the right side inside a chamber does not help unless one adds constraints not encoded by the three same-chamber certificate demands.

**Proof.**

Any refinement only enlarges the state set `\{\sigma\}`. But the admissible state `q_A` with incidence `(1,1,1)` remains present, so the primal feasible point from Lemma 1 remains available. Thus every refined-state LP still has optimum at most `\mu_A`, and by duality exactly `\mu_A`. Therefore refinement alone gives no gain. QED.

---

**DEAD**

The entire class of arguments

1. encoded by edge masses `B_e`,
2. generating only the verified chamber demands from opposite bad pairs,
3. then attempting to improve the lower bound by refining each chamber into more LP states,

is dead.

By Lemmas 1–3, every such argument collapses exactly to the chamber functional
\[
\mu_A=\max(m_1,m_2,m_3),
\]
so it cannot beat the already-analyzed `\mu`-LP.

Therefore the symmetric `3+2` obstruction from the previous pass remains a valid exact counterexample for this whole LP class. In particular, at `B=75/119` there is a feasible normalized profile with
\[
\max_x p_x \le 0.365970648\ldots < 0.39
\]
for every bound derived solely from chamber-local opposite-pair certificate LPs.

This is stronger than “compressed Hall is dead”: even arbitrary chamber-state refinement is dead unless new cross-state constraints are added.

---

**OPEN**

The LP bridge that is still missing must forbid the one-state collapse onto `q_A`. In label-graph terms, one needs a new validated inequality not implied by the three demands `m_1,m_2,m_3`, for example one of the following types:

1. A cross-chamber coupling constraint linking states in different chambers.
2. A vertex-sensitive incompatibility inequality forcing some certificate mass to live in distinct refined states.
3. A multiplicity constraint using `r`-fold overlap densities `[P7]` in a way that cannot be realized by a single all-certificates chamber state.

Formally, to escape the dead zone one needs a new constraint family that cuts off the primal witness
\[
y_{q_A}=\mu_A,\qquad y_\sigma=0\ (\sigma\neq q_A)
\]
in at least one chamber of the `3+2` profile. Without such a cut, every chamber-local LP remains exactly at the `\mu` barrier.

So the next proof step is now sharply identified: construct a valid inequality that separates the all-certificates state `q_A` from the true feasible region once neighboring chambers or vertex-load interactions are taken into account.

#### The Calculator (codex/gpt-5.4)

`TRANSLATED`

Normalize bad-label masses by `w_e := B_e/B` on `E(K_5)`, so

\[
w_e\ge 0,\qquad \sum_e w_e=1.
\]

For vertex `x`, let

\[
d_x:=\sum_{e\ni x} w_e.
\]

For chamber `A_v = [5]\setminus\{v\}`, define the chamber certificate

\[
\mu_v:=\max_{\{e,f\}\text{ opposite pair in }K_4[A_v]} \min(w_e,w_f).
\]

Using only the verified forcing mechanism from the current LP attack, every good fiber contributes a universal floor `1/17`, and certified chamber mass `\mu_v` contributes an extra `8/17` to each vertex in `A_v`. So the translated pointwise lower bound is

\[
p_x \ge \frac{B}{5}+\frac{2B}{5}d_x+\frac{1-B}{17}+\frac{8B}{17}\sum_{v\ne x}\mu_v.
\]

Equivalently,

\[
p_x \ge \frac1{17}+B\Big(\frac{12}{85}+\Phi_x(w)\Big),
\qquad
\Phi_x(w):=\frac25 d_x+\frac{8}{17}\sum_{v\ne x}\mu_v.
\]

The boundary disjoint-pair constraint at `B=75/119` is

\[
w_e+w_f<\frac{1}{4B}=\frac{119}{300}
\]

for every disjoint pair `e,f`.

`PROVEN`

A concrete admissible non-star family defeats this LP class.

Take the weighted graph

\[
w_{12}=w_{13}=w_{23}=\frac{1-c}{3},\qquad w_{45}=c,
\qquad \text{all other }w_e=0,
\]

with `0<c<19/200`. Then:

\[
w_{ij}+w_{45}=\frac{1+2c}{3}<\frac{119}{300},
\]

so all disjoint-pair constraints are satisfied at `B=75/119`.

The normalized vertex degrees are

\[
d_1=d_2=d_3=\frac{2(1-c)}{3},\qquad d_4=d_5=c.
\]

The chamber certificates are

\[
\mu_1=\mu_2=\mu_3=c,\qquad \mu_4=\mu_5=0.
\]

Hence

\[
\Phi_1=\Phi_2=\Phi_3
=\frac25\cdot \frac{2(1-c)}{3}+\frac{8}{17}(c+c)
=\frac{4}{15}+\frac{172}{255}c,
\]

and

\[
\Phi_4=\Phi_5
=\frac25 c+\frac{8}{17}(c+c+c)
=\frac{154}{85}c.
\]

For small `c`, the maximum is attained at vertices `1,2,3`, so

\[
\max_x \Phi_x(w)=\frac{4}{15}+\frac{172}{255}c.
\]

Therefore the LP-based pointwise bound gives

\[
\max_x p_x
\ge
\frac1{17}+B\Big(\frac{12}{85}+\frac{4}{15}+\frac{172}{255}c\Big).
\]

Letting `c\to 0^+`,

\[
\inf \max_x p_x
\le
\frac1{17}+B\Big(\frac{12}{85}+\frac{4}{15}\Big).
\]

At the boundary `B=75/119` this is exactly

\[
\frac1{17}+\frac{75}{119}\cdot\frac{56}{255}
=
\frac{639}{2023}
\approx 0.3158675235.
\]

`DEAD`

The proposed `K_5` edge-mass LP, using only:

- bad-degree contribution `B/5 + (2/5)d_x`,
- universal good floor `1/17`,
- certified disjoint-pair forcing through chamber masses `\mu_v`,

cannot prove `\max_x p_x\ge 0.39`.

Reason: the explicit admissible family above already drives the resulting lower bound down to

\[
0.3158675235<0.39
\]

at `B=75/119`, and in fact along a non-star degenerating ray.

So the issue is structural, not optimization slack: any argument whose certified good transfer is proportional only to `\min(B_e,B_f)` for disjoint bad pairs is vulnerable to a “heavy triangle + tiny opposite edge” profile.

`OPEN`

To escape this obstruction, the next valid inequality must assign non-vanishing good mass to a disjoint-pair configuration even when one edge in the pair is tiny. In optimization language, the current certificate functional is too close to a min-cut:

\[
(B_e,B_f)\mapsto \min(B_e,B_f),
\]

and that vanishes on the degenerating witness. A successful replacement has to use some different convex constraint, for example:

- a cross-chamber coupling,
- a vertex-load/overlap inequality using `[P7]`,
- or a certificate depending on existence/placement of disjoint bad pairs, not just their smaller mass.

So the current LP attack is refuted. The remaining task is to find a stronger translated constraint family.

#### The Explorer (codex/gpt-5.4)

**TRANSLATED**

Let `P` be the Petersen graph on the ten bad labels `e ∈ C([5],2)`, with adjacency
\[
e \sim f \iff e\cap f=\varnothing .
\]
So disjoint bad pairs are exactly edges of `P`.

For fixed bad mass `B`, normalize
\[
w_e:=\frac{B_e}{B},\qquad \sum_e w_e=1,
\]
and set
\[
\beta_B:=\frac1{4B},\qquad x_e:=\frac{w_e}{\beta_B}=4B\,w_e.
\]
Then the residual non-star regime constraints become
\[
x_e\ge 0,\qquad x_e+x_f\le 1\quad\text{for every }ef\in E(P),\qquad \sum_e x_e=4B<\frac{300}{119}.
\]

So the `10`-variable bad-edge problem is exactly a slice of the fractional stable-set polytope of the Petersen graph:
\[
\mathrm{FRAC}(P)=\{x\ge 0: x_u+x_v\le 1\ \forall uv\in E(P)\}.
\]

The five chambers are the five distinguished triangles of `P`: for each deleted vertex `v`, the three opposite pairs inside `[5]\setminus\{v\}` form a triangle in `P`.

---

**PROVEN**

**1. Half-integral reduction.**  
Every vertex of `FRAC(P)` is half-integral:
\[
x_e\in\{0,\tfrac12,1\}.
\]
Moreover, the subgraph induced by the `1/2`-coordinates is a disjoint union of odd cycles; in the Petersen graph that means disjoint `5`-cycles.

This is the Nemhauser-Trotter half-integrality theorem, specialized to `P`.

So the ambient disjoint-pair polytope has only two atomic obstruction types:

- `1`-blocks: stable sets of `P`,
- `1/2`-blocks: `5`-cycles of `P`.

**2. Stable sets = intersecting edge-families on `K_5`.**  
A stable set in `P` is a family of `2`-subsets of `[5]` with no disjoint pair, i.e. an intersecting family of edges in `K_5`.

For `2`-subsets of `[5]`, the maximal intersecting families are exactly:

- a `4`-edge star through one vertex,
- a `3`-edge triangle on three vertices.

So the only integral bad-label blocks are:

- star blocks,
- triangle blocks.

This pins down the current “heavy triangle + tiny opposite edge” witness as the unique non-star integral block.

**3. The only genuinely fractional obstruction is a Petersen `5`-cycle.**  
A concrete `5`-cycle in `P` is
\[
12 - 34 - 15 - 23 - 45 - 12,
\]
since each consecutive pair is disjoint.

Thus any LP over bad-edge masses using only the disjoint-pair inequalities is not a generic `10`-variable problem. Its geometry is forced into mixtures of three graph-theoretic atoms:

- stars,
- triangles,
- Petersen `5`-cycles.

That is the right structural reduction.

---

**DEAD**

Treating the residual regime as an unrestricted continuous weight optimization on `E(K_5)` is the wrong geometry.

Once translated to `P`, the obstruction space is already rigid: chamber-local pair constraints live on the five chamber-triangles of the Petersen graph, while the only global half-integral atoms are stars, triangles, and `5`-cycles. So any method that does not distinguish these three atom types is too coarse.

In particular, the current chamber-local machinery cannot see the difference between:

- a triangle block,
- a nearby slice direction toward a `5`-cycle block.

That explains why the LP keeps leaking along the “triangle + tiny opposite edge” ray.

---

**OPEN**

The next valid inequality should be stated directly on the Petersen side. Two plausible targets are:

1. A **triangle-vs-cycle separator**: a valid inequality on `FRAC(P)` that gives extra cost to triangle blocks or to motions from a triangle block toward a `5`-cycle block.

2. A **cross-chamber blossom constraint**: chamber triangles in `P` are not independent; one needs a coupling that cuts off the ability to realize several chamber certificates by a single local “all-certificates” state.

So the search space is now sharply reduced:

- star blocks are already handled by `[P17]` and `[P18]`,
- chamber-local LPs are dead,
- the only remaining graph-theoretic atoms to separate are triangle blocks and Petersen `5`-cycle blocks.

That is the structural bridge I would attack next.

### BUILD — Pass 3

#### The Proof Writer (codex/gpt-5.4)

`TRANSLATED`

Work on the Petersen graph
\[
P:=\Big(\binom{[5]}2,\ \sim\Big),\qquad e\sim f \iff e\cap f=\varnothing.
\]
Write normalized bad-label masses as
\[
w_e:=\frac{B_e}{B},\qquad w_e\ge 0,\qquad \sum_e w_e=1.
\]
Then the residual disjoint-pair constraint is
\[
w_e+w_f<\frac1{4B}\qquad (e\sim f).
\]
A “disjoint-pair-only” good-mass certificate is any bonus term \(G_x(w)\) that vanishes whenever \(\operatorname{supp}(w)\) is a stable set of \(P\). This is exactly the LP-language version of a certificate that only fires when some disjoint bad pair is present.

---

`PROVEN`

**Lemma 1.** If \(I\subseteq \binom{[5]}2\) is a stable set in \(P\), then either:

1. all edges in \(I\) share a common vertex (star type), or  
2. \(I\subseteq \{ab,ac,bc\}\) for some \(a,b,c\in[5]\) (triangle type).

**Proof.**  
“Stable in \(P\)” means “pairwise intersecting as edges of \(K_5\).”

If \(|I|\le 2\), the claim is immediate. Assume \(|I|\ge 3\). Pick two distinct edges in \(I\), say \(ab,ac\). They intersect at \(a\).

If every edge of \(I\) contains \(a\), we are in case 1.

Otherwise choose \(e\in I\) not containing \(a\). Since \(e\) must intersect both \(ab\) and \(ac\), it must contain both \(b\) and \(c\). Hence \(e=bc\).

Now let \(f\in I\). Since \(f\) intersects \(ab,ac,bc\), it must meet all three of these edges. Any edge of \(K_5\) meeting all three of \(ab,ac,bc\) is one of \(ab,ac,bc\). Therefore \(I\subseteq \{ab,ac,bc\}\), giving case 2. ∎

---

**Lemma 2.** The normalized triangle atom
\[
\tau_{12}=\tau_{13}=\tau_{23}=\frac13,\qquad \tau_e=0\ \text{otherwise},
\]
is feasible for every \(B<\tfrac34\). It is non-star.

**Proof.**  
Its support \(\{12,13,23\}\) is triangle type, so by Lemma 1 it is stable in \(P\). Hence for every disjoint pair \(e\sim f\), at most one of \(\tau_e,\tau_f\) is nonzero. Therefore
\[
\tau_e+\tau_f\le \frac13.
\]
If \(B<\tfrac34\), then \(\frac13<\frac1{4B}\), so all residual disjoint-pair constraints hold.

It is non-star because
\[
12\cap 13\cap 23=\varnothing,
\]
so no single vertex lies in all three labels. ∎

---

**Lemma 3.** Let
\[
p_x \ge \frac1{17}+B\Big(\frac{12}{85}+\frac25 d_x(w)\Big)+G_x(w),
\qquad
d_x(w):=\sum_{e\ni x} w_e,
\]
where each \(G_x\) is a disjoint-pair-only certificate, i.e.
\[
G_x(w)=0\quad\text{whenever }\operatorname{supp}(w)\text{ is stable in }P.
\]
Then at the triangle atom \(\tau\),
\[
G_x(\tau)=0\quad\text{for all }x,
\]
and
\[
\max_x p_x \ge \frac1{17}+B\cdot \frac{104}{255}.
\]

**Proof.**  
By Lemma 2, \(\operatorname{supp}(\tau)=\{12,13,23\}\) is stable in \(P\), so \(G_x(\tau)=0\).

The degrees are
\[
d_1(\tau)=d_2(\tau)=d_3(\tau)=\frac23,\qquad d_4(\tau)=d_5(\tau)=0.
\]
Hence
\[
\max_x\Big(\frac{12}{85}+\frac25 d_x(\tau)\Big)
=
\frac{12}{85}+\frac25\cdot \frac23
=
\frac{12}{85}+\frac{4}{15}
=
\frac{104}{255}.
\]
Substitute into the bound. ∎

---

**Corollary 4.** At the boundary \(B=\frac{75}{119}\), every disjoint-pair-only certificate scheme of the form in Lemma 3 yields at best
\[
\max_x p_x \ge \frac1{17}+\frac{75}{119}\cdot \frac{104}{255}
= \frac{639}{2023}
\approx 0.3158675,
\]
which is strictly below \(0.39\).

**Proof.**  
Immediate from Lemma 3. ∎

---

`DEAD`

Any argument whose extra good-mass transfer is triggered only by the presence of disjoint bad pairs is dead.

The obstruction is not merely the “heavy triangle + tiny opposite edge” ray. The exact triangle atom \(\tau\) already kills the whole certificate class, because:

1. \(\tau\) is feasible throughout the residual regime \(B<\tfrac34\), hence in particular for all \(B<\tfrac{75}{119}\);
2. \(\tau\) is non-star;
3. every disjoint-pair-only bonus vanishes identically on \(\tau\);
4. the remaining certified lower bound is only \(639/2023\approx 0.3158675\) at \(B=75/119\).

So the next inequality cannot depend only on disjoint-pair activation. It must already be nonzero on the triangle stable-set vertex of the Petersen LP.

---

`OPEN`

The residual LP problem has a sharper target now:

Construct a new vertex-sensitive certificate \(H_x(w)\) such that:

1. \(H_x\) is valid on the Petersen-side LP;
2. \(H_x\) is positive on every non-star extreme obstruction, in particular on the triangle atom \(\tau\);
3. for some \(x\in\{1,2,3\}\),
\[
H_x(\tau)\ge \frac{39}{100}-\frac{639}{2023}
= \frac{14997}{202300}
\approx 0.0741374
\]
at \(B=\frac{75}{119}\).

Equivalently, in the normalized \(B\)-coefficient, the triangle atom needs an additional contribution of at least
\[
\frac{14997}{127500}\approx 0.1176235.
\]

That is the precise missing constant. Any successful Petersen-side proof must separate triangle stable-set mass from star mass, not just detect disjoint pairs.

#### The Calculator (codex/gpt-5.4)

**TRANSLATED**

Let `P` be the Petersen graph on the `10` labels `e ∈ C([5],2)`, with adjacency
\[
e\sim f \iff e\cap f=\varnothing.
\]
Write
\[
w_e:=\frac{B_e}{B},\qquad w_e\ge 0,\qquad \sum_e w_e=1.
\]
Then the residual bad-label constraints are exactly
\[
w_e+w_f<\frac1{4B}\qquad (e\sim f).
\]

For vertex `x`, the certified lower bound coming from

- bad-fiber frequency `[P6]`,
- universal good-fiber floor `[P14]`,
- and any bonus triggered only by disjoint bad pairs,

has the LP form
\[
p_x \;\ge\; \frac{1-B}{17}+\frac{B}{5}+\frac{2B}{5}\,d_x(w)+G_x(w),
\]
where
\[
d_x(w):=\sum_{e\ni x} w_e,
\]
and `G_x(w) ≥ 0` is any certificate that vanishes when there are no disjoint bad pairs in the support.

Equivalently,
\[
p_x \ge \frac1{17}+B\Big(\frac{12}{85}+\frac25 d_x(w)\Big)+G_x(w).
\]

For a forced good fiber of `4`-support containing `x`, the certified frequency is

- `9/17` on the `4` support vertices by `[P15]`,
- `1/17` on the remaining vertex by `[P14]`.

So each such forced fiber contributes an `8/17` upgrade on its support.

---

**PROVEN**

The proposed `10`-variable LP attack, with bonuses coming only from nonintersecting bad pairs plus the universal `1/17` floor, cannot reach `0.39`. A single feasible non-star point blocks it.

Take the triangle atom
\[
w_{12}=w_{13}=w_{23}=\frac13,\qquad w_e=0\ \text{otherwise}.
\]

This is feasible for every
\[
B<\frac34,
\]
because its support is a stable set of `P`, so for every disjoint pair `e∼f`,
\[
w_e+w_f\le \frac13<\frac1{4B}.
\]

At this point there are no disjoint bad pairs in the support, hence every pair-triggered bonus vanishes:
\[
G_x(w)=0\qquad\forall x.
\]

The vertex loads are
\[
d_1=d_2=d_3=\frac23,\qquad d_4=d_5=0.
\]

Therefore
\[
p_1=p_2=p_3 \ge \frac1{17}+B\Big(\frac{12}{85}+\frac25\cdot\frac23\Big)
= \frac1{17}+\frac{104}{255}B,
\]
and
\[
p_4=p_5 \ge \frac1{17}+\frac{12}{85}B.
\]

So the LP value at this feasible point is
\[
\max_x p_x \ge \frac1{17}+\frac{104}{255}B.
\]

At the boundary
\[
B=\frac{75}{119},
\]
this becomes
\[
\max_x p_x \ge \frac1{17}+\frac{104}{255}\cdot \frac{75}{119}
= \frac{639}{2023}
\approx 0.3158675.
\]

That is strictly below `0.39`.

So the `10`-variable LP, in the form proposed, cannot prove the target. Its optimum is at most `639/2023` at `B=75/119`.

---

**PROVEN**

Boundary checks at `B=75/119`:

1. Global-sum threshold `[P8]`:
\[
\sum_x p_x \ge \frac95+\frac{119}{500}\cdot\frac{75}{119}
= \frac95+\frac{3}{20}
= \frac{39}{20}=1.95.
\]
Average vertex frequency is `1.95/5 = 0.39`, matching `[P9]`.

2. Star estimate `[P11]`:
\[
\frac35 B+\frac1{32}(1-B)
= \frac35\cdot\frac{75}{119}+\frac1{32}\cdot\frac{44}{119}
\approx 0.3897059.
\]

3. Pair-only LP obstruction:
\[
\frac{639}{2023}\approx 0.3158675.
\]

So the numerics are internally consistent: the general threshold closes at `75/119`, the old star floor misses slightly, and the proposed pair-only LP misses by a large margin.

---

**DEAD**

The concrete LP attack stated in the prompt is dead in its current form.

Reason in optimization language: its bonus variables are activated only by Petersen edges, i.e. by disjoint bad-label pairs. But the feasible stable-set vertex
\[
(12,13,23)
\]
is non-star and has zero Petersen-edge activation. Hence all such bonus coordinates are forced to `0`, leaving only the affine baseline
\[
\frac1{17}+B\Big(\frac{12}{85}+\frac25 d_x\Big),
\]
which is too small.

So any certificate depending only on nonintersecting-pair activation cannot close the residual regime.

---

**OPEN**

The next valid inequality must already be positive on the triangle stable-set vertex
\[
w_{12}=w_{13}=w_{23}=\frac13.
\]

Quantitatively, at `B=75/119`, the missing amount is
\[
0.39-\frac{639}{2023}
= \frac{14997}{202300}
\approx 0.0741374.
\]

So a successful new LP-side certificate must contribute at least that much for one of vertices `1,2,3` on the triangle atom itself. In particular, it must distinguish

- star stable sets,
- triangle stable sets,

not just detect disjoint pairs.

#### The Explorer (codex/gpt-5.4)

`TRANSLATED`

Work in the edge-weight space of `K_5`:
\[
E:=\binom{[5]}2,\qquad w\in \mathbb R^{E},\qquad w_e\ge 0,\qquad \sum_{e\in E} w_e=1.
\]
Here `w_e=B_e/B` is the normalized bad-label mass. The vertex-loads are
\[
d_x(w):=\sum_{e\ni x} w_e,\qquad \sum_{x=1}^5 d_x=2.
\]

The right graph-theoretic state space is not just the degree vector `d(w)`. It is the direct sum
\[
\mathbb R^{E}=\mathcal D\oplus \mathcal H,
\]
where
\[
\mathcal D:=\{u_{ij}=a_i+a_j:\ a\in\mathbb R^5\},
\qquad
\mathcal H:=\Bigl\{h\in\mathbb R^{E}:\sum_{j\ne i} h_{ij}=0\ \forall i\Bigr\}.
\]
So `\mathcal H=ker(d)` is the `5`-dimensional “harmonic” subspace invisible to vertex loads.

Equivalently, every normalized weight has the unique decomposition
\[
w_{ij}=\frac{d_i+d_j-\frac12}{3}+h_{ij},
\qquad h\in \mathcal H.
\]
This is the exact translation of “degree data + residual triangle mode.”

---

`PROVEN`

**1. Degree-based information sees only `\mathcal D`.**

If a certificate is built only from `B`, the vertex-loads `d_x(w)`, and any activation term that vanishes on stable supports of the Petersen graph, then it is blind to the harmonic part `h`.

Reason: `d_x` depends only on the `\mathcal D`-projection, while the stable-support activation vanishes on both star supports and triangle supports.

So the unresolved obstruction is not in the degree module; it lives in `\mathcal H`.

---

**2. Star atoms are exactly harmonic-free.**

For the normalized star at vertex `x`,
\[
\sigma^{(x)}_{ij}=\frac14\,1_{\{x\in\{i,j\}\}},
\]
the harmonic part is zero:
\[
\sigma^{(x)}\in \mathcal D,\qquad h(\sigma^{(x)})=0.
\]

Proof: its degree vector is `d_x=1`, `d_y=1/4` for `y\ne x`, and substituting into
\[
\frac{d_i+d_j-\frac12}{3}
\]
recovers exactly the star weights.

So “star-type” mass is the pure degree module.

---

**3. The triangle obstruction is purely a harmonic perturbation of its degree shadow.**

For the triangle atom
\[
\tau_{12}=\tau_{13}=\tau_{23}=\frac13,\qquad \tau_e=0\ \text{otherwise},
\]
the degrees are
\[
d_1=d_2=d_3=\frac23,\qquad d_4=d_5=0.
\]
Its harmonic part is
\[
h^\tau_{12}=h^\tau_{13}=h^\tau_{23}=\frac1{18},
\]
\[
h^\tau_{14}=h^\tau_{15}=h^\tau_{24}=h^\tau_{25}=h^\tau_{34}=h^\tau_{35}=-\frac1{18},
\]
\[
h^\tau_{45}=\frac16.
\]
In particular,
\[
h^\tau\neq 0.
\]

So the exact non-star obstruction already identified in the workshop is not a degree phenomenon. It is a nonzero harmonic mode.

---

**4. Structural consequence for the residual proof.**

The current lower bound
\[
p_x \ge \frac1{17}+B\Bigl(\frac{12}{85}+\frac25 d_x(w)\Bigr)+G_x(w)
\]
uses only:

- the degree module via `d_x(w)`,
- and a Petersen-edge activation term `G_x` that vanishes on stable supports.

On the triangle atom `\tau`, `G_x(\tau)=0`, so the whole bound ignores `h^\tau`. That is why the LP stalls there.

This is a stronger obstruction statement than “disjoint-pair-only bonuses are dead.” The dead part is exactly:
\[
\text{anything that factors through }(\,d(w),\ \text{Petersen-edge activation}\,)
\]
is dead.

---

`DEAD`

A certificate that only uses

- vertex loads `d_x`,
- plus any bonus triggered by Petersen edges (disjoint bad-label pairs),

cannot close the residual regime.

Reason in graph language: star atoms and the triangle atom are both Petersen-stable, so the activation part vanishes on both; the only remaining data is the degree projection. But the actual non-star obstruction is the harmonic component `h^\tau`, which such a certificate never sees.

So the next inequality must be a genuine `\mathcal H`-detector.

---

`OPEN`

The graph-theoretic bridge that now looks necessary is:

Find a valid lower bound for some vertex `x` of the form
\[
p_x \ge \frac1{17}+B\Bigl(\frac{12}{85}+\frac25 d_x(w)\Bigr)+\Phi_x(h),
\]
where `\Phi_x` is nonnegative on all feasible normalized bad-mass states and strictly positive on the triangle harmonic mode `h^\tau`.

In concrete structural terms, `\Phi_x` should measure a **triangle-mode** rather than a Petersen-edge mode. A natural target is a Hall-type transfer from triangle label mass
\[
(12,13,23)
\]
into `3`-support good fibers, since [P5] already says each intersecting pair opens a `3`-support fiber.

So the next foreign-domain problem is:

- build a matching/packing certificate from triangle mass to `3`-support good mass,
- or prove a spectral inequality converting `\mathcal H`-mass into vertex frequency bonus.

That is the exact structural gap: not “more pair forcing,” but a valid inequality that detects the harmonic triangle mode.

### VERIFY

#### The Proof Writer (codex/gpt-5.4)

**Ledger Update**

**CORRECT**

PROVEN: Chamber-local opposite-pair LP refinement is exhausted.

For one chamber `A`, let the three verified opposite-pair demands be `m_1,m_2,m_3`, and let `q_A` denote the admissible full-cube chamber state certifying all three at once. Then the primal feasible point
\[
y_{q_A}=\max(m_1,m_2,m_3),\qquad y_\sigma=0\ (\sigma\neq q_A)
\]
shows
\[
g_A^{\min}\le \max(m_1,m_2,m_3).
\]
In the dual, the constraint from `q_A` is
\[
\lambda_1+\lambda_2+\lambda_3\le 1,
\]
so
\[
\sum_{j=1}^3 \lambda_j m_j\le \max(m_1,m_2,m_3).
\]
Hence
\[
g_A^{\min}=\max(m_1,m_2,m_3).
\]
This correctly kills every chamber-local LP whose only constraints are those three same-chamber demands.

PROVEN: Stable sets of the Petersen label graph are exactly star-type or triangle-type intersecting edge families of `K_5`.

A stable set in the Petersen graph is a pairwise intersecting family of `2`-subsets of `[5]`. For `|I|\ge 3`, pick `ab,ac\in I`. If every edge contains `a`, it is star-type. Otherwise some `e\in I` omits `a`; then to meet both `ab` and `ac`, necessarily `e=bc`. Any further edge meeting all of `ab,ac,bc` must be one of those three. So the only maximal types are a `4`-edge star or a `3`-edge triangle. This part is correct.

PROVEN: The triangle atom is a valid obstruction to every disjoint-pair-only bonus scheme.

Take
\[
\tau_{12}=\tau_{13}=\tau_{23}=\frac13,\qquad \tau_e=0\ \text{otherwise}.
\]
Its support is a stable set, so for every disjoint pair `e\sim f`,
\[
\tau_e+\tau_f\le \frac13.
\]
Thus it is feasible whenever
\[
\frac13<\frac1{4B},
\]
in particular for all `B<3/4`, hence throughout the residual regime `B<75/119`.

If a proposed bonus `G_x(w)` vanishes on Petersen-stable supports, then `G_x(\tau)=0`. Also
\[
d_1=d_2=d_3=\frac23,\qquad d_4=d_5=0.
\]
So any bound of the form
\[
p_x \ge \frac1{17}+B\Big(\frac{12}{85}+\frac25 d_x(w)\Big)+G_x(w)
\]
gives at best
\[
\max_x p_x \ge \frac1{17}+B\Big(\frac{12}{85}+\frac{4}{15}\Big)
=\frac1{17}+\frac{104}{255}B.
\]
At
\[
B=\frac{75}{119},
\]
this is
\[
\frac1{17}+\frac{75}{119}\cdot\frac{104}{255}
=\frac{639}{2023}\approx 0.3158675<0.39.
\]
So that whole certificate class is genuinely dead.

PROVEN: The harmonic decomposition itself is correct.

For normalized edge weights,
\[
d_i=\sum_{j\ne i} w_{ij},\qquad \sum_i d_i=2,
\]
and
\[
w_{ij}=\frac{d_i+d_j-\frac12}{3}+h_{ij},
\]
with
\[
\sum_{j\ne i} h_{ij}=0\quad\forall i.
\]
The formula checks directly. The star atom has `h=0`, and the triangle atom has the stated nonzero `h`. So the linear decomposition is valid.

**WRONG / OVERSTATED**

DEAD: “The next inequality must be a genuine `\mathcal H`-detector.”

Reason: the decomposition `w=d+h` is correct, but the necessity claim does not follow. A valid future certificate could be nonlinear, cross-chamber, or transport-based, and still be positive on the triangle atom without factoring through `h` alone. So the structural conclusion is too strong. What is proved is only this: degree data plus bonuses vanishing on stable supports cannot work.

**UNJUSTIFIED**

OPEN: “The only genuinely fractional obstruction is a Petersen `5`-cycle.”

What is justified is the Nemhauser-Trotter half-integrality description: every vertex of `FRAC(P)` is `0,1/2,1`, and the `1/2` support is a disjoint union of odd cycles. In the Petersen graph those odd cycles are `5`-cycles. But one still needs the slice condition
\[
\sum_e x_e=4B<\frac{300}{119}
\]
to conclude that at most one such `5`-cycle can appear on the residual slice. Without that slice qualification, the statement is too compressed.

OPEN: The precise new obstruction target is now clear.

Any successful new certificate must already be positive on the triangle atom. At `B=75/119`, the missing amount is
\[
0.39-\frac{639}{2023}
=\frac{14997}{202300}\approx 0.0741374.
\]
So the next mechanism must deliver at least that much extra frequency to one of vertices `1,2,3` on the triangle profile itself.

**Overall Assessment**

FATAL FLAW: yes.

The fatal flaw is specific: every recent LP-style strengthening still vanishes on Petersen-stable supports, and the triangle stable-set atom
\[
\{12,13,23\}
\]
is already a feasible non-star residual configuration. That makes the whole disjoint-pair / chamber-local activation paradigm dead.

The next iteration should not try to repair that LP. The new direction should be a global transport argument from triangle bad-label mass to `3`-support good fibers, not mediated by disjoint pairs. In label-graph language: build a bipartite incidence/flow certificate whose left side is triangle-type bad mass and whose right side is forced `3`-slice good mass, with a Hall-type lower bound that is nonzero on the triangle atom itself.

PROGRESS RATING: 8/10

The workshop has cleanly identified a real obstruction and sharply narrowed the search space. The dead zone is now well certified; what remains is a genuinely different global certificate.

#### The Calculator (codex/gpt-5.4)

**CORRECT**

`PROVEN: Chamber-local opposite-pair LP refinement is exhausted.`

In one chamber \(A\), let the three verified opposite-pair demands be \(m_1,m_2,m_3\). If there is a single admissible chamber state \(q_A\) that satisfies all three demands simultaneously, then the chamber LP has primal feasible solution
\[
y_{q_A}=\max(m_1,m_2,m_3),\qquad y_\sigma=0\ \ (\sigma\neq q_A),
\]
so
\[
g_A^{\min}\le \max(m_1,m_2,m_3).
\]
In the dual, the constraint from \(q_A\) is
\[
\lambda_1+\lambda_2+\lambda_3\le 1,
\]
hence
\[
\sum_{j=1}^3 \lambda_j m_j\le \max(m_1,m_2,m_3).
\]
Therefore
\[
g_A^{\min}=\max(m_1,m_2,m_3).
\]
So any chamber-local LP using only those three same-chamber constraints is fully exhausted.

`PROVEN: Stable sets of the Petersen label graph are exactly star-type or triangle-type intersecting edge families of K_5.`

A stable set in the Petersen graph is a pairwise-intersecting family of \(2\)-subsets of \([5]\). If \(|I|\ge 3\), pick \(ab,ac\in I\). If every edge in \(I\) contains \(a\), it is star-type. Otherwise some edge omits \(a\), and to intersect both \(ab\) and \(ac\) it must be \(bc\). Any further edge intersecting all of \(ab,ac,bc\) is one of those three. So the only maximal types are a \(4\)-edge star or a \(3\)-edge triangle.

`PROVEN: The triangle atom kills every disjoint-pair-only bonus scheme.`

Take
\[
\tau_{12}=\tau_{13}=\tau_{23}=\frac13,\qquad \tau_e=0\ \text{otherwise}.
\]
Its support is Petersen-stable, so for every disjoint pair \(e\sim f\),
\[
\tau_e+\tau_f\le \frac13.
\]
Thus it is feasible whenever \(\frac13<\frac1{4B}\), in particular throughout the residual regime \(B<75/119\).

For any bonus \(G_x(w)\) that vanishes on Petersen-stable supports,
\[
G_x(\tau)=0.
\]
Also
\[
d_1=d_2=d_3=\frac23,\qquad d_4=d_5=0.
\]
Hence any bound of the form
\[
p_x\ge \frac1{17}+B\Big(\frac{12}{85}+\frac25 d_x(w)\Big)+G_x(w)
\]
gives at best
\[
\max_x p_x\ge \frac1{17}+B\Big(\frac{12}{85}+\frac{4}{15}\Big)
=\frac1{17}+\frac{104}{255}B.
\]
At
\[
B=\frac{75}{119},
\]
this is
\[
\frac1{17}+\frac{75}{119}\cdot\frac{104}{255}
=\frac{639}{2023}\approx 0.3158675<0.39.
\]
So that certificate class is dead.

`PROVEN: The harmonic decomposition is correct.`

For normalized weights \(w_{ij}\),
\[
d_i=\sum_{j\ne i} w_{ij},\qquad \sum_i d_i=2,
\]
and
\[
w_{ij}=\frac{d_i+d_j-\frac12}{3}+h_{ij},
\qquad
\sum_{j\ne i}h_{ij}=0.
\]
This is a valid linear decomposition \( \mathbb R^{E(K_5)}=\mathcal D\oplus\mathcal H\). The star atom has \(h=0\); the triangle atom has \(h\neq 0\).

**WRONG**

`DEAD: “The next inequality must be a genuine harmonic-subspace detector.”`

This is overstated. What is proved is narrower: any certificate depending only on vertex loads plus bonuses that vanish on Petersen-stable supports cannot work. It does **not** follow that every successful future certificate must factor through the harmonic projection \(h\) or be linear in \(h\). A nonlinear transport, packing, or overlap certificate could be positive on the triangle atom without being an \(h\)-detector.

**UNJUSTIFIED**

`OPEN: “The only genuinely fractional obstruction is a Petersen 5-cycle.”`

Half-integrality of \(\mathrm{FRAC}(P)\) is correct: vertices are \(0,\tfrac12,1\), and the \(\tfrac12\)-support is a disjoint union of odd cycles. In the Petersen graph, those odd cycles are \(5\)-cycles. But to conclude that the residual slice only sees one such block, one must use the slice constraint
\[
\sum_e x_e=4B<\frac{300}{119}.
\]
That extra slice argument was not fully supplied. So the compressed classification claim is not yet fully justified in the residual LP.

`OPEN: Precise target for any new certificate.`

At the triangle atom, the missing amount at the boundary is exactly
\[
0.39-\frac{639}{2023}
=\frac{14997}{202300}\approx 0.0741374.
\]
So any genuinely new certificate must already contribute at least that much to one of vertices \(1,2,3\) on the triangle profile itself.

**Overall Assessment**

FATAL FLAW: yes.

What is specifically wrong: every recent LP-style strengthening still activates only through disjoint bad pairs or chamber-local opposite-pair certificates. In optimization language, those bonus coordinates are zero on Petersen-stable supports. But the triangle stable-set atom
\[
\{12,13,23\}
\]
is a feasible non-star residual point, so the entire activation paradigm collapses there.

NEW direction: abandon pair-activation LPs and look for a global transport certificate from triangle-type bad mass to forced \(3\)-support or \(4\)-support good mass, using overlap-density constraints like \([P7]\) as capacity bounds. The left side should be triangle bad-label mass, the right side should be good-fiber mass, and the objective should certify vertex frequency directly on the triangle atom. That is a different optimization model, not a repair of the old one.

PROGRESS RATING: 8/10.

#### The Explorer (codex/gpt-5.4)

**CORRECT**

PROVEN: Stable sets of the Petersen label graph are exactly star-type or triangle-type intersecting edge families of `K_5`.

A stable set in the Petersen graph means a pairwise-intersecting family of `2`-subsets of `[5]`. If `|I| >= 3`, choose `ab, ac ∈ I`. If every edge contains `a`, it is star-type. Otherwise some edge omits `a`; to intersect both `ab` and `ac` it must be `bc`. Any further edge intersecting all of `ab, ac, bc` is one of those three. So the only maximal stable supports are stars and triangles.

PROVEN: The triangle atom is a feasible residual obstruction for every disjoint-pair-only bonus scheme.

Take
\[
\tau_{12}=\tau_{13}=\tau_{23}=\frac13,\qquad \tau_e=0\ \text{otherwise}.
\]
Its support is Petersen-stable, so every disjoint-pair constraint satisfies
\[
\tau_e+\tau_f\le \frac13<\frac1{4B}
\]
whenever `B<3/4`, hence throughout the residual regime `B<75/119`. Therefore any bonus term that vanishes on stable supports also vanishes on `\tau`.

PROVEN: Any certificate of the form
\[
p_x\ge \frac1{17}+B\Big(\frac{12}{85}+\frac25 d_x(w)\Big)+G_x(w),
\]
with `G_x` vanishing on Petersen-stable supports, is dead.

At `\tau`,
\[
d_1=d_2=d_3=\frac23,\qquad d_4=d_5=0,
\]
so
\[
\max_x p_x\ge \frac1{17}+B\Big(\frac{12}{85}+\frac{4}{15}\Big)
=\frac1{17}+\frac{104}{255}B.
\]
At
\[
B=\frac{75}{119},
\]
this equals
\[
\frac{639}{2023}\approx 0.3158675<0.39.
\]
So every disjoint-pair/chamber-activation LP of that type is genuinely blocked by the triangle atom.

PROVEN: The harmonic decomposition
\[
w_{ij}=\frac{d_i+d_j-\frac12}{3}+h_{ij},\qquad \sum_{j\ne i}h_{ij}=0,
\]
is correct.

Direct substitution verifies it, and the decomposition
\[
\mathbb R^{E(K_5)}=\mathcal D\oplus\mathcal H
\]
is valid. The star atom has `h=0`; the triangle atom has `h≠0`. This is a correct structural reformulation.

**WRONG**

DEAD: “The next inequality must be a genuine `\mathcal H`-detector.”

Reason: that conclusion is too strong. What is proved is only that degree data plus bonuses vanishing on stable supports cannot succeed. A future successful argument could be nonlinear, transport-based, or matching-based and still be positive on the triangle atom without factoring through the harmonic projection alone.

**UNJUSTIFIED**

OPEN: “The only genuinely fractional obstruction is a Petersen `5`-cycle.”

Half-integrality of `FRAC(P)` is correct, and the `1/2`-support is a disjoint union of odd cycles, hence `5`-cycles in Petersen. But to turn that into a complete classification on the residual slice one still has to use the slice constraint
\[
\sum_e x_e=4B<\frac{300}{119},
\]
and explicitly rule out multiple odd-cycle components or mixed half-integral patterns on that slice. That argument was not supplied.

OPEN: Can one build a certificate that is already positive on the triangle atom itself?

This is now the central unresolved question. At the boundary `B=75/119`, the triangle profile needs an extra contribution of
\[
0.39-\frac{639}{2023}
=\frac{14997}{202300}\approx 0.0741374
\]
to some vertex among `1,2,3`.

**Overall Assessment**

FATAL FLAW: yes.

What is specifically wrong: the current paradigm still activates only through disjoint bad pairs or chamber-local opposite-pair forcing. Those mechanisms vanish on Petersen-stable supports, and the triangle atom is a feasible non-star stable support in the whole residual regime. So the approach is not merely weak; it is structurally incapable of closing the gap.

NEW direction: abandon pair-activation LPs entirely and move to a global matching/transport argument. In graph language, treat triangle-type bad mass as the left side of a bipartite incidence system and forced `3`-support / `4`-support good fibers as the right side, then seek a Hall-style lower bound that certifies positive transfer on the pure triangle atom itself.

PROGRESS RATING: 8/10.

## Iteration 2

### BUILD — Pass 1

#### The Proof Writer (codex/gpt-5.4)

**TRANSLATED**

Let `P` be the Petersen label graph: its `10` vertices are the `2`-subsets of `[5]`, and two label-vertices are adjacent iff the corresponding `K_5` edges are disjoint.

For a bad-mass profile `(B_e)_{e∈E(K_5)}`, set
\[
x_e:=4B_e,\qquad \sigma:=\sum_e x_e=4B.
\]
Then the residual regime
\[
B<\frac{75}{119},\qquad B_e+B_f<\frac14\ \text{for every disjoint pair }e,f
\]
is exactly the LP slice
\[
Q_\sigma:=\Bigl\{x\in\mathbb R_{\ge 0}^{V(P)}:\ x_u+x_v<1\ \forall\, uv\in E(P),\ \sum_{v\in V(P)}x_v=\sigma<\frac{300}{119}\Bigr\}.
\]
Also, by `[P6]`,
\[
p_i^{\mathrm{bad}}=\frac{B}{5}+\frac25 d_i,
\qquad
d_i=\sum_{e\ni i} B_e=\frac14\sum_{e\ni i}x_e.
\]

So the residual problem is an LP on a sliced fractional stable-set polytope of `P`, with vertex objective
\[
p_i\ge \frac{B}{5}+\frac25 d_i+\frac1{17}(1-B)
\]
from `[P14]`.

---

**PROVEN**

**Lemma 1.** Let `x` be an extreme point of the closure `\overline{Q_\sigma}`. Form the tight graph `H_x` on the fractional coordinates
\[
F:=\{v:0<x_v<1\}
\]
by joining `u,v∈F` when `uv∈E(P)` and `x_u+x_v=1`. If `C` is a connected component of `H_x` containing an odd cycle, then
\[
x_v=\frac12\qquad \forall v\in C.
\]

**Proof.**
Pick `r∈C`. Along any path in `C`, the tight equations `x_u+x_v=1` propagate values by alternation:
\[
x_v=
\begin{cases}
x_r,&\text{if dist}_C(r,v)\text{ is even},\\[2mm]
1-x_r,&\text{if dist}_C(r,v)\text{ is odd}.
\end{cases}
\]
If `C` contains an odd cycle through `r`, returning to `r` along that odd cycle gives
\[
x_r=1-x_r,
\]
hence `x_r=1/2`. The same propagation then gives `x_v=1/2` for every `v∈C`. ∎

**Lemma 2.** In the residual slice `\sigma<300/119<3`, every odd-cycle component of `H_x` is a `5`-cycle.

**Proof.**
By Lemma 1, every vertex of such a component contributes exactly `1/2` to `\sum_v x_v`. Hence an odd-cycle component on `m` vertices contributes `m/2` mass. Since total mass is `<3`, we have `m/2<3`, so `m\le 5`. The Petersen graph has girth `5`, so the only odd cycle available has length `5`. Therefore the component is a `5`-cycle. ∎

**Lemma 3.** The residual LP has a canonical boundary obstruction: the `5`-cycle ray
\[
x=t\,\mathbf 1_C,\qquad 0<t<\frac12,
\]
where `C` is any `5`-cycle of `P`.

**Proof.**
For adjacent vertices of `C`, the pair sum is `2t<1`; all other coordinates are `0`. Hence `x∈Q_\sigma` with
\[
\sigma=5t,\qquad B=\frac{\sigma}{4}=\frac{5t}{4}<\frac58<\frac{75}{119}.
\]
So this family lies entirely inside the residual regime. ∎

**Lemma 4.** Along the `5`-cycle ray, every ground vertex has the same bad load
\[
d_i=\frac{2B}{5},
\]
hence
\[
p_i^{\mathrm{bad}}=\frac{9B}{25}\qquad \forall i.
\]

**Proof.**
Take the concrete Petersen `5`-cycle
\[
C=\{12,34,15,23,45\}.
\]
Each vertex of `[5]` belongs to exactly two labels in `C`. Since each active label has bad mass `B_e=t/4`,
\[
d_i=2\cdot \frac{t}{4}=\frac{t}{2}=\frac{2B}{5}.
\]
Then `[P6]` gives
\[
p_i^{\mathrm{bad}}=\frac{B}{5}+\frac25\cdot\frac{2B}{5}=\frac{9B}{25}.
\]
∎

**Lemma 5.** Along the `5`-cycle ray, the universal vertex floor `[P14]` yields only
\[
p_i\ge \frac1{17}(1-B)+\frac{9B}{25}
= \frac1{17}+\frac{128}{425}B
\qquad \forall i.
\]
At the endpoint `B\uparrow 5/8`, this tends to
\[
\frac{21}{85}\approx 0.2470588.
\]

**Proof.**
Add the good-fiber floor `(1-B)/17` from `[P14]` to Lemma 4:
\[
p_i\ge \frac{9B}{25}+\frac{1-B}{17}
= \frac1{17}+\Bigl(\frac{9}{25}-\frac1{17}\Bigr)B
= \frac1{17}+\frac{128}{425}B.
\]
At `B=5/8`,
\[
\frac1{17}+\frac{128}{425}\cdot\frac58
= \frac1{17}+\frac{16}{85}
= \frac{21}{85}.
\]
∎

**Corollary 6.** Any successful label-graph LP certificate must supply an additional bonus of at least
\[
0.39-\frac{21}{85}=\frac{243}{1700}\approx 0.142941
\]
on the closure point `B=5/8` of the `5`-cycle ray.

**Proof.**
Immediate from Lemma 5. ∎

---

**DEAD**

A certificate that is designed only to defeat stable supports (stars/triangles) is not enough in LP language.

Reason: Lemmas 1-6 exhibit a second atomic obstruction in the residual slice, namely the half-integral Petersen `5`-cycle boundary ray. Its baseline floor is much worse than the triangle deficit:
\[
\frac{243}{1700}\approx 0.142941
\]
must be recovered there, whereas the triangle atom at `B=75/119` only needs
\[
\frac{14997}{202300}\approx 0.0741374.
\]

So the residual LP does not reduce to “kill the triangle support.” It must also control the odd-cycle face of the Petersen slice.

---

**OPEN**

The structural bridge now looks precise:

1. In label-graph LP language, the residual bad-mass polytope has two qualitatively different atoms:
   - stable-support atoms (star/triangle),
   - odd-cycle atoms (Petersen `5`-cycles at weight `1/2` in the closure).

2. Existing disjoint-pair forcing attacks the first class but does not obviously generate a bonus of size `243/1700` on the second.

3. The missing ingredient is a dual inequality that is positive on the Petersen odd-cycle face itself, not merely on escape from stable supports.

That is the LP reformulation I would record as the current target.

#### The Calculator (codex/gpt-5.4)

**TRANSLATED**

Work on the bad-label mass polytope
\[
Q(B)=\Bigl\{(B_e)_{e\in E(K_5)}\in \mathbb R_{\ge 0}^{10}:\ \sum_e B_e=B,\ \ B_e+B_f\le \tfrac14\ \text{for every disjoint pair }e\perp f\Bigr\}.
\]
For each ground vertex \(i\),
\[
d_i=\sum_{e\ni i} B_e,\qquad p_i^{\mathrm{bad}}=\frac B5+\frac25 d_i.
\]
For good fibers, the verified local floor data are:

- plain good mass: \(p_x\ge 1/17\),
- one forced \(A_z\)-slice: \(p_x\ge 9/17\) for \(x\neq z\), and \(p_z\ge 1/17\),
- two spanning slices through a center \(x\): \(p_x\ge 1/2\).

So the certified per-unit bonus over the universal floor is currently:

- \(8/17\) on a supported vertex from one \(A_z\)-slice,
- \(15/34\) on the spanned center from the two-slice statement.

**PROVEN**

1. The degree LP solves exactly:
\[
\min_{Q(B)}\max_i d_i=\frac{2B}{5}.
\]
Reason: \(\sum_i d_i=2B\), so \(\max_i d_i\ge 2B/5\). Equality is attained by the uniform profile
\[
B_e=\frac{B}{10}\quad(\forall e),
\]
since then \(d_i=4(B/10)=2B/5\), and every disjoint-pair constraint is
\[
B_e+B_f=\frac{B}{5}\le \frac14
\]
throughout the residual regime \(B<75/119\).

Hence no certificate using only bad degrees can do better than
\[
p_i^{\mathrm{bad}}\ge \frac B5+\frac25\cdot\frac{2B}{5}=\frac{9B}{25}.
\]

2. At the boundary \(B=75/119\),
\[
\frac{9B}{25}=\frac{27}{119}\approx 0.2268908.
\]
Adding only the universal good floor gives
\[
\frac{27}{119}+\frac{1-B}{17}
=\frac{503}{2023}\approx 0.2486406.
\]
So the symmetric profile still needs extra
\[
\frac{39}{100}-\frac{503}{2023}
=\frac{28597}{202300}\approx 0.1413594
\]
at some vertex.

3. The global-sum relaxation is boundary-tight, not slack. From \([P8]\),
\[
\sum_x p_x\ge \frac95+\frac{119}{500}B.
\]
Therefore
\[
\max_x p_x\ge \frac15\sum_x p_x
\ge \frac{9}{25}+\frac{119}{2500}B.
\]
At \(B=75/119\),
\[
\frac{9}{25}+\frac{119}{2500}\cdot \frac{75}{119}
=\frac{39}{100}=0.39.
\]
Below the boundary this average bound is strictly \(<0.39\). So any LP that only sees total slice-counted good mass is exact at the boundary and fails inside the residual slice.

4. The triangle stable-support atom remains a feasible obstruction for every bonus that vanishes on Petersen-stable supports. Take
\[
B_{12}=B_{13}=B_{23}=\frac B3,\qquad B_e=0\ \text{otherwise}.
\]
Then
\[
d_1=d_2=d_3=\frac{2B}{3},\qquad d_4=d_5=0,
\]
so the heavy vertices satisfy
\[
p_i\ge \frac B5+\frac25\cdot\frac{2B}{3}+\frac{1-B}{17}
=\frac1{17}+\frac{104}{255}B.
\]
At \(B=75/119\),
\[
p_i\ge \frac{639}{2023}\approx 0.3158675.
\]
The missing amount is
\[
\frac{39}{100}-\frac{639}{2023}
=\frac{14997}{202300}\approx 0.0741325.
\]

5. Converting that deficit into certified forced-good mass:
- using only one-slice support bonus \(8/17\), the triangle atom needs at least
\[
\frac{14997/202300}{8/17}
=\frac{14997}{95200}\approx 0.15753
\]
of support mass on one heavy vertex;
- the uniform profile needs
\[
\frac{28597/202300}{8/17}
=\frac{28597}{95200}\approx 0.30039.
\]

**DEAD**

- DEAD: pure degree-side optimization on the \(10\) edge masses. The exact optimum is the uniform profile, so there is no degree imbalance to exploit.
- DEAD: any relaxation that only uses \(\sum_z G_z\) or \(\sum_x p_x\). It is exactly boundary-tight at \(B=75/119\) and gives \(<0.39\) for every smaller \(B\).
- DEAD: any bonus functional that vanishes on Petersen-stable supports. The triangle atom stays feasible and still sits \(0.0741325\) below target at the boundary.

**OPEN**

The missing LP ingredient must be a bonus functional that is positive on the triangle stable support itself, not just on disjoint-pair activations, and it must also create real slack beyond the average bound on nearly uniform profiles. Without that, the residual slice remains open.

#### The Explorer (codex/gpt-5.4)

**TRANSLATED**

Let \(P=KG(5,2)\) be the Petersen label graph, with one vertex for each bad label \(e\in E(K_5)\), and adjacency meaning disjointness. Write the bad-mass vector as
\[
b\in \mathbb R^{E(K_5)}_{\ge 0},\qquad \sum_e b_e=B.
\]

Let \(N\) be the \(5\times 10\) edge-vertex incidence matrix of \(K_5\):
\[
(Nb)_i=\sum_{e\ni i} b_e=d_i.
\]
Then the bad contribution is exactly
\[
p_i^{\mathrm{bad}}=\frac B5+\frac25\, d_i,
\]
so the native objective only sees \(Nb\).

Now let \(A\) be the adjacency matrix of \(P\). A direct edge-intersection check gives
\[
A = J + I - N^\top N.
\]
Indeed, for two labels \(e,f\):
- diagonal: \(1+1-2=0\),
- intersecting distinct pair: \(1-1=0\),
- disjoint pair: \(1-0=1\).

So the residual problem lives in the spectral decomposition of edge-space under the pair \((N,A)\).

---

**PROVEN**

1. The \(10\)-dimensional label space splits orthogonally as
\[
\mathbb R^{10}=\langle \mathbf 1\rangle \oplus \operatorname{im}(N^\top|_{\mathbf 1^\perp}) \oplus \ker N,
\]
with \(A\)-eigenvalues
\[
3,\quad -2,\quad 1
\]
on these three summands.

Reason:
- \(N N^\top=3I+J\), so on \(\mathbf 1^\perp\), \(N N^\top=3I\).
- Hence on \(\operatorname{im}(N^\top|_{\mathbf 1^\perp})\),
\[
A=J+I-N^\top N = I-3I=-2I.
\]
- On \(\ker N\), since also \(\mathbf 1^\top z=0\),
\[
Az=Jz+z-N^\top Nz=z.
\]
- On \(\mathbf 1\), \(A\mathbf 1=3\mathbf 1\).

2. The bad-frequency map is blind to the \(+1\)-eigenspace:
\[
Nb = N(\text{uniform part}) + N(\text{degree mode}) + N(\ker N),
\]
and \(N(\ker N)=0\). Therefore every certificate that factors only through the bad degrees \(d_i\) cannot distinguish two profiles differing by a kernel vector.

3. The Petersen \(5\)-cycle obstruction is exactly such a kernel excursion.

Take
\[
C=\{12,34,15,23,45\}.
\]
Let \(c_C\in\{0,1\}^{10}\) be its indicator, and let \(\mathbf 1\) be the all-edge vector. Then
\[
z_C:=c_C-\frac12\mathbf 1 \in \ker N.
\]
Reason: every ground vertex of \([5]\) lies in exactly two edges of \(C\) and exactly two edges outside \(C\), so each coordinate of \(Nz_C\) is \(2-\frac12\cdot 4=0\).

Thus a \(5\)-cycle profile is “uniform profile + pure kernel mode”. This is why it preserves the balanced degree load
\[
d_i=\frac{2B}{5}\quad \forall i,
\]
and defeats any degree-only argument.

4. The triangle obstruction is the opposite: it is degree-visible, not kernel.

For the triangle support
\[
T=\{12,13,23\},
\]
its indicator \(t_T\) satisfies
\[
Nt_T=(2,2,2,0,0)^\top,
\]
so it has a genuine \(-2\)-sector component. This is why degree data sees triangle-type concentration immediately.

5. Structural dichotomy:
- star/triangle obstructions live in the degree-visible sector \(\langle \mathbf 1\rangle \oplus \operatorname{im}(N^\top|_{\mathbf 1^\perp})\);
- \(5\)-cycle obstructions live in the invisible kernel sector \(\ker N\) after removing the uniform part.

That is the clean spectral reason the residual LP has two different atoms.

---

**DEAD**

A one-sector certificate cannot close the gap.

- Any certificate depending only on \(d_i=Nb\) is blind to the \(5\)-cycle kernel mode, so it cannot kill the odd-cycle obstruction.
- Any certificate built only from disjointness-side observables that vanish on intersecting supports cannot kill the triangle obstruction.

So the residual problem cannot be solved by a purely degree-side argument or by a purely disjoint-pair-side argument. The obstruction is genuinely two-channel.

---

**OPEN**

The right target now looks spectral/matching-theoretic:

Find a bonus functional with two nontrivial projections:
\[
\Phi(b)=\Phi_{\deg}(b)+\Phi_{\ker}(b),
\]
where
- \(\Phi_{\deg}\) is positive on the triangle sector,
- \(\Phi_{\ker}\) is positive on the \(5\)-cycle kernel sector.

In graph language, the remaining bridge is not “more LP on \(B_e\)”. It is a dual witness that couples the \(-2\)-eigenspace and the \(+1\)-eigenspace of the Petersen label graph.

That is the structural translation I would record: the residual regime is blocked by two spectrally orthogonal atoms, and any successful proof must hit both at once.

### BUILD — Pass 2

#### The Proof Writer (codex/gpt-5.4)

**TRANSLATED**

Let \(P=KG(5,2)\) be the Petersen label graph, with one vertex for each bad label \(e\in E(K_5)\), and adjacency \(e\sim f\) meaning \(e\cap f=\varnothing\). Write
\[
y_e:=4B_e,\qquad \tau:=\sum_e y_e=4B.
\]
Then the residual bad-mass polytope is the slice
\[
\mathcal F_\tau:=\Bigl\{y\in \mathbb R_{\ge 0}^{V(P)}:\ y_u+y_v\le 1\ \forall uv\in E(P),\ \sum_{v}y_v=\tau\Bigr\},
\]
with
\[
\tau=4B<\frac{300}{119}<3.
\]
This is a fractional stable-set LP on \(P\). The question becomes: what are the slice-extreme obstructions of \(\mathcal F_\tau\) below \(\tau<3\)?

---

**PROVEN**

**Lemma 1.** Every extreme point \(y\) of \(\mathcal F_\tau\) is half-integral:
\[
y_v\in \Bigl\{0,\frac12,1\Bigr\}\qquad(\forall v\in V(P)).
\]

**Proof.**
Let
\[
F:=\{v:0<y_v<1\}.
\]
Form the tight graph \(T\) on \(F\) by joining \(u,v\in F\) when \(uv\in E(P)\) and
\[
y_u+y_v=1.
\]
First, every \(v\in F\) lies in \(T\): if \(v\) were incident to no tight edge, then for sufficiently small \(\varepsilon>0\), both \(y+\varepsilon e_v\) and \(y-\varepsilon e_v\) would remain feasible after compensating on the sum hyperplane by an opposite perturbation at another fractional vertex, contradicting extremality.

Next, no connected component of \(T\) is bipartite. Indeed, if a component had bipartition \(A\sqcup B\), then for small \(\varepsilon>0\),
\[
y' = y+\varepsilon(1_A-1_B),\qquad
y''= y-\varepsilon(1_A-1_B)
\]
would preserve all tight equations \(y_u+y_v=1\) on that component, preserve the sum, and keep all coordinates in \((0,1)\), again contradicting extremality.

So every component of \(T\) is nonbipartite, hence contains an odd cycle
\[
v_1v_2\cdots v_{2m+1}v_1.
\]
Along this cycle,
\[
y_{v_1}=1-y_{v_2}=y_{v_3}=\cdots =1-y_{v_{2m+1}},
\]
and the final edge \(v_{2m+1}v_1\) gives
\[
y_{v_1}=1-y_{v_{2m+1}}=1-(1-y_{v_1})=y_{v_1},
\]
forcing
\[
y_{v_1}=\frac12.
\]
Propagating along the component gives \(y_v=\frac12\) for every \(v\) in that component. Thus every fractional coordinate equals \(1/2\). The remaining coordinates are \(0\) or \(1\). ∎

---

**Lemma 2.** If \(y\in \mathcal F_\tau\) is extreme and has a fractional coordinate, then the set
\[
H:=\{v:y_v=\tfrac12\}
\]
induces a single \(5\)-cycle in \(P\). In particular,
\[
|H|=5,\qquad \sum_{v\in H} y_v=\frac52.
\]

**Proof.**
By Lemma 1, every fractional vertex has value \(1/2\). As above, each connected component of the induced subgraph \(P[H]\) is nonbipartite. Since \(P\) is triangle-free and has girth \(5\), every nonbipartite induced subgraph of \(P\) has at least \(5\) vertices.

But \(\tau<3\), so the total fractional mass is
\[
\frac{|H|}{2}<3,
\]
hence \(|H|\le 5\). Therefore each nonbipartite component of \(P[H]\) has exactly \(5\) vertices, so it is an induced \(5\)-cycle. Since two such components would contribute mass \(5> \tau\), there is at most one. ∎

---

**Lemma 3.** In the regime \(B<75/119\), the only nonintegral LP atom is the \(5\)-cycle atom
\[
c_C:=\frac12\,1_C
\]
for an induced \(5\)-cycle \(C\subseteq P\). In \(B\)-coordinates this is
\[
B_e=\frac18\quad (e\in C),\qquad B_e=0\quad (e\notin C),
\]
with total bad mass
\[
B=\frac58.
\]

**Proof.**
By Lemma 2, any nonintegral extreme point of the slice has exactly five \(1/2\)-entries and the rest integral. Let \(C\) be that induced \(5\)-cycle. Every vertex of \(P\setminus C\) is adjacent to some vertex of \(C\) (check directly in the Petersen graph, or in label language: every label outside a \(5\)-cycle is disjoint from some label on it). Hence an outside coordinate cannot equal \(1\), because \(1+\tfrac12>1\) would violate a stable-set inequality. So all outside coordinates are \(0\). Thus the nonintegral extreme point is exactly \(c_C\), whose sum is \(5/2\), ანუ \(B=5/8\). ∎

---

**Lemma 4.** Every integral extreme point of \(\mathcal F_\tau\) is a stable-set indicator of \(P\), hence by [P2] is either star-type or triangle-type.

**Proof.**
If all coordinates are integral, feasibility of \(y_u+y_v\le 1\) says precisely that the \(1\)-support is a stable set of \(P\). Conversely every stable-set indicator is feasible. Classification is [P2]. ∎

---

**Proposition 5.** For every \(B<75/119\), every extreme point of the slice \(\mathcal F_{4B}\) is a convex combination of at most two atoms from:
- \(0\),
- a star-type stable-set indicator,
- a triangle-type stable-set indicator,
- a \(5\)-cycle atom \(c_C\).

Equivalently, every slice-extreme bad-mass profile is a two-atom LP interpolation of those four types.

**Proof.**
\(\mathcal F_{4B}\) is the intersection of the polytope
\[
\mathcal F:=\{y\ge 0,\ y_u+y_v\le 1\}
\]
with the hyperplane \(\sum_v y_v=4B\). A vertex of a hyperplane slice of a polytope lies either at a vertex of the ambient polytope or on an edge of the ambient polytope. Hence every slice-extreme point is either an ambient extreme point or a convex combination of two ambient extreme points. By Lemmas 3 and 4, the only ambient extreme points relevant below \(\tau<3\) are exactly the four atom types listed. ∎

---

**Lemma 6.** The non-star residual obstruction is therefore one-dimensional: triangle-cycle interpolation.

Let \(t\) be a triangle atom and \(c\) a \(5\)-cycle atom. Any non-star two-atom slice extreme with \(B\in[5/8,3/4]\) has the form
\[
b=\lambda c+(1-\lambda)t,\qquad \lambda=6-8B.
\]
For this family:
\[
d_{\mathrm{heavy}}=2B-1,\qquad d_{\mathrm{light}}=\frac32-2B,
\]
and hence
\[
p_{\mathrm{heavy}}^{\mathrm{bad}}=\frac B5+\frac25(2B-1)=B-\frac25,
\]
\[
p_{\mathrm{light}}^{\mathrm{bad}}=\frac B5+\frac25\Bigl(\frac32-2B\Bigr)=\frac35-\frac{3B}{5}.
\]

**Proof.**
The total masses of the two atoms are
\[
B(c)=\frac58,\qquad B(t)=\frac34.
\]
So the interpolation parameter is determined by
\[
B=\lambda\cdot \frac58 +(1-\lambda)\cdot \frac34=\frac34-\frac{\lambda}{8},
\]
hence \(\lambda=6-8B\).

In the cycle atom each ground vertex has bad degree \(1/4\). In the triangle atom the three heavy vertices have bad degree \(1/2\) and the two light vertices have bad degree \(0\). Therefore
\[
d_{\mathrm{heavy}}=\lambda\cdot\frac14+(1-\lambda)\cdot\frac12=2B-1,
\]
\[
d_{\mathrm{light}}=\lambda\cdot\frac14=\frac32-2B.
\]
Now apply [P6]:
\[
p_x^{\mathrm{bad}}=\frac B5+\frac25 d_x.
\]
This gives the stated formulas. ∎

At the boundary \(B=75/119\), this yields
\[
p_{\mathrm{heavy}}^{\mathrm{bad}}=\frac{137}{595}\approx 0.230252,\qquad
p_{\mathrm{light}}^{\mathrm{bad}}=\frac{159}{1190}\approx 0.133613.
\]

---

**DEAD**

Any LP certificate that does not distinguish the triangle atom from the \(5\)-cycle atom cannot close the residual slice.

Reason: Proposition 5 reduces the non-star residual LP to the two-atom family in Lemma 6. A functional that is positive only on the cycle channel misses the triangle endpoint; a functional that is positive only on the degree-visible triangle channel misses the cycle endpoint. This is the LP form of the two-channel obstruction already visible in the ledger, now reduced to a single segment.

---

**OPEN**

The remaining proof task is now sharper.

It is enough to build a vertex-sensitive LP bonus functional \(\Phi_x(b)\) such that:

1. \(\Phi_x\) is certified from actual good-fiber overlap data, not just from bad degrees.
2. \(\Phi_x\) is strictly positive on the triangle endpoint.
3. \(\Phi_x\) is also strictly positive on the \(5\)-cycle endpoint.
4. On the interpolation segment of Lemma 6, one gets
\[
\max_x\Bigl(\frac B5+\frac25 d_x+\Phi_x(b)\Bigr)\ge 0.39
\]
for every
\[
\frac58\le B<\frac{75}{119}.
\]

So the LP search space is no longer the full \(10\)-variable polytope. After the reduction above, the genuinely new obstruction is the triangle-cycle segment. Any successful certificate must hit both endpoints of that segment simultaneously.

#### The Calculator (codex/gpt-5.4)

**TRANSLATED**

Use the optimization variables

- `b_e = B_e` for the 10 edges `e` of `K_5`,
- `B = Σ_e b_e`,
- `d_i = Σ_{e∋i} b_e`,
- `q_j` = total good mass on 4-support fibers with support `[5]\{j}`.

Then every vertex frequency satisfies the linear lower bound
\[
p_i \ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac{8}{17}\sum_{j\neq i}q_j
\]
because:

- bad fibers contribute `[P6]`,
- every good fiber gives baseline `1/17` to every vertex `[P14]`,
- a forced 4-support good fiber gives `9/17` on its support and `1/17` off-support, so the bonus above baseline is `8/17` on its 4 support vertices `[P15]`.

Writing `Q:=Σ_j q_j`, this is
\[
p_i \ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac{8}{17}(Q-q_i).
\]

So the LP-side problem is: minimize `max_i p_i` over feasible bad profiles `b`, with any coupling constraints one can prove between `q` and `b`.

---

**PROVEN**

1. For fixed `B` and fixed total 4-support good mass `Q`, the adversarial choice of the support-distribution `q` is uniform:
\[
q_1=\cdots=q_5=\frac Q5.
\]
Reason: `max_i (Q-q_i)=Q-\min_i q_i`, so minimizing the maximum means maximizing `\min_i q_i`, which is achieved by equalizing the `q_i`.

Hence the sharp LP lower bound with only total `Q` known is
\[
p_i \ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac{32}{85}Q.
\]

2. On the non-star slice-extreme segment from the ledger,
\[
b=\lambda c+(1-\lambda)t,\qquad \lambda=6-8B,\qquad \frac58\le B\le \frac34,
\]
the bad degrees are
\[
d_{\text{heavy}}=2B-1,\qquad d_{\text{light}}=\frac32-2B,
\]
so
\[
p_{\text{heavy}}^{\rm bad}=B-\frac25,\qquad
p_{\text{light}}^{\rm bad}=\frac35-\frac{3B}{5}.
\]

3. At the boundary `B=75/119`,
\[
\lambda=6-8B=\frac{114}{119}.
\]
So the boundary point is overwhelmingly cycle-like, with a triangle weight only `5/119`.

Its bad frequencies are exactly
\[
p_{\text{heavy}}^{\rm bad}=\frac{137}{595}\approx 0.230252,\qquad
p_{\text{light}}^{\rm bad}=\frac{159}{1190}\approx 0.133613.
\]

The universal good-floor contributes
\[
\frac{1-B}{17}=\frac{44}{2023}\approx 0.021751.
\]

Therefore, with only total forced 4-support mass `Q`,
\[
p_{\max}\ge \frac{137}{595}+\frac{44}{2023}+\frac{32}{85}Q.
\]

To reach `0.39`, one needs
\[
Q \ge \frac{85}{32}\left(\frac{39}{100}-\frac{137}{595}-\frac{44}{2023}\right)\approx 0.3666.
\]

But here
\[
1-B=\frac{44}{119}\approx 0.3697.
\]

So at the boundary, this LP requires at least about `99.2%` of **all** good mass to be certified as 4-support bonus mass. That is an extremely tight requirement.

4. If, optimistically, all good mass were such forced 4-support mass and uniformly distributed among the 5 supports, then at `B=75/119`
\[
Q=1-B=\frac{44}{119},
\]
and the heavy vertices would get
\[
p_{\text{heavy}}
=\frac{137}{595}+\frac{37}{85}\cdot\frac{44}{119}
=\frac{3957}{10115}\approx 0.39120.
\]
So the boundary point is only barely above target even in this best-case LP scenario.

5. The triangle endpoint kills every disjoint-pair-only bonus scheme. At `B=3/4`, there are no disjoint bad pairs, so `Q=0`, and the heavy vertex frequency is only
\[
p_{\max}=\left(\frac34-\frac25\right)+\frac{1/4}{17}
=\frac7{20}+\frac1{68}
=\frac{31}{85}
\approx 0.364706<0.39.
\]

That is an exact LP obstruction.

---

**DEAD**

A certificate using only:

- the bad edge masses `b_e`,
- the universal `1/17` floor on all good mass,
- and bonuses from disjoint-pair-forced 4-support fibers,

cannot prove `max p_x ≥ 0.39` for all `B<75/119`.

Reason: the triangle atom is feasible, has `Q=0`, and gives the exact countervalue
\[
\frac{31}{85}<0.39.
\]
So any such LP relaxation is globally insufficient.

This matches the ledger’s “triangle atom kills every disjoint-pair-only bonus scheme,” now with the exact endpoint value.

---

**OPEN**

The missing LP bridge is a coupling inequality of the form
\[
Q \ge \Psi(b)
\]
or, better, vertexwise
\[
Q-q_i \ge \Psi_i(b),
\]
that is positive on both atoms:

- positive on the cycle channel,
- positive on the triangle channel.

Without such a two-channel constraint, the optimization problem is underdetermined: `b` alone does not force enough `q`.

So the 10-variable bad-mass LP is solved as far as this relaxation goes:

- boundary `B=75/119` is barely winnable only if almost all good mass is promoted to 4-support bonus mass,
- the triangle endpoint makes the disjoint-pair-only LP approach formally impossible.

#### The Explorer (codex/gpt-5.4)

**TRANSLATED**

Let  
\[
L=\binom{[5]}2
\]
be the 10 bad labels, and let  
\[
\mathcal C_3=\binom{[5]}3,\qquad \mathcal C_4=\binom{[5]}4
\]
be the 3-support and 4-support chambers.

Define the bipartite **support-chamber graph**
\[
\Gamma=(L,\mathcal C_3\sqcup \mathcal C_4)
\]
by containment: a label \(e\in L\) is adjacent to a chamber \(U\) iff \(e\subset U\).

This graph packages both forcing channels at once:

- If \(e,f\) intersect, then they have a unique common neighbor in \(\mathcal C_3\), namely \(U=e\cup f\).
- If \(e,f\) are disjoint, then they have a unique common neighbor in \(\mathcal C_4\), namely \(U=e\cup f\).

So every pair of bad labels determines a unique chamber node on the right. In this language, the residual problem is not a Petersen-graph LP anymore; it is a **right-neighbor load problem** on \(\Gamma\): can one force enough good mass onto chamber nodes so that some ground vertex \(i\in[5]\) receives enough incident chamber load?

For a chamber \(U\), any good fiber carrying that chamber gives every \(i\in U\) the same extra bonus \(8/17\) above the universal \(1/17\) floor, by [P14] and [P15]. Thus triangle-support and 4-support witnesses are the same object in this translated graph: a chamber node with support \(U\subset[5]\).

---

**PROVEN**

Let \(m_U\ge 0\) be any **disjointly packed chamber mass** on \(U\in\mathcal C_3\sqcup\mathcal C_4\), meaning we assign pair-forced good mass to chamber nodes without double-counting any good fiber. Define the incident chamber load at vertex \(i\) by
\[
\sigma_i:=\sum_{U\ni i} m_U.
\]

Then
\[
p_i\ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac{8}{17}\sigma_i.
\]

This is the correct graph-theoretic master inequality: the bad side contributes the degree term, and the good side contributes through chamber incidence only.

Now specialize to the non-star slice-extreme segment from the ledger:
\[
b=\lambda c+(1-\lambda)t,\qquad \lambda=6-8B,\qquad \frac58\le B\le \frac34.
\]
For a heavy vertex on that segment,
\[
d_{\mathrm{heavy}}=2B-1,
\]
hence
\[
p_{\mathrm{heavy}}\ge B-\frac25+\frac{1-B}{17}+\frac{8}{17}\sigma_{\mathrm{heavy}}
=\frac{80B-29}{85}+\frac{8}{17}\sigma_{\mathrm{heavy}}.
\]

Therefore the target \(p_{\mathrm{heavy}}\ge 0.39\) follows as soon as
\[
\sigma_{\mathrm{heavy}}\ge \beta(B):=\frac{17}{8}\left(\frac{39}{100}-\frac{80B-29}{85}\right)
=\frac{105655-136000B}{68000}.
\]

Concrete endpoint values:

- At the triangle atom \(B=\frac34\),
\[
\beta\!\left(\frac34\right)=\frac{43}{800}=0.05375.
\]
So a single heavy vertex needs only \(5.375\%\) incident chamber load.

- At the boundary \(B=\frac{75}{119}\),
\[
\beta\!\left(\frac{75}{119}\right)=\frac{474589}{1618400}\approx 0.29325.
\]

So the whole residual regime reduces to a single graph statement:

> Force \(\max_i \sigma_i\ge \beta(B)\) from the bad-label mass \(b\), using only the chamber-incidence structure of \(\Gamma\).

This unifies the triangle and disjoint-pair channels. The triangle endpoint is no longer mysterious: it just asks for a tiny amount of chamber load on one heavy vertex.

---

**PROVEN**

A disjoint-pair-only chamber theory is exactly the restriction to \(\mathcal C_4\). That restriction is dead for the translated reason already visible in \(\Gamma\):

- the triangle atom lives entirely through one \(\mathcal C_3\)-chamber,
- and gives zero \(\mathcal C_4\)-load strong enough to close the gap.

So any successful Hall/matching argument must use both sides of the right part:
\[
\mathcal C_3 \quad\text{and}\quad \mathcal C_4.
\]

---

**DEAD**

Any certificate that depends only on the left-side Petersen profile \(b\) and ignores how mass can be packed onto the right-side chambers of \(\Gamma\) is too coarse. The triangle and cycle endpoints have different right-neighbor geometry, even when left-side degree data is already exhausted.

So the correct foreign-domain object is not the stable-set polytope of the Petersen graph alone. It is the **bipartite chamber incidence graph** \(\Gamma\) together with a Hall-type packing of pair-forced joins onto its right side.

---

**OPEN**

The missing bridge is now very sharp.

Prove a Hall/b-matching theorem on \(\Gamma\) of the form
\[
\max_{i\in[5]}\sigma_i \ge \Psi(b),
\]
for every feasible non-star bad profile \(b\), where \(\Psi(b)\ge \beta(B)\) on the triangle-cycle segment.

Equivalent endpoint targets:

- Triangle endpoint: force chamber load at least \(43/800\) onto one heavy vertex through the unique \(\mathcal C_3\)-chamber.
- Boundary point \(B=75/119\): force chamber load at least \(0.29325\) onto some vertex, using both \(\mathcal C_3\) and \(\mathcal C_4\).

So the workshop problem is no longer “solve a 10-variable LP.” It is:

> Build a fractional Hall theorem for the join-map from bad-label classes to support chambers in the bipartite graph \(\Gamma\).

That is the structural translation I would keep.

### BUILD — Pass 3

#### The Proof Writer (codex/gpt-5.4)

**TRANSLATED**

Work on the bipartite chamber-incidence graph
\[
\Gamma=\Big(\binom{[5]}2,\ \binom{[5]}3\sqcup\binom{[5]}4\Big),
\]
with left variables \(b_e=B_e\) and right-side good-mass variables packed onto chamber nodes.

For \(i\in[5]\), write
\[
d_i:=\sum_{e\ni i} b_e,\qquad B:=\sum_e b_e.
\]
Let \(\tau_i\) be the total packed good mass on \(3\)-chambers containing \(i\), and \(\sigma_i\) the total packed good mass on \(4\)-chambers containing \(i\).

I will isolate the exact LP obstruction for any chamber-load proof that does not build a new coupling \(b\mapsto (\tau,\sigma)\).

---

**PROVEN**

**Lemma 1.** Suppose a chamber certificate uses only the following two support bonuses above the universal floor \(1/17\):

1. every packed \(3\)-chamber mass contributes at least \(\beta_3\) to each support vertex;
2. every packed \(4\)-chamber mass contributes at least \(\beta_4\) to each support vertex.

Then every vertex \(i\) satisfies
\[
p_i\ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\beta_3\tau_i+\beta_4\sigma_i.
\]

*Proof.* The bad-fiber contribution is exactly \(\frac B5+\frac25 d_i\) by [P6]. The remaining mass is good, so [P14] contributes the universal floor \((1-B)/17\). By definition of \(\tau_i,\sigma_i\), the packed chamber bonuses add linearly. This is an LP superposition inequality. \(\square\)

---

**Lemma 2.** In the triangle atom \(t\) at \(B=\frac34\), any certificate of Lemma 1 can only force \(p_{\max}\ge 0.39\) if
\[
\beta_3\ge \frac{43}{400}=0.1075.
\]

*Proof.* On the triangle atom there are no disjoint bad pairs, hence no forced \(4\)-chamber load. The heavy-vertex bad contribution is
\[
\frac B5+\frac25 d_{\mathrm{heavy}}=\frac35-\frac{3B}{5}=\frac 3{20},
\]
using the verified endpoint formula from the ledger at \(B=\frac34\), so after adding the universal floor,
\[
p_{\mathrm{heavy}}\ge \frac3{20}+\frac{1/4}{17}+\beta_3\tau_{\mathrm{heavy}}
=\frac{31}{85}+\beta_3\tau_{\mathrm{heavy}}.
\]
Also \(\tau_{\mathrm{heavy}}\le 1-B=\frac14\). Therefore
\[
p_{\mathrm{heavy}}\le \frac{31}{85}+\frac{\beta_3}{4}.
\]
To reach \(39/100\), one must have
\[
\frac{31}{85}+\frac{\beta_3}{4}\ge \frac{39}{100}.
\]
Solving gives
\[
\beta_3\ge 4\Big(\frac{39}{100}-\frac{31}{85}\Big)=\frac{43}{400}.
\]
\(\square\)

---

**Lemma 3.** At the residual boundary point \(B=\frac{75}{119}\), any certificate of Lemma 1 with only the verified \(4\)-chamber bonus \(\beta_4=\frac 8{17}\) requires
\[
\max_i \sigma_i \ge \frac{474589}{1618400}\approx 0.29325.
\]

*Proof.* On the non-star extremal segment, the heavy-vertex bad part is
\[
\frac B5+\frac25 d_{\mathrm{heavy}}=B-\frac25.
\]
Hence Lemma 1 gives
\[
p_{\mathrm{heavy}}\ge B-\frac25+\frac{1-B}{17}+\frac8{17}\sigma_{\mathrm{heavy}}.
\]
Substitute \(B=\frac{75}{119}\):
\[
B-\frac25+\frac{1-B}{17}
=\frac{137}{595}+\frac{44}{2023}.
\]
Therefore \(p_{\mathrm{heavy}}\ge 39/100\) only if
\[
\frac8{17}\sigma_{\mathrm{heavy}}
\ge \frac{39}{100}-\frac{137}{595}-\frac{44}{2023}.
\]
Multiplying by \(17/8\) yields
\[
\sigma_{\mathrm{heavy}}
\ge \frac{474589}{1618400}.
\]
\(\square\)

---

**Lemma 4.** Under uniform support distribution of \(4\)-chamber mass at \(B=\frac{75}{119}\), the total \(4\)-chamber mass must satisfy
\[
Q_4\ge \frac54\cdot \frac{474589}{1618400}
=\frac{2372945}{6473600}\approx 0.36656.
\]
Equivalently, at least
\[
\frac{2372945/6473600}{44/119}
=\frac{282380455}{284838400}
\approx 0.99137
\]
of all good mass must be promoted into \(4\)-chamber bonus mass.

*Proof.* If the omitted-support loads are equalized, then each vertex sees incident \(4\)-chamber load
\[
\sigma_i=\frac45 Q_4.
\]
Combine with Lemma 3:
\[
\frac45 Q_4\ge \frac{474589}{1618400}.
\]
This gives the first formula. Since total good mass is \(1-B=\frac{44}{119}\), division gives the second. \(\square\)

---

**DEAD**

The following LP template is now formally exhausted:

- variables \(b_e\) on the \(10\) label edges,
- universal good floor \(1/17\),
- a fixed support bonus \(\beta_4=\frac8{17}\) on \(4\)-chambers,
- any \(3\)-chamber bonus with no new coupling from \(b\) to \(\tau\),
- and no structural inequality relating the bad profile \(b\) to the chamber-load profile \((\tau,\sigma)\).

Lemmas 2 and 4 show why:

1. the triangle endpoint demands a genuine \(3\)-chamber support bonus of at least \(43/400\);
2. the cycle-side boundary demands that about \(99.137\%\) of all good mass be certified as \(4\)-chamber bonus mass.

So an “uncoupled chamber LP” is too weak on both channels at once.

---

**OPEN**

The missing bridge is a vertex-sensitive coupling theorem on \(\Gamma\), not a new degree calculation. The needed form is:

\[
\beta_3\tau_i+\frac8{17}\sigma_i \ge \Psi_i(b)
\]
for some explicit linear or convex functional \(\Psi_i\) that is positive on both atoms:

- triangle channel: positive when bad mass sits on a \(K_3\);
- cycle channel: positive when bad mass sits on the \(5\)-cycle profile near \(B=\frac{75}{119}\).

Without such a coupling, the chamber-load LP has no mechanism forcing enough right-side incidence on a heavy vertex.

That is the structural obstruction I would record from the LP side.

#### The Calculator (codex/gpt-5.4)

**TRANSLATED**

I translated the residual \(k=5\) problem into a pure minimax LP on two regular incidence operators.

- Left operator: the \(5\times 10\) vertex-edge incidence matrix of \(K_5\), acting on bad edge masses \(x_e=B_e\).
- Right operators: the \(5\times 10\) vertex-triple incidence matrix and the \(5\times 5\) vertex-\(4\)-set incidence matrix, acting on certified good masses on \(3\)-supports and \(4\)-supports.

For \(i\in[5]\), define
\[
d_i:=\sum_{e\ni i}x_e,
\qquad
\lambda_i:=\sum_{U\in\binom{[5]}3,\ i\in U} y_U+\sum_{V\in\binom{[5]}4,\ i\in V} z_V.
\]
Here \(y_U\) is certified good mass on \(3\)-support chamber \(U\), and \(z_V\) is certified good mass on \(4\)-support chamber \(V\).

Using only [P6], [P14], [P15], every vertex satisfies
\[
p_i\;\ge\;\frac B5+\frac25 d_i+\frac{1-B}{17}+\frac8{17}\lambda_i.
\]

So the uncoupled chamber LP is:

\[
\min m
\]
subject to
\[
m\ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac8{17}\lambda_i
\quad (i=1,\dots,5),
\]
with
\[
x_e\ge 0,\ \sum_e x_e=B,
\]
\[
y_U\ge 0,\ \sum_U y_U=Q_3,
\]
\[
z_V\ge 0,\ \sum_V z_V=Q_4.
\]

No combinatorics is used here: this is just load balancing on two regular hypergraph incidence matrices.

---

**PROVEN**

The above LP is exactly solvable.

Because
\[
\sum_{i=1}^5 d_i = 2B,
\qquad
\sum_{i=1}^5 \lambda_i = 3Q_3+4Q_4,
\]
averaging the five vertex constraints gives the dual lower bound
\[
m\ge
\frac B5+\frac25\cdot \frac{2B}{5}+\frac{1-B}{17}+\frac8{17}\cdot \frac{3Q_3+4Q_4}{5}.
\]
Hence
\[
m\ge
\frac{9B}{25}+\frac{1-B}{17}+\frac{8}{85}(3Q_3+4Q_4).
\tag{1}
\]

This lower bound is attained by the uniform primal point:
\[
x_e=\frac{B}{10}\quad(\forall e),
\qquad
y_U=\frac{Q_3}{10}\quad(\forall U\in\binom{[5]}3),
\qquad
z_V=\frac{Q_4}{5}\quad(\forall V\in\binom{[5]}4).
\]
Then
\[
d_i=\frac{2B}{5},
\qquad
\lambda_i=\frac{3Q_3+4Q_4}{5}
\quad(\forall i),
\]
so all five vertex constraints are equalities.

Therefore the exact optimum of the uncoupled LP is
\[
m^*(B;Q_3,Q_4)=
\frac{9B}{25}+\frac{1-B}{17}+\frac{8}{85}(3Q_3+4Q_4).
\tag{2}
\]

This is the complete LP solution.

---

**PROVEN**

At fixed total certified chamber mass \(Q:=Q_3+Q_4\), the adversary-minimizing value is still given by (2), and the prover-maximizing choice is to put all certified mass on \(4\)-supports, since \(4>3\). So
\[
m^*(B;Q)\le \frac{9B}{25}+\frac{1-B}{17}+\frac{32}{85}Q.
\]
Even in the strongest possible uncoupled case \(Q=1-B\), one gets
\[
m^*_{\mathrm{best}}(B)=\frac{9B}{25}+\frac{1-B}{17}+\frac{32}{85}(1-B)
=\frac{185-32B}{425}.
\tag{3}
\]

At the boundary \(B=\frac{75}{119}\),
\[
m^*_{\mathrm{best}}\!\left(\frac{75}{119}\right)
=
\frac{185-32\cdot 75/119}{425}
=
\frac{3923}{10115}
\approx 0.387839.
\]

This is strictly below \(0.39\).

So even if:

- every unit of good mass is fully certified,
- every certified unit is promoted to the stronger \(4\)-support bonus,
- and the good mass is packed in the best possible uncoupled way,

the exact LP minimum is still \(0.387839<0.39\).

---

**DEAD**

Any certificate whose optimization content is only:

- bad edge masses \(x_e\),
- total certified \(3\)-support mass \(Q_3\),
- total certified \(4\)-support mass \(Q_4\),
- the universal \(1/17\) good floor,
- the support bonus \(8/17\) on certified chambers,

and **no coupling constraint linking where the bad mass sits to where the chamber load lands**, is dead.

The exact LP optimum is the fully symmetric solution, and at the boundary it gives
\[
\frac{3923}{10115}\approx 0.387839<0.39.
\]

This is stronger than the earlier “\(99.137\%\) of good mass must be \(4\)-certified” obstruction: even \(100\%\) \(4\)-certification does not suffice without a localization/correlation constraint.

---

**OPEN**

What is missing is not another scalar LP bound. It is a coupling inequality of the form
\[
\lambda_i \ge \Phi_i(x)
\]
for some explicit functional \(\Phi_i\) that forces chamber load onto vertices already heavy in the bad profile.

In optimization language: the independent marginals are solved exactly, and the symmetric primal beats them. To cross \(0.39\), one must add a new constraint that forbids simultaneous flattening of

- the bad degree profile \(d_i\), and
- the certified chamber-load profile \(\lambda_i\).

That coupling is the remaining bridge.

#### The Explorer (codex/gpt-5.4)

**TRANSLATED**

Let `E = R^{binom([5],2)}` be the bad-label mass space on the `10` edges of `K_5`, with coordinates `b_e = B_e`.  
Let `V = R^[5]` be the vertex space, with degree load
\[
d_i=(Ab)_i=\sum_{e\ni i} b_e,
\]
where `A` is the `5 x 10` vertex-edge incidence matrix.

Let `W = R^[5]` be the chamber space indexed by omitted vertices: chamber `j` means the `4`-set `[5]\setminus\{j\}`. Any per-vertex good bonus obtained from chamber loads is another vector in `V`.

So the residual problem is not “count sets”; it is:

- left side: a mass vector `b` on the `10` vertices of the Petersen label graph;
- right side: a chamber-load vector in a `5`-dimensional permutation module;
- target: force a heavy coordinate in `V`.

The foreign-domain question is: what maps from `E` to `V` can possibly transfer bad mass into vertex-local good bonus?

---

**PROVEN**

**Spectral obstruction for all linear symmetric couplings.**  
Any `S_5`-equivariant linear map
\[
T:E\to V
\]
has the form
\[
(Tb)_i=\alpha\, d_i+\beta\, B,
\qquad B=\sum_e b_e,
\]
for some scalars `alpha,beta`.

Equivalently: the only linear symmetric vertex-local statistics of the bad profile are total mass and degree.

*Proof.* As permutation modules,
\[
V \cong \mathbf{1}\oplus \mathbf{4},
\qquad
E \cong \mathbf{1}\oplus \mathbf{4}\oplus \mathbf{5}.
\]
Hence
\[
\dim \operatorname{Hom}_{S_5}(E,V)=2,
\]
coming from the common irreducible summands `1` and `4`. Concretely those two maps are:

1. `b -> B * 1`, the total-mass map;
2. `b -> Ab`, the degree map.

So every equivariant linear transfer is their linear combination. `square`

A chamber-load map `E -> W` has the same obstruction, since `W` is another copy of the vertex permutation module. Therefore any symmetric linear transport from bad labels into `4`-chamber loads, and then into vertex bonuses, still collapses to
\[
\text{const}\cdot B + \text{const}\cdot d_i.
\]

---

**DEAD**

This kills an entire class of approaches at once:

- any linear coupling `b -> lambda` from bad-label masses to per-vertex certified good load;
- any such coupling that is `S_5`-equivariant;
- any chamber-packing certificate whose only new input is a linear symmetric function of the `B_e`.

Reason: after the spectral collapse above, the final lower bound is still of the form
\[
p_i \ge c_0(B)+c_1 d_i,
\]
and the exact minimax for that degree-only geometry is already exhausted in the ledger. The missing information lives in the `5`-dimensional irreducible summand of `E`, which has no linear image in `V`.

So the residual obstruction is not “another LP coefficient”; it is a representation-theoretic kernel.

---

**OPEN**

The only plausible transfer left is **nonlinear**. In graph language, it must see the Petersen adjacency, not just the degree projection.

The canonical quadratic chamber statistic is:
\[
q_j \;:=\!\!\sum_{\{e,f\}\text{ opposite in }K_4([5]\setminus\{j\})}\!\!\! b_e b_f,
\]
one term for each of the three disjoint edge-pairs spanning chamber `j`.

This `q_j` detects exactly the disjoint-pair geometry that linear maps erase. It is also the right object spectrally: it depends on the Petersen-graph adjacency inside the chamber.

So the structural bridge I would record is:

- linear symmetric transfer is provably exhausted;
- the remaining route must be a **quadratic Hall/transport theorem** converting opposite-pair mass `q_j` into actual chamber load on the same `j`;
- once such a theorem exists, one can push that chamber load back to vertices through the `4`-support bonus.

A useful target statement would be:

\[
\lambda_i \ge c \sum_{j\ne i} q_j
\]
for some explicit `c>0`, or a fractional-matching variant with the same effect.

That is the graph-theoretic frontier: the proof must use a nonlinear transport on the Petersen disjointness graph, because all equivariant linear maps have already collapsed to the dead degree LP.

### VERIFY

#### The Proof Writer (codex/gpt-5.4)

**Findings**

1. **WRONG**  
Tag as `DEAD: slice-extreme half-integrality / triangle-cycle reduction` because the slice argument is false.

The claim in pass 2 that every extreme point of
\[
\mathcal F_\tau=\{y\ge 0,\ y_u+y_v\le 1,\ \sum_v y_v=\tau\}
\]
is half-integral is incorrect. A concrete counterexample is
\[
\tau=\frac15,\qquad y=\frac15\,e_v
\]
for any label-vertex \(v\) of the Petersen graph. This point is feasible, and it is a vertex of the slice since it is the intersection of the hyperplane \(\sum y_v=\frac15\) with the ambient edge from \(0\) to \(e_v\). Its nonzero coordinate is \(1/5\), not in \(\{0,\frac12,1\}\).

So pass-2 Lemma 1 is false. As a consequence, pass-2 Lemmas 2, 3, and the reduction to a unique \(5\)-cycle atom are not valid.

2. **WRONG**  
Tag as `DEAD: “only non-star obstruction is the triangle-cycle segment”` because the ambient stable-set classification was misused.

Pass 2 treats every integral ambient vertex as a star-type or triangle-type stable set. That is not true for the Petersen stable-set polytope: singleton stable sets, 2-edge intersecting families, and many 3-edge subfamilies are also stable sets. So even after fixing the half-integrality issue, the claimed atom list is incomplete.

Hence Proposition 5 and Lemma 6 from pass 2 are not justified.

3. **CORRECT**  
Tag as `PROVEN: exact optimum of the uncoupled chamber LP`.

The Calculator pass 3 result is correct:
\[
m^*(B;Q_3,Q_4)=\frac{9B}{25}+\frac{1-B}{17}+\frac{8}{85}(3Q_3+4Q_4).
\]
Proof is by averaging the five vertex constraints, using
\[
\sum_i d_i=2B,\qquad \sum_i \lambda_i=3Q_3+4Q_4,
\]
and then observing that the uniform primal point attains equality. This is a complete exact solution of that LP.

The corollary
\[
m^*_{\rm best}(B)=\frac{185-32B}{425}
\]
when all good mass is fully certified on \(4\)-supports is also correct, as is
\[
m^*_{\rm best}\!\left(\frac{75}{119}\right)=\frac{3923}{10115}\approx 0.387839<0.39.
\]

So the whole uncoupled-chamber LP template is genuinely dead.

4. **CORRECT**  
Tag as `PROVEN: equivariant linear transfer E\to V collapses to degree plus total mass`.

The Explorer pass 3 representation-theoretic claim is correct. As \(S_5\)-modules,
\[
V\cong \mathbf 1\oplus \mathbf 4,\qquad E\cong \mathbf 1\oplus \mathbf 4\oplus \mathbf 5,
\]
so
\[
\dim \mathrm{Hom}_{S_5}(E,V)=2.
\]
Concretely the two basis maps are \(b\mapsto B\mathbf 1\) and \(b\mapsto d\). Therefore every \(S_5\)-equivariant linear coupling is
\[
(Tb)_i=\alpha d_i+\beta B.
\]
That correctly kills every linear symmetric localization scheme.

5. **WRONG**  
Tag as `DEAD: pass-3 triangle threshold 43/400` because the arithmetic is off.

In Proof Writer pass 3, the necessary threshold for a \(3\)-chamber bonus at the triangle atom was stated as
\[
\beta_3\ge \frac{43}{400}.
\]
That is incorrect.

At the triangle atom \(B=\frac34\), the heavy bad contribution is
\[
B-\frac25=\frac{7}{20},
\]
not \(\frac{3}{20}\). After adding the universal floor,
\[
\frac{7}{20}+\frac{1/4}{17}=\frac{31}{85}.
\]
Since \(\tau_{\rm heavy}\le \frac14\), the correct necessary inequality is
\[
\frac{31}{85}+\frac{\beta_3}{4}\ge \frac{39}{100},
\]
hence
\[
\beta_3\ge 4\left(\frac{39}{100}-\frac{31}{85}\right)=\frac{43}{425}\approx 0.101176,
\]
not \(\frac{43}{400}\).

So the numerical claim is wrong, although the qualitative point remains true.

6. **CORRECT**  
Tag as `PROVEN: boundary 4-chamber load threshold`.

Proof Writer pass 3’s boundary calculation is correct:
\[
\sigma_{\rm heavy}\ge \frac{17}{8}\left(\frac{39}{100}-\frac{137}{595}-\frac{44}{2023}\right)
=\frac{474589}{1618400}\approx 0.29325.
\]
The derived uniform-load consequence
\[
Q_4\ge \frac54\cdot \frac{474589}{1618400}\approx 0.36656
\]
is also correct.

7. **UNJUSTIFIED**  
Tag as `OPEN: quadratic chamber statistic q_j must be tied to actual certified load`.

The Explorer’s proposed quadratic statistic
\[
q_j=\sum_{\{e,f\}\text{ opposite in chamber }j} b_eb_f
\]
is a sensible new object, but no theorem was proved that converts \(q_j\) into actual chamber load \(\lambda_i\) or \(\sigma_i\). That bridge is still missing.

So this is not a proven advance yet; it is an open direction.

**Overall Assessment**

- `FATAL FLAW: yes`

The fatal flaw is the pass-2 reduction to the triangle-cycle segment. It rests on a false claim about slice vertices being half-integral and on an incomplete classification of stable sets. That reduction cannot be used to shrink the residual problem to one dimension.

The genuinely solid progress is negative:
- the uncoupled chamber LP is exactly solved and dead;
- every equivariant linear coupling is dead.

So the next iteration should not try to repair the slice-extreme classification. A genuinely different direction is needed.

The best new direction is a **nonlinear spectral/SDP transport** on the Petersen/chamber incidence structure: build a quadratic or semidefinite certificate that sees the \(\mathbf 5\)-isotypic component of the bad profile and forces localized chamber load from that energy. That is different in kind from the dead LP/linear-coupling program.

- `PROGRESS RATING: 4/10`

There is real cleanup: two broad LP classes are now rigorously killed. But the main claimed structural reduction was false, so the positive path did not advance.

#### The Calculator (codex/gpt-5.4)

1. **CORRECT**  
Tag as `PROVEN: exact optimum of the uncoupled chamber LP`.

With variables `x_e` on the 10 bad edges, `y_U` on the 10 triple-chambers, and `z_V` on the 5 four-chambers, the vertex constraints are
\[
p_i\ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac8{17}\lambda_i,
\]
with
\[
d_i=\sum_{e\ni i}x_e,\qquad \lambda_i=\sum_{U\ni i}y_U+\sum_{V\ni i}z_V.
\]
Averaging over `i=1,\dots,5` gives
\[
m\ge \frac{9B}{25}+\frac{1-B}{17}+\frac{8}{85}(3Q_3+4Q_4),
\]
since `Σ_i d_i=2B` and `Σ_i λ_i=3Q_3+4Q_4`. The uniform feasible point attains equality, so this is the exact minimax value. In particular, even with `Q_4=1-B, Q_3=0`,
\[
m^*_{\rm best}(B)=\frac{185-32B}{425},
\]
and at `B=75/119`,
\[
m^*_{\rm best}=\frac{3923}{10115}\approx 0.387839<0.39.
\]
So that LP template is exhausted.

2. **CORRECT**  
Tag as `PROVEN: equivariant linear transfer E\to V collapses to degree plus total mass`.

As an `S_5`-equivariant optimization statement, any linear map from bad-edge mass space `E` to vertex-load space `V` must lie in the 2-dimensional span of total mass and degree load:
\[
(Tb)_i=\alpha d_i+\beta B.
\]
This follows from the module decomposition
\[
V\cong \mathbf 1\oplus \mathbf 4,\qquad E\cong \mathbf 1\oplus \mathbf 4\oplus \mathbf 5.
\]
Hence every symmetric linear localization scheme reduces to the already-dead degree LP.

3. **CORRECT**  
Tag as `PROVEN: boundary 4-chamber load threshold`.

At the boundary `B=75/119`, the heavy-coordinate lower bound is
\[
p_{\rm heavy}\ge \frac{137}{595}+\frac{44}{2023}+\frac8{17}\sigma_{\rm heavy}.
\]
Imposing `p_{\rm heavy}\ge 39/100` gives
\[
\sigma_{\rm heavy}\ge \frac{474589}{1618400}\approx 0.29325.
\]
Under uniform chamber-load distribution, this forces
\[
Q_4\ge \frac54\cdot \frac{474589}{1618400}\approx 0.36656.
\]
The arithmetic is sound.

4. **WRONG**  
Tag as `DEAD: slice-extreme half-integrality / triangle-cycle reduction` because the slice-vertex claim is false.

The pass-2 reduction relied on the assertion that extreme points of
\[
\mathcal F_\tau=\{y\ge 0,\ y_u+y_v\le 1,\ \sum_v y_v=\tau\}
\]
are half-integral. Counterexample: for any label-vertex `v` and `τ=1/5`,
\[
y=\frac15 e_v
\]
is a vertex of the slice, but its nonzero entry is `1/5`, not in `{0,1/2,1}`. Therefore the reduction to a one-parameter triangle-cycle segment is invalid.

5. **WRONG**  
Tag as `DEAD: “only non-star obstruction is the triangle-cycle segment”` because the stable-set classification used there is incomplete.

The ambient stable-set polytope contains many stable supports besides star-type and triangle-type ones: singletons, 2-edge intersecting supports, and multiple 3-edge supports. So the atom list used in pass 2 does not describe all feasible extreme support types. The claimed reduction is not justified.

6. **WRONG**  
Tag as `DEAD: pass-3 triangle threshold 43/400` because the endpoint arithmetic is off.

At the triangle atom with `B=3/4`, the heavy bad contribution is
\[
B-\frac25=\frac7{20},
\]
so after the universal floor,
\[
\frac7{20}+\frac{1/4}{17}=\frac{31}{85}.
\]
If `\tau_{\rm heavy}\le 1/4`, then the necessary threshold for a triple-chamber bonus is
\[
\frac{31}{85}+\frac{\beta_3}{4}\ge \frac{39}{100},
\]
hence
\[
\beta_3\ge \frac{43}{425}\approx 0.101176,
\]
not `43/400`.

7. **UNJUSTIFIED**  
Tag as `OPEN: quadratic chamber statistic must be coupled to actual chamber load`.

The proposed nonlinear statistic
\[
q_j=\sum_{\{e,f\}\text{ opposite in chamber }j} b_eb_f
\]
is a legitimate quadratic observable, but no certified transport inequality of the form
\[
\lambda_i\ge \Phi_i(q)\quad\text{or}\quad \sigma_i\ge \Psi_i(q)
\]
has been proved. What is missing is a rigorous optimization bridge from quadratic opposite-pair energy to realizable chamber load.

**Overall Assessment**

- `FATAL FLAW: yes`

The fatal flaw is the pass-2 structural reduction. It depends on a false half-integrality statement and an incomplete feasible-support classification, so the claimed one-dimensional residual model is not valid.

A genuinely new direction should be nonlinear and spectral/SDP in nature: introduce quadratic chamber-energy variables on opposite-pair incidences and seek a semidefinite or convex transport inequality that forces localized chamber load from that energy. That would use information in the `\mathbf 5`-component that all linear symmetric LPs provably erase.

- `PROGRESS RATING: 4/10`

#### The Explorer (codex/gpt-5.4)

**Findings**

1. **CORRECT**  
Tag as `PROVEN: exact optimum of the uncoupled chamber LP`.

With bad-edge masses `x_e`, triple-chamber masses `y_U`, and 4-chamber masses `z_V`, one has
\[
p_i\ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac8{17}\lambda_i,
\]
where
\[
d_i=\sum_{e\ni i}x_e,\qquad \lambda_i=\sum_{U\ni i}y_U+\sum_{V\ni i}z_V.
\]
Averaging over vertices gives
\[
m\ge \frac{9B}{25}+\frac{1-B}{17}+\frac{8}{85}(3Q_3+4Q_4),
\]
since `\sum_i d_i=2B` and `\sum_i \lambda_i=3Q_3+4Q_4`. The uniform feasible point attains equality, so this is the exact minimax. In particular, even with all good mass fully certified on 4-chambers,
\[
m^*_{\max}(B)=\frac{185-32B}{425},
\]
and at \(B=\frac{75}{119}\),
\[
m^*=\frac{3923}{10115}\approx 0.387839<0.39.
\]
So the uncoupled chamber-LP template is fully exhausted.

2. **CORRECT**  
Tag as `PROVEN: equivariant linear transfer E\to V collapses to degree plus total mass`.

As \(S_5\)-modules,
\[
V\cong \mathbf 1\oplus \mathbf 4,\qquad E\cong \mathbf 1\oplus \mathbf 4\oplus \mathbf 5.
\]
Hence
\[
\dim \mathrm{Hom}_{S_5}(E,V)=2,
\]
so every equivariant linear map \(T:E\to V\) is
\[
(Tb)_i=\alpha d_i+\beta B.
\]
Thus every symmetric linear localization scheme reduces to the already-dead degree-plus-mass information.

3. **CORRECT**  
Tag as `PROVEN: boundary 4-chamber load threshold`.

At \(B=\frac{75}{119}\), the heavy-vertex lower bound is
\[
p_{\rm heavy}\ge B-\frac25+\frac{1-B}{17}+\frac8{17}\sigma_{\rm heavy}.
\]
Solving \(p_{\rm heavy}\ge 0.39\) gives
\[
\sigma_{\rm heavy}\ge \frac{474589}{1618400}\approx 0.29325.
\]
Under uniform 4-chamber incidence, this forces
\[
Q_4\ge \frac54\cdot \frac{474589}{1618400}\approx 0.36656.
\]
The arithmetic is correct.

4. **WRONG**  
Tag as `DEAD: slice-extreme half-integrality / triangle-cycle reduction` because the slice-vertex claim is false.

The claimed half-integrality of extreme points of
\[
\mathcal F_\tau=\{y\ge 0,\ y_u+y_v\le 1,\ \sum_v y_v=\tau\}
\]
fails. For any label-vertex \(v\) and \(\tau=\frac15\),
\[
y=\frac15 e_v
\]
is a vertex of the slice, but its nonzero coordinate is \(1/5\), not in \(\{0,\tfrac12,1\}\). So the pass-2 reduction to a one-parameter triangle/cycle segment is invalid.

5. **WRONG**  
Tag as `DEAD: “only non-star obstruction is the triangle-cycle segment”` because the stable-support classification used there is incomplete.

Stable supports in the Petersen label graph are not only star-type or triangle-type. Singletons, 2-edge intersecting supports, and various 3-edge supports are also stable. So the claimed structural reduction is not justified.

6. **WRONG**  
Tag as `DEAD: pass-3 triangle threshold 43/400` because the endpoint arithmetic is incorrect.

At the triangle atom \(B=\frac34\), the heavy bad contribution is
\[
B-\frac25=\frac7{20},
\]
so after adding the universal floor,
\[
\frac7{20}+\frac{1/4}{17}=\frac{31}{85}.
\]
If \(\tau_{\rm heavy}\le \frac14\), then to reach \(0.39\) one needs
\[
\frac{31}{85}+\frac{\beta_3}{4}\ge \frac{39}{100},
\]
hence
\[
\beta_3\ge \frac{43}{425}\approx 0.101176,
\]
not \(43/400\).

7. **UNJUSTIFIED**  
Tag as `OPEN: quadratic chamber statistic must be coupled to realizable chamber load`.

The proposed quadratic chamber observable
\[
q_j=\sum_{\{e,f\}\text{ opposite in chamber }j} b_e b_f
\]
is a valid new object, but no theorem has been proved connecting \(q_j\) to actual certified chamber load \(\lambda_i\) or \(\sigma_i\). What is needed is a rigorous transport inequality, fractional matching theorem, or SDP certificate turning opposite-pair energy into localized chamber incidence.

**Overall Assessment**

- `FATAL FLAW: yes`

The fatal flaw is the pass-2 structural reduction. It depends on a false half-integrality claim and an incomplete classification of feasible stable supports, so the asserted 1-dimensional residual model is not valid.

The next iteration should not try to repair that reduction. The genuinely new direction is a **nonlinear transport/SDP approach** on the chamber-incidence graph: build a quadratic or semidefinite certificate from opposite-pair energy or chamber-pair correlations that forces localized chamber load on a heavy vertex. Linear symmetric couplings are now provably exhausted, so only a nonlinear localization mechanism can plausibly close the residual regime.

- `PROGRESS RATING: 4/10`

## Iteration 3

### BUILD — Pass 1

#### The Proof Writer (codex/gpt-5.4)

`TRANSLATED`

Let `E=E(K_5)` be the 10 bad labels, viewed as the 10 vertices of the Petersen label graph `P`; two labels are adjacent in `P` iff they are disjoint as 2-subsets of `[5]`. Write the bad mass profile as
\[
b=(b_e)_{e\in E}\in \mathbb R_{\ge 0}^{10},\qquad \sum_{e} b_e = B<\frac{75}{119}.
\]
For each chamber `c` (equivalently, each 4-subset of `[5]`), define its opposite-pair energy
\[
q_c:=\sum_{\{e,f\}\text{ opposite in }c} b_e b_f.
\]
Then `q_c` is exactly the quadratic mass on the 3 edges of the matching graph inside that chamber. The residual problem is to build a vertex-load certificate
\[
\sigma_i=\text{good mass carried by chambers through }i
\]
from the quadratic data `q=(q_c)` and prove for some `i`
\[
\frac B5+\frac25 d_i+\frac{1-B}{17}+\frac8{17}\sigma_i\ge \frac{39}{100}.
\]
The missing bridge is a nonlinear transport inequality from opposite-pair energy on `P` to chamber load on vertices.

## Lemma 1
`PROVEN` For every bad profile `b`,
\[
Q:=\sum_c q_c=\sum_{\{e,f\}\in E(P)} b_e b_f
=\frac12\, b^\top A_P b
=\frac12\Big(B^2+\|b\|_2^2-\sum_{i=1}^5 d_i^2\Big),
\]
where `A_P` is the adjacency matrix of the Petersen graph and `d_i=\sum_{e\ni i} b_e`.

**Proof.**
Each disjoint pair of labels spans a unique 4-subset, hence contributes to exactly one chamber, so
\[
Q=\sum_{\{e,f\}\in E(P)} b_e b_f=\frac12\,b^\top A_P b.
\]
Also
\[
\sum_{e<f} b_e b_f=\frac12\Big(B^2-\sum_e b_e^2\Big).
\]
Subtract the intersecting pairs. Two distinct edges of `K_5` intersect in a unique vertex, so
\[
\sum_{\substack{e<f\\ e\cap f\neq\varnothing}} b_e b_f
=\sum_{i=1}^5 \sum_{\substack{e<f\\ e,f\ni i}} b_e b_f
=\frac12\sum_{i=1}^5\Big(d_i^2-\sum_{e\ni i} b_e^2\Big).
\]
Since each label-edge is incident to exactly two vertices,
\[
\sum_{i=1}^5\sum_{e\ni i} b_e^2 = 2\sum_e b_e^2.
\]
Therefore
\[
Q=\frac12\Big(B^2-\sum_e b_e^2\Big)-\frac12\Big(\sum_i d_i^2-2\sum_e b_e^2\Big)
=\frac12\Big(B^2+\sum_e b_e^2-\sum_i d_i^2\Big).
\]
This is the claimed identity. ∎

## Lemma 2
`PROVEN` Let
\[
\mathbb R^{10}=\mathbf 1\oplus \mathbf 4\oplus \mathbf 5
\]
be the standard `S_5`-module decomposition of label space, and write
\[
b=b_{\mathbf 1}+b_{\mathbf 4}+b_{\mathbf 5}.
\]
Then
\[
Q=\frac{3}{20}B^2+\frac12\|b_{\mathbf 4}\|_2^2-\|b_{\mathbf 5}\|_2^2.
\]

**Proof.**
The Petersen adjacency acts by eigenvalues
\[
3 \text{ on } \mathbf 1,\qquad 1 \text{ on } \mathbf 4,\qquad -2 \text{ on } \mathbf 5.
\]
By Lemma 1,
\[
2Q=b^\top A_P b
=3\|b_{\mathbf 1}\|_2^2+\|b_{\mathbf 4}\|_2^2-2\|b_{\mathbf 5}\|_2^2.
\]
Now
\[
b_{\mathbf 1}=\frac{B}{10}(1,\dots,1),\qquad \|b_{\mathbf 1}\|_2^2=\frac{B^2}{10}.
\]
Hence
\[
2Q=\frac{3B^2}{10}+\|b_{\mathbf 4}\|_2^2-2\|b_{\mathbf 5}\|_2^2,
\]
so
\[
Q=\frac{3}{20}B^2+\frac12\|b_{\mathbf 4}\|_2^2-\|b_{\mathbf 5}\|_2^2.
\]
∎

**Interpretation.**
The quadratic opposite-pair observable detects the hidden `\mathbf 5`-mode that every equivariant linear `E\to V` transfer erases. This is the correct translated object. But it detects that mode only globally, with a negative sign.

## Lemma 3
`DEAD` Any certificate whose extra bonus depends only on the chamber-energy vector `q=(q_c)` cannot close the problem.

**Proof.**
Take any bad profile supported on an intersecting family of labels. Equivalently, the support is a stable set of the Petersen graph. By the verified stable-set classification, triangle-type supports are such examples. For every chamber `c`, there is then no disjoint pair inside the support, hence
\[
q_c=0\qquad\text{for all }c.
\]
So every bonus functional of the form `\Phi(q)` vanishes identically on the triangle atom.

But the ledger already verifies that the triangle atom is a feasible residual obstruction for every disjoint-pair-only bonus scheme. Numerically, at the endpoint `B=3/4`, the heavy vertex sits at
\[
\frac{7}{20}+\frac{1}{68}=\frac{31}{85}=0.364705\ldots,
\]
still short of `0.39` by
\[
\frac{39}{100}-\frac{31}{85}=\frac{43}{425}=0.101176\ldots.
\]
Since `q_c=0` on that profile, any bonus extracted only from `q` contributes exactly `0`, so such a certificate cannot prove the target. ∎

## Lemma 4
`OPEN` To make the quadratic translation useful, one must prove a transport inequality that couples opposite-pair energy to realized chamber load, not merely to total energy.

A sufficient form would be:
\[
\exists i\in[5]\quad \sigma_i\ge \Psi_i(b),
\]
where `\Psi_i` depends on chamber-incidence structure of the quadratic data, not just on `Q` or on `q_c` separately.

At the boundary `B=75/119`, the verified threshold is
\[
\sigma_i\ge \frac{474589}{1618400}=0.293249\ldots
\]
for some vertex `i`. Any translated proof has to produce at least that much localized load.

**Structural obstruction.**
The union map collapses many disjoint bad pairs into the same good chamber. Quadratic energy counts pairs in `E(P)`, while `\sigma_i` counts realizable chamber mass. The missing theorem is exactly a nonlinear many-to-one transport bound:
\[
\text{pair energy on }P \Longrightarrow \text{localized chamber mass}.
\]
Without that bridge, the quadratic language remains too coarse.

## Verdict

- `PROVEN`: the correct foreign-domain translation is the Petersen quadratic chamber-energy model.
- `PROVEN`: total opposite-pair energy has the exact identities in Lemmas 1 and 2.
- `DEAD`: any bonus depending only on disjoint-pair energy data `q` is killed by triangle-type stable supports.
- `OPEN`: a nonlinear chamber-load transport inequality coupling quadratic energy to realizable good mass. That is the precise bridge still missing.

#### The Calculator (codex/gpt-5.4)

`TRANSLATED`

Let `b=(b_e)_{e\in E(K_5)}` be the 10 bad-edge masses, with
\[
b_e\ge 0,\qquad \sum_e b_e=B<\frac{75}{119},
\]
and disjoint-pair caps
\[
b_e+b_f\le \frac14\qquad(e\cap f=\varnothing).
\]
For each vertex `i`, define the bad degree load
\[
d_i:=\sum_{e\ni i} b_e.
\]
For each forced 4-support good chamber `U` (a 4-subset of `[5]`), let `z_U` be its mass. By `[P15]` and `[P14]`, the extremal per-vertex contribution of one such chamber is exactly
\[
\frac1{17}\mathbf 1+\frac8{17}\mathbf 1_U.
\]
So if
\[
\lambda_i:=\sum_{U\ni i} z_U,
\]
then every certificate of the proposed type gives
\[
p_i\ge \frac B5+\frac25 d_i+\frac{1-B}{17}+\frac8{17}\lambda_i.
\]
This is the LP relaxation.

**PROVEN: exact minimax of the LP**

Set
\[
m(B):=\min \max_i\left(\frac B5+\frac25 d_i+\frac{1-B}{17}+\frac8{17}\lambda_i\right)
\]
subject to
\[
b_e\ge 0,\ \sum_e b_e=B,\ b_e+b_f\le \frac14\ (e\cap f=\varnothing),\ z_U\ge 0,\ \sum_U z_U=1-B.
\]

Averaging over the 5 vertices gives
\[
\frac15\sum_i d_i=\frac{2B}{5},\qquad \frac15\sum_i \lambda_i=\frac{4(1-B)}5,
\]
hence
\[
m(B)\ge \frac B5+\frac25\cdot \frac{2B}{5}+\frac{1-B}{17}+\frac8{17}\cdot \frac{4(1-B)}5
= \frac{9B}{25}+\frac{37(1-B)}{85}
= \frac{185-32B}{425}.
\]

Equality is attained by the uniform feasible point
\[
b_e=\frac{B}{10}\quad(\forall e),\qquad z_U=\frac{1-B}{5}\quad(\forall U).
\]
At this point
\[
d_i=\frac{2B}{5},\qquad \lambda_i=\frac{4(1-B)}5,
\]
so every vertex has exactly
\[
p_i=\frac{185-32B}{425}.
\]

Therefore
\[
\boxed{m(B)=\frac{185-32B}{425}}.
\]

The disjoint-pair constraints are inactive at the minimax point, because
\[
b_e+b_f=\frac{B}{5}<\frac14
\]
for every `B<5/4`, in particular for `B\le 75/119`.

**PROVEN: boundary value at \(B=75/119\)**

Substituting `B=75/119`,
\[
m\!\left(\frac{75}{119}\right)
=\frac{185-32\cdot(75/119)}{425}
=\frac{3923}{10115}
\approx 0.3878398418<0.39.
\]

So the concrete LP attack, even with all good mass optimistically upgraded to 4-support chambers, still misses by
\[
\frac{39}{100}-\frac{3923}{10115}
=\frac{437}{202300}
\approx 0.00216016.
\]

**PROVEN: minimum vertex frequencies in the forced good fibers**

For a forced 4-support chamber `U`:
- if `i\in U`, the sharp minimum is `9/17`,
- if `i\notin U`, the universal sharp floor is `1/17`.

So the sharp chamber contribution vector is exactly
\[
c^U_i=
\begin{cases}
9/17,& i\in U,\\
1/17,& i\notin U.
\end{cases}
\]

**PROVEN: boundary checks for the existing bounds**

At `B=75/119`:

- `[P9]` global threshold is exact:
\[
\frac15\left(\frac95+\frac{119}{500}\cdot \frac{75}{119}\right)=\frac{39}{100}.
\]

- `[P11]` old star bound gives
\[
\frac35\cdot \frac{75}{119}+\frac1{32}\left(1-\frac{75}{119}\right)
=\frac{53}{136}\approx 0.3897058824.
\]

- `[P17]` improved star bound with `b_2=0` already gives
\[
\frac35\cdot \frac{75}{119}+\frac1{17}\left(1-\frac{75}{119}\right)
=\frac{809}{2023}\approx 0.3999011369>0.39.
\]

- The present uncoupled LP gives
\[
\frac{3923}{10115}\approx 0.3878398418.
\]

**DEAD**

The proposed 10-variable/uncoupled chamber LP does **not** close the residual regime. Its exact adversary optimum is the uniform bad-edge profile, and the exact optimum at the boundary is below `0.39`.

**OPEN**

Any remaining proof must add a new constraint coupling the bad-edge profile `b` to **localized** chamber load beyond the average
\[
\lambda_i=\frac{4(1-B)}5.
\]
Pure degree balancing plus uncoupled 4-chamber bonuses is now fully exhausted.

#### The Explorer (codex/gpt-5.4)

`TRANSLATED`

The chamber-balanced non-star residual is most naturally a **fractional stable-set problem on the Petersen graph** `P`.

- Vertices of `P` are the 10 bad labels `e∈E(K_5)`.
- Edges of `P` are disjoint label-pairs.
- A 4-support chamber `U=[5]\setminus{v}` is exactly one of the 5 perfect matchings inside `K_5`; on a support carried by a 5-cycle of labels, each chamber is hit exactly once.

The key support-level atom is the cycle
\[
C=\{12,23,34,45,51\}\subset E(K_5).
\]
In `P`, those 5 labels form an induced `C_5`, because consecutive cycle edges in `K_5` are disjoint exactly when they are distance 2 along the pentagon.

`PROVEN`

Let `x_1,\dots,x_5` be the bad masses on that `C_5`, in cyclic order. Then the disjoint-pair caps are exactly
\[
x_i+x_{i+1}\le \frac14 \qquad (i\!\!\!\pmod 5)
\]
after relabeling along the Petersen `C_5`. Summing the 5 edge-constraints gives
\[
2\sum_{i=1}^5 x_i \le \frac54,
\]
hence
\[
\boxed{B_C:=\sum_{i=1}^5 x_i \le \frac58.}
\]

This is the odd-cycle facet of the fractional stable-set polytope, stated in the Petersen language.

So the chamber-balanced `C_5` atom cannot carry bad mass above `5/8 = 0.625`.

`PROVEN`

The dead uncoupled LP extremizer is support-compatible with this `C_5` atom for every `B\le 5/8`:

- take `x_i=B/5` on the 5 cycle labels,
- take the 5 chambers equally weighted.

Then each vertex load is uniform:
\[
d_i=\frac{2B}{5},\qquad \lambda_i=\frac{4(1-B)}5,
\]
and the LP value is exactly
\[
p_i=\frac{185-32B}{425}.
\]

Thus the LP failure is not a fake artifact below `5/8`; it is already modeled by the Petersen odd cycle.

`DEAD`

Odd-cycle/fractional-stable-set inequalities alone do not close the conjecture.

At the endpoint `B=5/8`, the cycle atom still gives
\[
p_i=\frac{33}{85}\approx 0.388235<0.39.
\]
So pure stable-set polytope control on the Petersen side is still short.

`OPEN`

This does isolate the remaining bridge sharply:

- For `B>5/8`, the balanced Petersen `C_5` obstruction disappears.
- Therefore any residual profile in `5/8 < B < 75/119` must leave the odd-cycle face.

What needs proving in graph language is:

\[
\text{mass outside the Petersen } C_5
\Longrightarrow
\text{either a double-hit chamber or a near-star concentration.}
\]

A usable form would be:

- either some chamber is hit by at least two Petersen edges, activating `[P16]` / overlap bonuses,
- or the support collapses toward a star regime already closed by `[P17]`.

That is the structural transfer I would attack next.

### BUILD — Pass 2

#### The Proof Writer (codex/gpt-5.4)

`TRANSLATED`

Let `P` be the Petersen label graph on the `10` labels `e∈E(K_5)`, with `e~f` iff `e∩f=∅`. Write the bad masses as
\[
b=(b_e)_{e\in E(K_5)},\qquad b_e\ge 0,\qquad \sum_e b_e=B,
\]
with residual constraints
\[
b_e+b_f<\frac14\qquad(e\sim f).
\]

For each omitted vertex `v∈[5]`, let
\[
U_v:=[5]\setminus\{v\}
\]
be the corresponding `4`-support chamber, and define its bad chamber load
\[
w_v:=\sum_{e\subseteq U_v} b_e.
\]
Inside chamber `U_v`, there are exactly `3` opposite pairs; write their sums as
\[
u_{v,1},u_{v,2},u_{v,3}.
\]

The foreign-domain problem is therefore:

Find a vertex-forcing inequality on the fractional stable-set polytope of `P` that uses more than the coarse summaries
\[
(d_i)_{i=1}^5,\qquad (w_v)_{v=1}^5,
\]
because the existing linear transfer on those summaries is already exhausted.

**Lemma 1 (Exact chamber identity). PROVEN**

For every chamber `U_v`,
\[
w_v=u_{v,1}+u_{v,2}+u_{v,3},
\]
and globally
\[
\sum_{v=1}^5 w_v = 3B.
\]

**Proof.**
A `4`-set has `6` internal edges. Those `6` edges split uniquely into the `3` opposite pairs of the chamber, so the chamber total is the sum of the `3` opposite-pair sums:
\[
w_v=u_{v,1}+u_{v,2}+u_{v,3}.
\]
Now sum over the `5` chambers. A label `e=ab` lies in exactly the `3` chambers omitting one of the other `3` vertices, hence contributes `3b_e` to \(\sum_v w_v\). Therefore
\[
\sum_v w_v = \sum_e 3b_e = 3B.\qedhere
\]

**Lemma 2 (Boundary isotropic profile). PROVEN**

At the critical boundary
\[
B_*:=\frac{75}{119},
\]
the uniform profile
\[
b_e=\frac{B_*}{10}=\frac{15}{238}\qquad(\forall e)
\]
is feasible and satisfies:

1. Every disjoint-pair sum is strictly subcritical:
\[
b_e+b_f=\frac{15}{119}<\frac14.
\]

2. Every vertex bad degree is identical:
\[
d_i=\sum_{e\ni i} b_e = 4\cdot \frac{15}{238}=\frac{30}{119}.
\]

3. Every chamber load is identical:
\[
w_v=6\cdot \frac{15}{238}=\frac{45}{119}.
\]

4. Every opposite-pair sum in every chamber is identical:
\[
u_{v,1}=u_{v,2}=u_{v,3}=\frac{15}{119}.
\]

**Proof.**
Each label has the same mass, so every disjoint pair has sum `2·15/238=15/119<1/4`. Each vertex of `K_5` is incident to `4` labels, giving
\[
d_i=4\cdot \frac{15}{238}=\frac{30}{119}.
\]
Each chamber contains `6` labels, so
\[
w_v=6\cdot \frac{15}{238}=\frac{45}{119}.
\]
Each chamber has `3` opposite pairs, each pair consisting of `2` labels, hence
\[
u_{v,j}=2\cdot \frac{15}{238}=\frac{15}{119}.
\]
All statements follow. \(\qed\)

**Lemma 3 (This profile evades all currently verified coarse triggers). PROVEN**

For the uniform boundary profile of Lemma 2:

1. The disjoint-pair trigger `[P10]` does not fire:
\[
\frac{15}{119}<\frac14.
\]

2. The `K_4`-slice trigger `[P12]` does not fire:
\[
w_v=\frac{45}{119}<\frac{969}{1972}.
\]

3. The uncoupled chamber LP value is still below target:
\[
p_i\ge \frac{185-32B_*}{425}
=\frac{3923}{10115}
\approx 0.38783984<0.39.
\]

**Proof.**
Items `1` and `3` are immediate from Lemma 2 and the verified LP formula from the ledger. For item `2`,
\[
\frac{45}{119}\approx 0.37815126,\qquad \frac{969}{1972}\approx 0.49137931,
\]
so the verified `K_4` threshold is far from reached. \(\qed\)

**Lemma 4 (Odd-cycle escape can be completely isotropic). PROVEN**

Let `\mathcal H` be the `12` Hamilton `5`-cycles of `K_5`. For each `H∈\mathcal H`, define the cycle half-atom
\[
c^{H}_e=
\begin{cases}
\frac18,& e\in H,\\
0,& e\notin H.
\end{cases}
\]
Then
\[
\frac1{12}\sum_{H\in\mathcal H} c^H_e=\frac1{16}\qquad(\forall e).
\]
Hence the boundary-uniform profile decomposes as
\[
\frac{15}{238}=\frac1{16}+\frac1{1904}.
\]

**Proof.**
There are `12` Hamilton `5`-cycles in `K_5`. By edge-transitivity, each of the `10` labels lies in the same number `m` of such cycles. Counting edge-cycle incidences gives
\[
12\cdot 5 = 10m,
\]
so `m=6`. Therefore
\[
\frac1{12}\sum_{H\in\mathcal H} c^H_e
= \frac1{12}\cdot 6\cdot \frac18
=\frac1{16}.
\]
Finally,
\[
\frac{15}{238}-\frac1{16}
=\frac{120-119}{1904}
=\frac1{1904}.
\]
So the move from total mass `5/8` to total mass `75/119` can be realized by adding the completely isotropic increment `1/1904` to every label. \(\qed\)

**Consequence. DEAD**

The following label-graph program is exhausted:

- use only the degree vector \((d_i)\),
- the chamber-load vector \((w_v)\),
- the disjoint-pair caps \(b_e+b_f<1/4\),
- and the verified one-chamber floors `[P14]`, `[P15]`, `[P12]`.

The uniform boundary profile from Lemmas `2`-`4` defeats all such coarse summaries simultaneously. In particular, merely “leaving the Petersen `C_5` face” does not force any localization: the escape above `5/8` can be made perfectly isotropic.

`OPEN`

A successful proof must detect information finer than total chamber mass. In label-graph language, the missing bridge is:

- either a nonlinear rule that upgrades a chamber from the three subcritical hits
\[
u_{v,1}=u_{v,2}=u_{v,3}=\frac{15}{119},
\]
- or a cross-chamber alignment rule showing that many subcritical opposite pairs must cohere around one vertex strongly enough to activate a `1/2`-type or star-type bound.

The residual obstruction is therefore not chamber mass itself, but the lack of a proved transfer from a distributed pattern of many subcritical opposite-pair hits to a vertex-sensitive frequency gain above `0.39`.

#### The Calculator (codex/gpt-5.4)

`TRANSLATED`

I model the residual as a symmetric optimization problem on the `10` edge masses of `K_5`.

- Variables: `b_e >= 0` for `e in E(K_5)`, with `sum_e b_e = B`.
- Feasibility caps: `b_e + b_f <= 1/4` for every disjoint pair `e ⟂ f`.
- Vertex bad loads: `d_i = sum_{e∋i} b_e`.
- Chamber loads on `U_v = [5]\setminus{v}`:
  `w_v = sum_{e⊂U_v} b_e`.
- Opposite-pair loads inside chamber `U_v`:
  `u_{v,1}, u_{v,2}, u_{v,3}`, with `w_v = u_{v,1}+u_{v,2}+u_{v,3}`.

The forced-good-fiber lower envelope is the chamber vector
\[
q^{(v)}_i=
\begin{cases}
9/17,& i\in U_v,\\
1/17,& i=v,
\end{cases}
\]
coming from `[P15]` on the `4` support vertices and `[P14]` on the omitted vertex.

Its coordinate sum is
\[
4\cdot \frac{9}{17}+\frac{1}{17}=\frac{37}{17},
\]
so its mean per vertex is
\[
\frac1{5}\cdot\frac{37}{17}=\frac{37}{85}.
\]

`PROVEN`

The chamber vector above is the exact minimum frequency profile for a single forced `4`-support good fiber under the currently verified local rules.

- Support vertices: `>= 9/17`.
- Omitted vertex: `>= 1/17`.
- Mean vertex frequency in such a fiber: `37/85`.

This is the quantity the LP can use chamber-by-chamber.

`PROVEN`

For the `10`-variable symmetric LP, the isotropic profile is feasible at the boundary `B=75/119`:
\[
b_e=\frac{B}{10}=\frac{15}{238}\qquad(\forall e).
\]

Then:
\[
b_e+b_f=\frac{15}{119}<\frac14
\]
for every disjoint pair, so all pair caps are satisfied.

Also:
\[
d_i=4\cdot \frac{15}{238}=\frac{30}{119},
\qquad
w_v=6\cdot \frac{15}{238}=\frac{45}{119},
\qquad
u_{v,1}=u_{v,2}=u_{v,3}=2\cdot \frac{15}{238}=\frac{15}{119}.
\]

So the boundary profile is completely isotropic at the level of edge masses, vertex degrees, chamber loads, and opposite-pair loads.

`PROVEN`

Under any uncoupled linear transfer that depends only on these symmetric chamber-local inputs, group averaging forces the optimizer onto the `S_5`-fixed subspace. Therefore the boundary test reduces to this isotropic point.

At that point, the verified uncoupled LP value is
\[
p_i=\frac{185-32B}{425}.
\]
At `B=75/119`,
\[
p_i
=
\frac{185-32\cdot 75/119}{425}
=
\frac{3923}{10115}
\approx 0.3878398428.
\]

This is strictly below `0.39`.

`DEAD`

The proposed `10`-variable LP on `K_5` edge masses, using only:

- the disjoint-pair caps,
- the bad-degree transfer `[P6]`,
- the chamber-local forced-good floors `[P14]` and `[P15]`,
- and any symmetric uncoupled aggregation of those local data,

does **not** close the gap. Its exact boundary value is
\[
\frac{3923}{10115}<0.39.
\]

So this LP cannot prove the conjectured `0.39` threshold.

`PROVEN`

The boundary profile also evades the currently verified coarse triggers:

- Disjoint-pair trigger `[P10]`: `15/119 < 1/4`.
- `K_4`-slice trigger `[P12]`: `45/119 < 969/1972`.
- High-`B` trigger `[P9]`: boundary only, not strict improvement below it.

`OPEN`

What is missing is a **coupled** rule, not another uncoupled chamber LP. The obstruction is structural:

- every chamber is loaded subcritically but equally,
- every opposite pair is subcritical but equally,
- every vertex degree is equal,
- and the LP sees no alignment signal.

So the next bridge must detect one of these two genuinely nonlocal effects:

1. Several subcritical opposite-pair hits in different chambers cohering around one vertex strongly enough to upgrade that vertex beyond the `9/17` single-chamber floor.
2. A cross-chamber compatibility rule showing that the isotropic boundary pattern cannot be realized without creating either a `[P16]` two-cube span or a star concentration already handled by `[P17]`.

At the LP level alone, the residual obstruction survives.

#### The Explorer (codex/gpt-5.4)

`TRANSLATED`

Let `K_5` carry the bad label masses `b_e` on its `10` edges. For each omitted vertex `v`, the chamber `U_v=[5]\setminus{v}` is a `K_4`, and its `3` opposite pairs are exactly the `3` perfect matchings of that `K_4`. Write their loads as
\[
u_{v,1},u_{v,2},u_{v,3}.
\]

So the residual problem is a weighted compatibility problem between the `5` chambers, each chamber offering `3` local matchings.

The key foreign-domain object is the `5`-partite compatibility hypergraph `\mathcal T`:

- part `v` consists of the `3` perfect matchings of `U_v`,
- a transversal `(m_v)_{v=1}^5` is admissible iff the chosen `5` opposite pairs come from one Hamilton `5`-cycle of `K_5`.

There are exactly `12` admissible transversals, one for each Hamilton cycle.

---

`PROVEN`

If the bad degree vector is uniform,
\[
d_1=\cdots=d_5=\frac{2B}{5},
\]
then the bad mass is a convex combination of Hamilton `5`-cycles of `K_5`.

**Proof.**
Set
\[
x_e:=\frac{5}{B}b_e.
\]
Then `x_e\ge 0` and for every vertex `i`,
\[
\sum_{e\ni i} x_e=\frac{5}{B}d_i=2.
\]
Thus `x` is a fractional `2`-factor of `K_5`. By the `2`-factor polytope theorem, every fractional `2`-factor is a convex combination of integral `2`-factors. On `5` vertices, the only integral `2`-factors are Hamilton `5`-cycles. Hence
\[
x=\sum_H \lambda_H\,1_H,\qquad \lambda_H\ge 0,\quad \sum_H\lambda_H=1,
\]
so
\[
b=\frac{B}{5}\sum_H \lambda_H\,1_H.
\]
This is exactly a Hamilton-cycle decomposition of the bad mass. `\square`

Interpretation: the equal-degree face is not “random edge mass”; it is the Hamilton-cycle polytope.

---

`PROVEN`

Each Hamilton cycle chooses exactly one opposite pair in each chamber.

**Proof.**
Fix a Hamilton cycle `H` and a chamber `U_v`. The restriction `H[U_v]` has `4` vertices and `2` edges, because deleting `v` from a `5`-cycle leaves two disjoint edges plus one `3`-vertex path gap through `v`. Those two disjoint edges form one of the `3` perfect matchings of `U_v`. So `H` defines one local matching in every chamber. `\square`

Hence a Hamilton cycle is exactly an admissible transversal of `\mathcal T`.

---

`PROVEN`

The completely isotropic boundary profile is the barycenter of the transversal polytope `\mathrm{conv}(\mathcal T)`.

**Proof.**
Average uniformly over the `12` Hamilton cycles. Each edge of `K_5` lies in exactly `6` of them, so
\[
\frac1{12}\sum_H 1_H
\]
puts weight `1/2` on every edge. After scaling by `B/5`, this becomes the uniform bad profile `b_e=B/10`. At chamber level, each chamber matching is selected equally often, so
\[
u_{v,1}=u_{v,2}=u_{v,3}=\frac{B}{5}
\]
for every chamber `v`. `\square`

So the residual obstruction is structurally a barycenter of compatible global transversals, not merely an LP artifact.

---

`DEAD`

Any argument that only reads:

- the degree vector `(d_i)`,
- the chamber totals `(w_v)`,
- or even the chamber-local matching marginals `(u_{v,1},u_{v,2},u_{v,3})`

but does **not** use cross-chamber compatibility of the chosen matchings cannot distinguish the barycenter above from a genuine extremizer. In matching language: one-part marginals on the `5` chamber parts are too coarse; the obstruction lives in the coupling inside the transversal polytope.

This is the graph-theoretic reason the uncoupled LP dies.

---

`OPEN`

A viable foreign-domain bridge now looks like this:

1. Define a chamber-certificate hypergraph whose vertices are the `15` local opposite pairs and whose hyperedges are the `12` admissible Hamilton transversals.
2. Prove a Hall-type strengthening: a weighting near the barycenter of `\mathrm{conv}(\mathcal T)` cannot be realized by actual union-closed fibers unless many certificates reuse a common support vertex.
3. Convert that reuse into a vertex-frequency gain, ideally a forced `P16`-type span or an improved omitted-chamber deficit `G_x < C/5`.

So the next nontrivial invariant is not a chamber total. It is a **cross-chamber compatibility defect** inside the Hamilton-transversal hypergraph.

### BUILD — Pass 3

#### The Proof Writer (codex/gpt-5.4)

`TRANSLATED`

I isolate the exact LP class that the last two contributions are using.

Let `b=(b_e)_{e\in E(K_5)}` be the bad-label mass, with
\[
b_e\ge 0,\qquad \sum_e b_e=B,\qquad b_e+b_f\le \frac14\ \text{ for } e\perp f.
\]
For each vertex `x`, define the bad degree
\[
d_x:=\sum_{e\ni x} b_e.
\]
For each chamber `v`, let `g_v\ge 0` denote the total good mass that is certified through the omitted-vertex chamber `U_v=[5]\setminus\{v\}`. Thus
\[
\sum_{v=1}^5 g_v=1-B.
\]

The local chamber certificate from `[P14]` and `[P15]` is the vector
\[
q^{(v)}_x=
\begin{cases}
9/17,& x\neq v,\\
1/17,& x=v.
\end{cases}
\]
So a one-chamber marginal certificate gives vertex `x` the lower bound
\[
p_x\ \ge\ \frac B5+\frac25 d_x+\sum_{v=1}^5 g_v\,q^{(v)}_x.
\]

That is the foreign-domain LP reformulation.

---

`PROVEN`

**Lemma 1.** For every vertex `x`,
\[
p_x\ge \frac B5+\frac25 d_x+\frac{1-B}{17}+\frac8{17}\sum_{v\neq x} g_v.
\]

**Proof.**
By the chamber certificate,
\[
\sum_v g_v q^{(v)}_x
=
g_x\cdot \frac1{17}+\sum_{v\neq x} g_v\cdot \frac9{17}.
\]
Using `\sum_v g_v=1-B`,
\[
g_x\cdot \frac1{17}+\sum_{v\neq x} g_v\cdot \frac9{17}
=
\frac{1-B}{17}+\frac8{17}\sum_{v\neq x} g_v.
\]
Substitute into the translated LP bound. `\square`

---

`PROVEN`

**Lemma 2.** Assume the bad profile is isotropic:
\[
d_x=\frac{2B}{5}\qquad (x=1,\dots,5).
\]
Then among all chamber allocations `g_v\ge 0` with `\sum_v g_v=1-B`, the minimum possible value of `\max_x p_x` in Lemma 1 is attained at the balanced allocation
\[
g_1=\cdots=g_5=\frac{1-B}{5}.
\]

**Proof.**
Under `d_x=2B/5`, Lemma 1 becomes
\[
p_x\ge \frac{9B}{25}+\frac{1-B}{17}+\frac8{17}h_x,
\qquad
h_x:=\sum_{v\neq x} g_v.
\]
Hence minimizing `\max_x p_x` is equivalent to minimizing `\max_x h_x`.

Now
\[
\sum_{x=1}^5 h_x
=
\sum_{x=1}^5\sum_{v\neq x} g_v
=
4\sum_{v=1}^5 g_v
=
4(1-B).
\]
Therefore
\[
\max_x h_x\ge \frac15\sum_x h_x=\frac{4(1-B)}5.
\]
Equality holds iff all `h_x` are equal, which is equivalent to all `g_v` being equal. Thus the optimizer is
\[
g_v=\frac{1-B}{5}\quad \forall v.
\]
`\square`

---

`PROVEN`

**Lemma 3.** On the isotropic bad profile with balanced chamber allocation,
\[
p_x\ge \frac{185-32B}{425}\qquad \forall x.
\]

**Proof.**
Insert
\[
d_x=\frac{2B}{5},\qquad \sum_{v\neq x} g_v=\frac{4(1-B)}5
\]
into Lemma 1:
\[
p_x\ge \frac B5+\frac25\cdot \frac{2B}{5}+\frac{1-B}{17}+\frac8{17}\cdot \frac{4(1-B)}5.
\]
This is
\[
p_x\ge \frac{9B}{25}+\frac{1-B}{17}+\frac{32(1-B)}{85}
= \frac{9B}{25}+\frac{37(1-B)}{85}
= \frac{185-32B}{425}.
\]
`\square`

---

`PROVEN`

**Lemma 4.** The one-chamber marginal LP cannot prove `p_x\ge 0.39` throughout the residual range. Its exact threshold is
\[
\frac{185-32B}{425}\ge \frac{39}{100}
\iff
B\le \frac{77}{128}=0.6015625.
\]

**Proof.**
Solve
\[
\frac{185-32B}{425}\ge \frac{39}{100}.
\]
Multiplying by `1700` gives
\[
740-128B\ge 663,
\]
hence
\[
128B\le 77,\qquad B\le \frac{77}{128}.
\]
At the boundary point from `[P9]`,
\[
B=\frac{75}{119},
\]
Lemma 3 gives
\[
p_x\ge \frac{185-32\cdot 75/119}{425}
=\frac{3923}{10115}
\approx 0.3878398428<0.39.
\]
So this LP family fails on the interval
\[
\frac{77}{128}<B\le \frac{75}{119}.
\]
`\square`

---

`DEAD`

Any certificate using only:

1. the bad-degree term `[P6]`,
2. the universal floor `[P14]`,
3. the single-chamber `4`-support upgrade `[P15]`,
4. and a decomposition of good mass into chamber marginals `(g_v)`,

cannot close the residual regime. The exact obstruction window is
\[
0.6015625<B\le 0.6302521\ldots
\]
and the isotropic Hamilton-barycenter profile survives there.

This sharpens the previous no-go statement: the failure is not merely at `B=75/119`; it is an entire interval.

---

`OPEN`

The next LP bridge must be genuinely **coupled across chambers**. A sufficient breakthrough would be any linear or convex constraint that forbids the simultaneous saturation of:

- equal bad degrees `d_x=2B/5`,
- equal chamber allocations `g_v=(1-B)/5`,
- and the Hamilton-barycenter compatibility pattern.

In the label-graph language, one must force a positive defect from the transversal polytope itself, not from its one-part marginals.

#### The Calculator (codex/gpt-5.4)

`TRANSLATED`

Fix `B in [0,75/119]`. The LP class suggested in the prompt is:

Variables:
- `b_e >= 0` for the `10` edges `e` of `K_5`
- `g_v >= 0` for the `5` omitted-vertex chambers `v`
- `t` = target upper envelope for vertex frequencies

Constraints:
- `sum_e b_e = B`
- `sum_v g_v = 1-B`
- `b_e + b_f <= 1/4` for every disjoint edge pair `e ⟂ f`

Derived loads:
- `d_x := sum_{e∋x} b_e`

Forced-good-fiber certificate:
- for chamber `v`, the minimal certified profile is
  - `q_x^(v) = 9/17` if `x != v`
  - `q_x^(v) = 1/17` if `x = v`

So the LP lower bound on each vertex is
\[
p_x \ge \frac B5+\frac25 d_x+\sum_v g_v q_x^{(v)}
= \frac B5+\frac25 d_x+\frac{9}{17}(1-B)-\frac{8}{17}g_x.
\]
Therefore the adversary LP is
\[
\min t
\quad\text{s.t.}\quad
t \ge \frac B5+\frac25 d_x+\frac{9}{17}(1-B)-\frac{8}{17}g_x
\quad(\forall x).
\]

`PROVEN`

This LP is solved exactly. Its optimum value is
\[
t^*(B)=\frac{185-32B}{425}.
\]

Proof in optimization language:

1. For any feasible point, average the `5` vertex constraints:
\[
\frac15\sum_x p_x
\ge
\frac B5+\frac25\cdot \frac{1}{5}\sum_x d_x+\frac{9}{17}(1-B)-\frac{8}{17}\cdot \frac{1}{5}\sum_x g_x.
\]
Since `sum_x d_x = 2B` and `sum_x g_x = 1-B`,
\[
\max_x p_x \ge \frac15\sum_x p_x
\ge \frac B5+\frac{4B}{25}+\frac{9}{17}(1-B)-\frac{8}{85}(1-B)
= \frac{185-32B}{425}.
\]

2. Equality is attained by the symmetric feasible point
- `b_e = B/10` for every edge
- `g_v = (1-B)/5` for every chamber

because then
- `d_x = 4(B/10) = 2B/5` for every vertex
- every disjoint-pair constraint is `2(B/10)=B/5 <= 1/4` throughout the residual regime

and each vertex has the same value
\[
p_x =
\frac B5+\frac25\cdot\frac{2B}{5}+\frac{9}{17}(1-B)-\frac{8}{17}\cdot\frac{1-B}{5}
=
\frac{185-32B}{425}.
\]

So this is the exact optimum.

`PROVEN`

Minimum certified vertex frequencies inside the forced good fibers are:

- Universal good fiber floor: `1/17`
- One forced `4`-support chamber fiber: support vertices `9/17`, omitted vertex `1/17`
- Two-cube span through the same vertex: that vertex `>= 1/2`

These are the exact chamber-side coefficients available to this LP class.

`PROVEN`

Boundary test at `B = 75/119`:

\[
t^*\!\left(\frac{75}{119}\right)
=
\frac{185-32\cdot 75/119}{425}
=
\frac{3923}{10115}
\approx 0.3878398418.
\]

Target gap:
\[
0.39-\frac{3923}{10115}
=
\frac{437}{202300}
\approx 0.0021601582.
\]

So the LP minimum is strictly below `0.39`.

At the optimizer:
- `b_e = 15/238 ≈ 0.0630252`
- `d_x = 30/119 ≈ 0.2521008`
- `g_v = 44/595 ≈ 0.0739496`

Per vertex:
- bad part `= B/5 + (2/5)d_x = 27/119 ≈ 0.2268908`
- good part `= 37(1-B)/85 = 1628/10115 ≈ 0.1609491`
- total `= 3923/10115 ≈ 0.3878398`

`DEAD`

The full LP class
- `10` bad-edge masses `b_e`,
- total good-mass split `g_v`,
- chamber certificates `9/17` on support and `1/17` off support,
- and disjoint-pair cap `b_e+b_f<1/4`

does **not** prove `max_x p_x >= 0.39` in the residual regime.

Its exact success threshold is
\[
\frac{185-32B}{425}\ge \frac{39}{100}
\iff
B\le \frac{77}{128}=0.6015625.
\]

Hence it fails on the entire interval
\[
\frac{77}{128} < B \le \frac{75}{119}.
\]

`OPEN`

Any successful next LP must add a genuinely new coupling constraint beyond this chamber-marginal model. In optimization terms, the symmetric point
- `b_e=B/10`,
- `g_v=(1-B)/5`

is an exact extremizer for the current relaxation, so the missing ingredient must cut off that isotropic feasible point. The needed bridge is a cross-chamber compatibility inequality, not another refinement of degree totals or one-chamber marginals.

#### The Explorer (codex/gpt-5.4)

`TRANSLATED`

Model the bad-label profile as a weighted graph `w` on `K_5`, with edge weights `w_e = B_e`.  
For each omitted vertex `v`, the chamber `U_v = [5]\setminus{v}` contains exactly the `3` perfect matchings of `K_4 = K_5[U_v]`.  
Define the chamber forcing load
\[
m_v := \sum_{\{e,f\}\text{ a matching in }K_5[U_v]} \min(w_e,w_f).
\]
This is a purely matching-theoretic object: it is the total amount of bad mass that can be paired into disjoint bad edges inside chamber `v`.

Each unit counted by `m_v` forces one `U_v`-good fiber. If two different matchings in the same chamber share a bad edge, then that edge is incident to all three support vertices of some `x\in U_v`, so in chamber language this is exactly a `two-cube span` through `x`, hence [P16] upgrades that chamber contribution from the `9/17` profile to a `1/2` profile at `x`.

So the real missing variable is not the chamber mass `g_v`, but the **matching congestion**
\[
c_v(x):=\text{amount of chamber-}v\text{ forcing routed through two matchings containing a common edge at }x.
\]

`PROVEN`

For every chamber `v`,
\[
m_v \le \frac12\, d(U_v),
\qquad
d(U_v):=\sum_{e\subset U_v} w_e,
\]
with equality at the isotropic point `w_e=B/10`.

Proof: `K_4` has `3` perfect matchings, every edge lies in exactly one of them, and
\[
\min(a,b)\le \frac{a+b}{2}.
\]
Summing over the `3` matchings gives the claim.

This shows why the symmetric point survives every relaxation that only sees total chamber load: it maximizes matching load uniformly in all chambers.

`PROVEN`

At the isotropic point `w_e=B/10`, every chamber has
\[
m_v = 3\cdot \frac{B}{10}.
\]
Moreover every support vertex `x` in that chamber lies in exactly `2` of the `3` matchings, so the chamber has unavoidable overlap multiplicity
\[
\sum_{x\in U_v}\deg_{\mathcal M_v}(x)=6,
\]
where `\mathcal M_v` is the 3-edge matching hypergraph of the chamber. This is the structural source of possible [P16]-type upgrades: isotropy does not destroy overlap; it maximizes symmetric overlap.

`DEAD`

Any argument that compresses chamber data to only the scalar marginals `g_v` throws away exactly the overlap multiplicity that distinguishes a lone `9/17` chamber from a `1/2` two-cube-span chamber. In matching language, the dead relaxation forgets whether the forced load in `U_v` comes from one matching or from several intersecting matchings. That is why the isotropic barycenter remains feasible.

`OPEN`

A viable next bridge is a Hall-type routing statement inside each chamber:

If a chamber `U_v` carries forcing load `m_v`, then either

1. that load can be assigned injectively to distinct support vertices, giving the old `9/17` certificate, or  
2. Hall fails, which means some vertex `x\in U_v` is hit by forcing from at least two chamber matchings, and then [P16] yields a `1/2` chamber certificate at `x`.

What remains is to make this quantitative: prove a lower bound of the form
\[
\sum_v c_v(x) \ge \Phi(w)-\Psi((g_v)),
\]
with `\Phi(w)>0` at the isotropic point. That would be the cross-chamber inequality that cuts off the exact optimizer found by the last LP.

So the foreign-domain conclusion is:

- `TRANSLATED`: the residual problem is a weighted matching-congestion problem on the `5` chambers `K_4`.
- `PROVEN`: chamber marginals alone are insufficient because they ignore matching overlap, and the isotropic point actually maximizes symmetric overlap.
- `OPEN`: derive a Hall/congestion inequality converting overlap of chamber matchings into mandatory `1/2`-type vertex bonuses. That is the structural bridge the current LP is missing.

### VERIFY

#### The Proof Writer (codex/gpt-5.4)

`PROVEN`

1. **Exact solution of the uncoupled chamber-marginal LP.**  
The pass-3 LP identity
\[
t^*(B)=\frac{185-32B}{425}
\]
is correct.

Proof sketch: averaging the five vertex constraints gives the lower bound
\[
\max_x p_x\ge \frac15\sum_x p_x
\ge \frac B5+\frac25\cdot \frac{2B}{5}+\frac{9}{17}(1-B)-\frac{8}{17}\cdot \frac{1-B}{5}
=\frac{185-32B}{425}.
\]
Equality is attained by the symmetric feasible point
\[
b_e=\frac{B}{10}\quad(\forall e),\qquad g_v=\frac{1-B}{5}\quad(\forall v),
\]
since then
\[
d_x=\frac{2B}{5},\qquad b_e+b_f=\frac{B}{5}\le \frac14
\]
throughout the residual regime. So the LP optimum is exact.

Tag as PROVEN: any relaxation using only `b_e`, chamber marginals `g_v`, the bad-degree term `[P6]`, and the one-chamber coefficients `1/17,9/17` has exact value \((185-32B)/425\).

2. **Failure interval of that LP class.**  
Solving
\[
\frac{185-32B}{425}\ge \frac{39}{100}
\]
gives
\[
B\le \frac{77}{128}=0.6015625.
\]
Hence the LP fails on
\[
\frac{77}{128}<B\le \frac{75}{119}.
\]
At the boundary \(B=75/119\),
\[
\frac{185-32B}{425}=\frac{3923}{10115}\approx 0.38783984<0.39.
\]

Tag as PROVEN: the uncoupled chamber-marginal LP is dead on the whole interval \((77/128,75/119]\).

3. **Symmetric optimizer / isotropic obstruction.**  
The uniform profile
\[
b_e=\frac{B}{10}
\]
indeed has
\[
d_x=\frac{2B}{5},\qquad w_v=\frac{3B}{5},
\qquad u_{v,1}=u_{v,2}=u_{v,3}=\frac{B}{5},
\]
so it defeats every certificate depending only on these one-part marginals.

Tag as PROVEN: the isotropic Hamilton-barycenter profile survives every currently verified uncoupled chamber LP.

`DEAD`

1. **Matching-congestion as currently formulated.**  
The Explorer’s new object
\[
m_v=\sum_{\text{matchings } \{e,f\}\subset K_5[U_v]}\min(w_e,w_f)
\]
is a legitimate weighted matching statistic, but the key transfer claim is unsupported:

> “Each unit counted by \(m_v\) forces one \(U_v\)-good fiber.”

No proof was given that a weighted overlap quantity in the label graph corresponds to distinct actual forced fibers in the union-closed family. The map from chamber matching weight to good-fiber mass is exactly the missing step, and it is not implied by `[P5]` or `[P7]`.

Worse, the follow-up claim

> overlap of two chamber matchings sharing an edge gives a `[P16]` two-cube span

also has a gap: two positive minima in two matchings do not produce the required common-fiber geometric configuration. They only show that one bad edge participates in two local weighted pairings, not that one fiber is simultaneously `A_z`-good in the sense needed for `[P7]` / the two-cube upgrade.

Tag as DEAD: the current matching-congestion route, because it assumes an unproved transfer from weighted chamber matchings to actual good-fiber certificates.

`OPEN`

1. **What exact coupling constraint cuts off the isotropic optimizer?**  
A successful next step must produce a valid inequality not expressible in terms of only
\[
(d_x),\quad (g_v),\quad (w_v),\quad (u_{v,j}).
\]
Concretely: what additional certified quantity, derived from actual fiber overlap and not just label-graph marginals, is strictly positive at the isotropic profile and yields a vertex gain exceeding
\[
0.39-\frac{3923}{10115}=\frac{437}{202300}\approx 0.00216016?
\]

2. **Can one extract a true cross-chamber overlap certificate from `[P7]`?**  
What is needed is a rigorous statement of the form: if bad mass is distributed near the isotropic profile across many chambers, then a positive proportion of good fibers are simultaneously `A_z`-good for at least two distinct `z`, with an explicit constant strong enough to beat the LP gap.

Tag as OPEN: derive a genuine fiber-level multi-slice overlap lower bound that is invisible to chamber marginals.

**Overall Assessment**

FATAL FLAW: `yes`

What is specifically wrong: the only genuinely new ingredient in the latest round, the matching-congestion proposal, does not connect weighted local matchings to actual forced good fibers or to a valid `[P7]` / two-cube upgrade. The exact uncoupled LP analysis is correct, but it proves that whole chamber-marginal program is exhausted.

What new direction should be explored next: not another LP on label masses or chamber statistics. The next iteration should attack a different object entirely: a **fiber-incidence bipartite graph** between bad labels and good fibers, and prove a global overlap theorem there. The target is a counting lemma that lower-bounds the mass of genuinely multi-slice good fibers from the isotropic bad profile, with explicit constants. That is a different domain object than the current chamber LP and does not try to repair it.

PROGRESS RATING: `7/10`

#### The Calculator (codex/gpt-5.4)

`TRANSLATED`

I treat the residual problem purely as a small optimization system on the `10` edge-mass variables `b_e` of `K_5`, with auxiliary chamber-marginal variables `g_v`. The recent claims split into two groups:

1. exact statements about the uncoupled LP;
2. a proposed new matching-congestion transfer.

Only the first group is fully verified.

`PROVEN`

1. **Exact optimum of the uncoupled chamber-marginal LP.**  
The LP
\[
t\ge \frac B5+\frac25 d_x+\sum_v g_v q_x^{(v)},
\qquad
q_x^{(v)}=
\begin{cases}
9/17,&x\neq v,\\
1/17,&x=v,
\end{cases}
\]
with
\[
\sum_e b_e=B,\quad \sum_v g_v=1-B,\quad d_x=\sum_{e\ni x}b_e,
\]
has exact optimum
\[
t^*(B)=\frac{185-32B}{425}.
\]

Justification: averaging the `5` vertex constraints gives
\[
\max_x p_x\ge \frac15\sum_x p_x
\ge \frac B5+\frac25\cdot \frac{2B}{5}+\frac{9}{17}(1-B)-\frac{8}{17}\cdot \frac{1-B}{5}
=\frac{185-32B}{425}.
\]
Equality is attained by the symmetric feasible point
\[
b_e=\frac{B}{10}\quad(\forall e),\qquad g_v=\frac{1-B}{5}\quad(\forall v),
\]
since then
\[
d_x=\frac{2B}{5}
\]
for all `x`, and every vertex constraint is tight at the same value.  
Tag as PROVEN: any relaxation using only `b_e`, chamber marginals `g_v`, the bad-degree term `[P6]`, and the one-chamber coefficients `1/17,9/17` has exact value \((185-32B)/425\).

2. **Failure interval of that LP class.**  
Solving
\[
\frac{185-32B}{425}\ge \frac{39}{100}
\]
gives
\[
B\le \frac{77}{128}=0.6015625.
\]
Hence this LP class fails throughout
\[
\frac{77}{128}<B\le \frac{75}{119}.
\]
At the boundary
\[
B=\frac{75}{119},
\qquad
t^*(B)=\frac{3923}{10115}\approx 0.38783984<0.39.
\]
Tag as PROVEN: the uncoupled chamber-marginal LP is dead on the whole interval \((77/128,75/119]\).

3. **Isotropic optimizer survives all currently verified uncoupled data.**  
For
\[
b_e=\frac{B}{10},
\]
one has
\[
d_x=\frac{2B}{5},\qquad
w_v=\frac{3B}{5},\qquad
u_{v,1}=u_{v,2}=u_{v,3}=\frac{B}{5}.
\]
So every one-part statistic currently in play is perfectly balanced.  
Tag as PROVEN: the isotropic Hamilton-barycenter profile survives every currently verified uncoupled chamber LP.

`DEAD`

1. **Matching-congestion transfer as currently stated.**  
The quantity
\[
m_v=\sum_{\text{matchings }\{e,f\}\subset K_5[U_v]}\min(w_e,w_f)
\]
is a valid weighted matching statistic, but the crucial transfer claim is unproved:

> each unit counted by `m_v` forces one `U_v`-good fiber.

Nothing supplied shows that weighted pairing mass in a chamber corresponds to distinct actual good-fiber mass in the family. That bridge is exactly the missing step.

2. **Overlap of two local matchings does not by itself yield a `[P7]` / two-cube upgrade.**  
Two positive minima on two chamber matchings sharing an edge only imply reuse of bad-edge weight inside a chamber statistic. They do **not** imply existence of a single fiber simultaneously good in the required multiple-slice sense. The optimization object and the fiber object are not yet linked.

Tag as DEAD: the current matching-congestion route, because it assumes an unproved transfer from weighted chamber matchings to actual good-fiber certificates.

`OPEN`

1. **Need a genuine coupled certificate beyond chamber marginals.**  
What valid extra inequality cuts off the isotropic point
\[
b_e=\frac{B}{10},\qquad g_v=\frac{1-B}{5}
\]
and yields gain at least
\[
0.39-\frac{3923}{10115}=\frac{437}{202300}\approx 0.00216016?
\]
This must depend on a coupled object, not only on `(d_x)`, `(g_v)`, `(w_v)`, or `(u_{v,j})`.

2. **Need a fiber-level overlap lower bound.**  
A useful next theorem would be: near-isotropic bad mass forces a positive amount of genuinely multi-slice good fibers, with an explicit constant strong enough to beat the `437/202300` gap. That is not yet proved.

Tag as OPEN: derive a valid cross-fiber incidence inequality that converts near-isotropic bad mass into certified multi-slice good mass.

**Overall Assessment**

FATAL FLAW: `yes`

What is specifically wrong: the only new ingredient beyond the exhausted uncoupled LP, namely matching congestion, never establishes a valid map from weighted chamber matchings to actual good fibers or to `[P7]`-type overlap certificates. So the proposed improvement is not yet an admissible bound.

What NEW direction should be explored next: abandon chamber-local weighted matching summaries and move to a **fiber-incidence transport model**. Build a bipartite flow object between bad labels and actual good fibers, then prove a global overlap/expansion theorem in that transport graph. The target is a certified lower bound on multi-slice good mass that is invisible to all current marginals.

PROGRESS RATING: `7/10`

#### The Explorer (codex/gpt-5.4)

`TRANSLATED`

I audit the recent round in the graph/matching model on the weighted label graph `K_5`, with edge weights `b_e`, chamber matchings in the `5` induced `K_4`’s, and the chamber-marginal good masses `g_v`.

`CORRECT`

1. **Exact optimizer of the uncoupled chamber-marginal LP.**  
Averaging the five vertex lower bounds is valid because
\[
\sum_x d_x=2B,\qquad \sum_x g_x=1-B.
\]
This gives
\[
\max_x p_x\ge \frac15\sum_x p_x\ge \frac{185-32B}{425}.
\]
The symmetric point
\[
b_e=\frac B{10}\quad(\forall e),\qquad g_v=\frac{1-B}{5}\quad(\forall v)
\]
attains equality, since then `d_x=2B/5` for every vertex and all five constraints are equalities.  
Tag as `PROVEN`: any relaxation using only `b_e`, chamber marginals `g_v`, the bad-degree term `[P6]`, and the one-chamber coefficients `1/17,9/17` has exact value \((185-32B)/425\).

2. **Failure interval of that LP class.**  
Solving
\[
\frac{185-32B}{425}\ge 0.39
\]
gives
\[
B\le \frac{77}{128}.
\]
Hence the relaxation fails on
\[
\left(\frac{77}{128},\frac{75}{119}\right].
\]
At `B=75/119` it gives
\[
\frac{3923}{10115}\approx 0.38783984<0.39.
\]
Tag as `PROVEN`: the uncoupled chamber-marginal LP is dead on the whole interval \((77/128,75/119]\).

3. **Symmetric obstruction survives all one-part marginals.**  
Under
\[
b_e=\frac B{10},
\]
the graph data are completely isotropic:
\[
d_x=\frac{2B}{5},\qquad w_v=\frac{3B}{5},\qquad u_{v,1}=u_{v,2}=u_{v,3}=\frac B5.
\]
So any certificate depending only on vertex degrees, chamber totals, or chamber matching marginals cannot separate this point from an extremizer.  
Tag as `PROVEN`: the isotropic Hamilton-barycenter profile survives every currently verified uncoupled chamber LP.

`WRONG`

1. **Matching-congestion transfer.**  
The statistic
\[
m_v=\sum_{\{e,f\}\text{ matching in }U_v}\min(b_e,b_f)
\]
is a legitimate matching quantity, but the claimed bridge
“each unit of `m_v` forces one `U_v`-good fiber”
has no proved realization map from weighted matchings to actual fiber mass. In matching language, `m_v` is only a fractional packing count inside the chamber; no injection into certified fibers was established.  
Tag as `DEAD`: the matching-congestion approach because weighted chamber matchings are not linked to actual good-fiber mass.

2. **Shared-edge local overlap implies a `[P7]`-type upgrade.**  
Two chamber matchings sharing an edge only show reuse inside the chamber matching system. That does not produce a single fiber that is simultaneously good in two slices, which is what `[P7]` needs. The combinatorial object is wrong: overlap of matching support is weaker than overlap of certified fibers.  
Tag as `DEAD`: the local-overlap-to-two-cube upgrade route because chamber overlap is not the same as fiber overlap.

`UNJUSTIFIED`

1. **Need a coupled graph object that lives at fiber level, not chamber-marginal level.**  
What is missing is a valid graph-theoretic transport theorem from near-isotropic bad edge mass to genuine multi-slice good-fiber overlap. Equivalently: construct a bipartite incidence graph between bad labels and certified good fibers, and prove an expansion/Hall-type lower bound forcing positive mass of fibers of overlap multiplicity at least `2`.  
Tag as `OPEN`: prove a fiber-incidence expansion theorem strong enough to beat the gap
\[
0.39-\frac{3923}{10115}=\frac{437}{202300}.
\]

2. **Need a compatibility obstruction beyond one-part marginals.**  
The current data record only marginals of the transversal system. What is needed is a valid inequality using actual compatibility among certificates, not just averaged chamber loads.  
Tag as `OPEN`: find a cross-fiber compatibility invariant that is strictly positive at the isotropic Hamilton-barycenter profile.

**Overall Assessment**

FATAL FLAW: `yes`

What is specifically wrong: the only new ingredient beyond the exhausted LP, namely chamber matching congestion, never establishes a rigorous transfer from weighted local matching structure to actual good fibers or to verified multi-slice overlap. So the proposed improvement is not an admissible bound.

What NEW direction should be explored next: abandon chamber-weight summaries entirely and move to a **fiber-incidence bipartite graph**. Left side: bad labels or bad fibers. Right side: actual good fibers, marked by the slice vertices they certify. Then prove a Hall/expansion theorem showing that near-isotropic bad mass forces many right-vertices of degree at least `2`, which would activate `[P7]` at the correct scale. This is structurally different from the dead chamber-LP program.

PROGRESS RATING: `7/10`

### SYNTHESIS

#### The Proof Writer (codex/gpt-5.4)

**1. THEOREM / RESULT STATEMENT**

`PROVEN` **The strongest rigorous result obtained in this workshop is a no-go theorem for the entire chamber-marginal / linear-transfer LP program in the residual `k=5` case.**

Let `M=[5]`. Let the bad-label masses be `b_e=B_e` on the `10` edges `e∈E(K_5)`, with
\[
b_e\ge 0,\qquad \sum_{e} b_e=B<\frac{75}{119},
\]
and write
\[
d_x:=\sum_{e\ni x} b_e \qquad (x\in[5]).
\]
For each omitted vertex `v`, let `g_v\ge 0` be the total good mass assigned to the `4`-support chamber `U_v=[5]\setminus\{v\}`, so
\[
\sum_{v=1}^5 g_v=1-B.
\]
Assume one only uses the verified local chamber profile
\[
q_x^{(v)}=
\begin{cases}
9/17,&x\neq v,\\
1/17,&x=v,
\end{cases}
\]
together with the bad-fiber formula `[P6]`. Then every such proof can only certify
\[
p_x\ge \frac B5+\frac25 d_x+\sum_{v=1}^5 g_v q_x^{(v)}.
\]

Define
\[
t^*(B):=\min \max_{x\in[5]}
\left(\frac B5+\frac25 d_x+\sum_{v=1}^5 g_v q_x^{(v)}\right)
\]
over all feasible `b,g`.

Then
\[
\boxed{t^*(B)=\frac{185-32B}{425}.}
\]

Hence this entire LP class proves `max_x p_x\ge 0.39` only when
\[
B\le \frac{77}{128}=0.6015625.
\]
In particular, at the critical residual boundary
\[
B=\frac{75}{119}\approx 0.6302521,
\]
it gives only
\[
\boxed{t^*\!\left(\frac{75}{119}\right)=\frac{3923}{10115}\approx 0.38783984<0.39.}
\]

So the chamber-marginal LP is `DEAD` on the full interval
\[
\left(\frac{77}{128},\frac{75}{119}\right].
\]

A second `PROVEN` structural result is:

`PROVEN` **Every `S_5`-equivariant linear transfer from bad-label mass space to vertex-load space collapses to degree plus total mass.**

If `E=\mathbb R^{E(K_5)}` and `V=\mathbb R^{[5]}`, then every `S_5`-equivariant linear map `T:E\to V` has the form
\[
(Tb)_x=\alpha\, d_x+\beta\, B
\]
for some constants `\alpha,\beta`. Thus every symmetric linear localization scheme reduces to the already exhausted degree LP.

---

**2. PROOF OR PROOF SKETCH**

`PROVEN` **Lemma 1.** For every vertex `x`,
\[
p_x\ge \frac B5+\frac25 d_x+\frac{1-B}{17}+\frac8{17}\sum_{v\neq x} g_v.
\]

**Proof.**
From the chamber profile,
\[
\sum_v g_v q_x^{(v)}
=
g_x\cdot \frac1{17}+\sum_{v\neq x} g_v\cdot \frac9{17}
=
\frac{1-B}{17}+\frac8{17}\sum_{v\neq x} g_v,
\]
since `\sum_v g_v=1-B`. Add the bad term `[P6]`. ∎

`PROVEN` **Lemma 2.** Averaging the five inequalities of Lemma 1 gives
\[
\max_x p_x\ge \frac{185-32B}{425}.
\]

**Proof.**
Average over `x`. Since
\[
\sum_x d_x=2B
\]
and each chamber `g_v` appears in exactly `4` of the sums `\sum_{v\neq x} g_v`,
\[
\frac15\sum_x \sum_{v\neq x} g_v = \frac45(1-B).
\]
Therefore
\[
\max_x p_x\ge \frac15\sum_x p_x
\ge \frac B5+\frac25\cdot \frac{2B}{5}+\frac{1-B}{17}+\frac8{17}\cdot \frac45(1-B).
\]
This simplifies to
\[
\frac{9B}{25}+\frac{37(1-B)}{85}
=\frac{185-32B}{425}.
\]
∎

`PROVEN` **Lemma 3.** Equality in Lemma 2 is attained by the isotropic profile
\[
b_e=\frac{B}{10}\quad(\forall e),\qquad g_v=\frac{1-B}{5}\quad(\forall v).
\]

**Proof.**
At this point,
\[
d_x=4\cdot \frac{B}{10}=\frac{2B}{5}\qquad(\forall x),
\]
and all disjoint-pair constraints are satisfied because
\[
b_e+b_f=\frac{B}{5}<\frac14
\]
throughout the residual regime. Also
\[
\sum_{v\neq x} g_v=\frac{4(1-B)}{5}.
\]
Substituting into Lemma 1 gives equality at every vertex:
\[
p_x=\frac B5+\frac25\cdot \frac{2B}{5}+\frac{1-B}{17}+\frac8{17}\cdot \frac{4(1-B)}5
=\frac{185-32B}{425}.
\]
Thus the lower bound is exact. ∎

Combining Lemmas 2 and 3 proves
\[
t^*(B)=\frac{185-32B}{425}.
\]

`PROVEN` **Lemma 4.** This LP class succeeds only for
\[
B\le \frac{77}{128}.
\]

**Proof.**
Solve
\[
\frac{185-32B}{425}\ge \frac{39}{100}.
\]
Multiplying by `1700` gives
\[
740-128B\ge 663,
\]
so
\[
B\le \frac{77}{128}.
\]
At `B=75/119`,
\[
\frac{185-32\cdot 75/119}{425}
=\frac{3923}{10115}
\approx 0.38783984<0.39.
\]
∎

`PROVEN` **Lemma 5.** Every `S_5`-equivariant linear map `T:E\to V` is of the form
\[
(Tb)_x=\alpha d_x+\beta B.
\]

**Proof sketch.**
As `S_5`-modules,
\[
V\cong \mathbf 1\oplus \mathbf 4,\qquad E\cong \mathbf 1\oplus \mathbf 4\oplus \mathbf 5.
\]
Hence
\[
\dim \operatorname{Hom}_{S_5}(E,V)=2,
\]
coming from the common irreducible summands `\mathbf 1` and `\mathbf 4`. Concretely these are:
\[
b\mapsto B\mathbf 1,\qquad b\mapsto d.
\]
Therefore every equivariant linear transfer is a linear combination of these two maps. ∎

So any symmetric linear localization is already subsumed by the exhausted LP above.

---

**3. WHAT WAS ACHIEVED**

This is **partial progress**, not a proof of `max p_x\ge 0.39`.

What was rigorously achieved is sharper than a heuristic failure report:

- `PROVEN`: the full uncoupled chamber-marginal LP has exact value
  \[
  \frac{185-32B}{425}.
  \]
- `PROVEN`: it fails on the whole interval
  \[
  \left(\frac{77}{128},\frac{75}{119}\right].
  \]
- `PROVEN`: at the critical boundary it misses by the explicit amount
  \[
  0.39-\frac{3923}{10115}=\frac{437}{202300}\approx 0.00216016.
  \]
- `PROVEN`: every `S_5`-equivariant linear transfer `E\to V` collapses to degree plus total mass, so no symmetric linear refinement can rescue this LP.

So the workshop did **not** close the residual regime, but it did rigorously identify a large dead zone: any proof based only on bad degrees, chamber marginals, and one-chamber `4`-support certificates cannot succeed.

---

**4. OPEN QUESTIONS**

`OPEN` **Problem 1. Fiber-level coupling beyond chamber marginals.**  
Find a valid inequality of the form
\[
\lambda_x \ge \Phi_x(b),
\]
where `\lambda_x` is actual localized good-fiber load through `x`, and `\Phi_x` is not determined solely by `(d_x)` and `(g_v)`. It must cut off the isotropic extremizer
\[
b_e=\frac{B}{10},\qquad g_v=\frac{1-B}{5}.
\]

`OPEN` **Problem 2. Multi-slice overlap theorem.**  
Prove that near-isotropic bad mass forces a positive amount of genuinely multi-slice good fibers, with explicit constant at least
\[
\frac{437}{202300}
\]
at the final vertex-frequency level, or an equivalent chamber-load constant.

`OPEN` **Problem 3. Triangle-stable support obstruction.**  
Any new certificate must already be positive on the triangle atom
\[
b_{12}=b_{13}=b_{23}=\frac B3,\qquad b_e=0\ \text{otherwise},
\]
since all disjoint-pair-only bonuses vanish there. Quantitatively, at `B=75/119` it must recover at least
\[
0.39-\frac{639}{2023}
=\frac{14997}{202300}\approx 0.0741374
\]
for one of vertices `1,2,3`.

`OPEN` **Problem 4. Nonlinear transport from compatibility data.**  
The plausible remaining route is nonlinear: a fiber-incidence expansion theorem, a quadratic transport inequality, or an SDP-type certificate that detects structure erased by linear symmetric maps.

---

**5. KEY INSIGHT**

The single most important idea was this:

`TRANSLATED` **The residual `k=5` problem is not blocked by lack of LP optimization; it is blocked by lack of a coupling theorem.**

Once translated into the label-graph / chamber language, all one-part information can be optimized exactly, and the isotropic profile
\[
b_e=\frac{B}{10},\qquad g_v=\frac{1-B}{5}
\]
is a genuine exact extremizer of that relaxed program. So the missing ingredient is not a better coefficient. It is a new theorem forcing **correlation** between where bad mass sits and where good-fiber support localizes.

That is the sharpest outcome of the workshop.

